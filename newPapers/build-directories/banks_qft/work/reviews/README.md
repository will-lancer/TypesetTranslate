# Review evidence status

Reports produced before the 2026-09-02 convention adaptation are historical
evidence for the original transcription and solution campaign. They do not
authorize the convention-adapted releases. Some retain statements about the
superseded metric by design.

The adapted release is authorized only by the `convention-final-range-*.md`
reports and the checksum-bound `review-coverage-*.json`,
`solution-review-*.json`, `convention-audit-*.json`, and visual-review records.
Each current record must bind the final native snapshot or rendered PDF named
inside it.
