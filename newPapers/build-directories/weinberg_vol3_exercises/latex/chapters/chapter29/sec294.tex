% Source: physical PDF pp. 299--310 (printed pp. 276--287), from the
% Section 29.4 heading through the final paragraph immediately before
% Section 29.5.
% Inventory: Eqs. (29.4.1)--(29.4.23), five unnumbered display groups,
% three ordinary footnotes, six linked citation-marker occurrences
% (references [2], [6], [7], and [8]), and no figures, tables, or
% centered three-asterisk dividers.
% Corrections: the O(N) threshold is N>=7; the index argument concerns
% unbroken supersymmetry at strong and weak coupling; and the SU(3) Cartan
% generators use the t_3,t_8 notation established in the same paragraph.
% The prose following Eq. (29.4.18) is retained as printed.
% Uncertainties: none.

\subsection{Supersymmetry Breaking in Gauge Theories}

We now turn to a question of great physical interest: in what gauge
theories is supersymmetry spontaneously broken?

Let us start with an Abelian gauge theory, the supersymmetric version of
quantum electrodynamics described in Section 27.5. This is a \(U(1)\) gauge
theory with two chiral superfields \(\Phi_\pm\) carrying \(U(1)\) quantum
numbers \(\pm e\), and superpotential
\(f(\Phi)=m\Phi_+\Phi_-\). We saw in Section 27.5 that supersymmetry is
broken in the tree approximation if we include a Fayet--Iliopoulos term
\(\xi[V]_D\) in the Lagrangian density, so the Witten index is zero for
\(\xi\neq0\) and small \(e\), and hence also for all values of \(e\) and
\(\xi\), including \(\xi=0\). Is supersymmetry unbroken for \(\xi=0\)?
It is unbroken in the tree approximation, but how can we tell if
higher-order corrections or non-perturbative effects give the vacuum a
finite energy in this case?

To answer this we shall use a symmetry principle of the theory for
\(\xi=0\), in the way that was described in general terms in Section 29.1.
The symmetry here is charge conjugation: the whole Lagrangian density is
invariant under the charge conjugation transformation of the chiral and
gauge superfields:
\begin{equation}
  C\Phi_\pm C^{-1}=\Phi_\mp\,,
  \qquad
  CVC^{-1}=-V\,.
  \tag{29.4.1}\label{eq:29.4.1}
\end{equation}
There is a massless fermion in the tree approximation, the photino, but if
we take the vacuum to be even under \(C\) then the one-photino state is odd
under \(C\), so these states are not related to each other by multiplication
with the supersymmetry generator. The chiral fermion here has mass \(m\) in
the tree approximation, so for small \(e\) it is not paired with the vacuum
either. With no fermionic state available to pair with, the vacuum state must
have strictly zero energy, at least for \(e\) small enough so that
perturbation theory gives a good qualitative picture of the spectrum. As we
saw in Section 29.1, the zero energy of the vacuum implies that
supersymmetry is not broken. Likewise, the photino must be strictly
massless, since it has no bosonic state with which to pair.

Now, what about values of \(e\) that are so large that perturbation theory
cannot be trusted at all? The Witten index itself is no help here, because it
vanishes. Instead, let us consider the weighted Witten index,
\(\operatorname{Tr}C(-1)^F\). We have seen that for small \(e\) and
\(\xi=0\) the vacuum has zero energy, and there are also two zero-energy
states containing a zero-momentum photino of spin up or down, and a bosonic
zero-energy state containing two zero-momentum photinos of opposite spin.
The vacuum makes a contribution \(+1\) to \(\operatorname{Tr}C(-1)^F\);
the two one-photino states make a contribution \(+2\) (because both \(C\)
and \((-1)^F\) are \(-1\)); and the two-photino state makes a contribution
\(+1\), giving a weighted Witten index
\(\operatorname{Tr}C(-1)^F=4\). This is independent of the value of \(e\),
so even for strong couplings the weighted Witten index is 4, and so
supersymmetry is not broken.

There is a complication here~\hyperref[ch29-ref-2]{[2]}. In counting
zero-momentum states in the tree approximation, we have not considered the
zero-momentum components of the gauge field \(V_\mu(x)\). A constant term
in \(V_0(x)\) is no problem, because it can be removed by a gauge
transformation
\begin{equation}
  V_\mu(x)\longrightarrow V_\mu(x)+\partial_\mu\Lambda(x)\,,
  \tag{29.4.2}\label{eq:29.4.2}
\end{equation}
with gauge parameter \(\Lambda(x)\) proportional to \(x^0\). On the other
hand, we cannot simply remove a constant term in \(V_i(x)\), because this
would require a gauge transformation with \(\Lambda(x)\) proportional to
\(x^i\), which would conflict with the assumed periodicity of the fields
under translations by the box dimension \(L\equiv V^{1/3}\). In the
particular model under consideration here all fields have charges \(\pm e\)
or zero, so this periodicity is preserved if we limit ourselves to a lattice
of gauge transformations with
\begin{equation}
  \Lambda(x)=\frac{2\pi}{eL}\sum_i\ell_i x^i\,,
  \tag{29.4.3}\label{eq:29.4.3}
\end{equation}
where \(\ell_i\) are three positive or negative integers. Therefore although
we cannot remove the zero-momentum components of \(V_i(x)\) by a gauge
transformation, we can freely shift them by amounts \(2\pi\ell_i/eL\). The
tree-approximation Lagrangian (\emph{not} Lagrangian density) for the
\(x^i\)-independent part of \(V_\mu\) in a gauge with \(V_0=0\) is simply
\(-\frac12L^3\sum_i(\partial_0V_i)^2\), so the Hamiltonian is
\(+\frac12L^{-3}\sum_i(\pi_i)^2\), where \(\pi_i\) is the canonical
conjugate to \(V_i\): \(\pi_i=L^3\partial_0V_i\). The wave function
\(\Psi(V)\) for this field is then just like that of a free particle of unit
mass in three dimensions, in a box of linear dimensions \(2\pi/eL\) with
periodic boundary conditions. The wave functions of definite energy are
proportional to \(\exp(\ii\mathbf{k}\cdot\mathbf{V})\), with energy
\(\mathbf{k}^2/2L^3\), and \(k_i=eL\ell_i\), with \(\ell_i\) integers.
There is a unique zero-energy state of this field, with \(k_i=0\) and with a
normalized wave function equal to the constant \((eL/2\pi)^{3/2}\).
Because this state is unique, our counting of zero-energy states is
unaffected by the gauge degree of freedom, and the weighted Witten index is
indeed 4.

Now let us consider the theory of a simple non-Abelian gauge superfield,
without chiral superfields. Witten's 1982
paper~\hyperref[ch29-ref-2]{[2]} on the Witten index presented an argument
that for such theories this index is \(r+1\) (or possibly \(-r-1\)), where
\(r\) is the rank of the gauge group, the maximum number of commuting
generators. In 1997 he found a correction to this
calculation~\hyperref[ch29-ref-6]{[6]}, with the result that for the
classical unitary, orthogonal, and symplectic groups the index is a Casimir
invariant \(C_1\), which for the unitary and symplectic groups is indeed
equal to \(r+1\), but for the orthogonal groups \(O(N)\) with \(N\geq7\) and
the exceptional groups takes a different value. In general, this Casimir
invariant is defined by Eq.~\eqref{eq:17.5.33}:
\begin{equation}
  \sum_{CD}C_{ACD}C_{BCD}=g^2C_1\delta_{AB}\,,
  \tag{29.4.4}\label{eq:29.4.4}
\end{equation}
where \(g\) is a coupling constant, whose definition can be made
unambiguous by specifying that when the generators \(t_A\), \(t_B\), and
\(t_C\) are restricted to the `standard' \(SU(2)\) subalgebra that was used
in the calculation of instanton effects in Section 23.5, the structure
constant is simply \(g\epsilon_{ABC}\). For the classical groups, we have
\begin{equation}
  C_1=
  \begin{cases}
    N   & SU(N)\\
    N-2 & SO(N)\text{ for }N>3\\
    N+1 & USp(2N)
  \end{cases}
  \,.
  \tag{29.4.5}\label{eq:29.4.5}
\end{equation}
For \(SU(N)\) the rank is \(r=N-1\), while for \(USp(2N)\) it is \(r=N\),
so in both cases \(r+1=C_1\). But \(SO(N)\) with \(N>6\) has rank
\(r=(N-1)/2=(C_1+1)/2\) for \(N\) odd and
\(r=N/2=(C_1+2)/2\) for \(N\) even, and the index is \(C_1\), not
\(r+1\). Of course, this does not affect the main conclusion that, since
the Witten index is not zero, supersymmetry is not spontaneously broken.
Kac and Smilga showed in 1999 that the index is also equal to \(C_1\) for
exceptional groups~\hyperref[ch29-ref-7]{[7]}. We shall calculate the
Witten index here only for \(SU(N)\) and \(USp(2N)\) supersymmetric gauge
theories with no chiral superfields, but in the course of this calculation
we will also see why the orthogonal and exceptional groups present special
difficulties.

The general strategy of this calculation is the same as for the Abelian
theories considered previously. We first examine the states of zero energy,
to see if there are any that cannot be paired by action of the supersymmetry
generator. If there are, then as long as the coupling is weak enough so that
the tree approximation gives a good qualitative picture of the spectrum,
we know that these unpaired states really have precisely zero energy. We can
then find some non-zero weighted Witten index, which will be constant even
for stronger couplings, and conclude that supersymmetry is not broken for
any coupling strength.

There is another complication here, that did not enter in the earlier
example of an Abelian theory with charged chiral superfields. As usually
formulated, general renormalizable theories of gauge bosons and gauginos
with no chiral superfields do not contain any dimensionless parameters at
all, so that there is no coupling parameter that can be adjusted to make the
couplings weak. Instead we have a running coupling constant, depending on
the ratio of the energy to a characteristic energy scale \(\Lambda\), like
the scale \(\Lambda\approx200\ \mathrm{MeV}\) for quantum chromodynamics
discussed in Section 18.7. For non-Abelian theories the gauge coupling
constant becomes strong at energies below \(\Lambda\), however weak it may
be at higher energies. But here we are working in a box of volume \(L^3\),
which provides an infrared cut-off at an energy \(\approx1/L\), that is
normally lacking in theories with unbroken gauge symmetries. When we speak
of a weakly coupled gauge theory, it should be understood that we mean one
with a coupling that is small down to energies of order \(1/L\). In this
case, it is essential to be able to argue that supersymmetry is unbroken for
strong as well as weak couplings, in order to be able to draw any
conclusions at all about the realistic limit of infinite volume.

We will work in temporal gauge, where \(V_A^0=0\). The Lagrangian density
\eqref{eq:27.3.1} is then
\begin{equation}
  \mathcal{L}
  =
  -\frac14\sum_{Aij}f_{Aij}^2
  -\frac12\sum_A(\partial_0V_{Ai})^2
  -\frac12\sum_A\left(\bar\lambda_A(\sl{D}\lambda)_A\right)
  +\frac12\sum_A D_A^2\,,
  \tag{29.4.6}\label{eq:29.4.6}
\end{equation}
where, in temporal gauge,
\begin{equation}
  f_{Aij}
  =
  \partial_iV_{Aj}-\partial_jV_{Ai}
  +\sum_{BC}C_{ABC}V_{Bi}V_{Cj}\,,
  \tag{29.4.7}\label{eq:29.4.7}
\end{equation}
\begin{equation}
  (D_i\lambda)_A
  =
  \partial_i\lambda_A+\sum_{BC}C_{ABC}V_{Bi}\lambda_C\,,
  \tag{29.4.8}\label{eq:29.4.8}
\end{equation}
\begin{equation}
  (D_0\lambda)_A=\partial_0\lambda_A\,.
  \tag{29.4.9}\label{eq:29.4.9}
\end{equation}
(As usual, the gauge coupling constant or constants are included as factors
in the structure constants \(C_{ABC}\).) Without chiral superfields, there
is no other dependence on the auxiliary field \(D_A\); since it enters
quadratically, it may be put equal to the value at which the Lagrangian is
stationary, \(D_A=0\), and ignored from now on. Retaining only the
\(x\)-independent modes, the effective Lagrangian becomes
\begin{equation}
  \begin{aligned}
  \int \dd^3x\,\mathcal{L}
  =L^3\Bigg[
    &-\frac14\sum_{Aij}
      \left(\sum_{BC}C_{ABC}V_{Bi}V_{Cj}\right)^2
     -\frac12\sum_{Ai}(\partial_0V_{Ai})^2
  \\
    &-\frac12\sum_{ABCi}
      C_{ABC}\left(\bar\lambda_A\gamma_iV_{Bi}\lambda_C\right)
     -\frac12\sum_A\bar\lambda_A\gamma^0\partial_0\lambda_A
  \Bigg]\,.
  \end{aligned}
  \tag{29.4.10}\label{eq:29.4.10}
\end{equation}
The Hamiltonian is then
\begin{equation}
  \begin{aligned}
  H={}&
  \frac{1}{2L^3}\sum_{Ai}\pi_{Ai}^2
  +\frac{L^3}{4}\sum_{Aij}
    \left(\sum_{BC}C_{ABC}V_{Bi}V_{Cj}\right)^2
  \\
  &+\frac{L^3}{2}\sum_{ABCi}
    C_{ABC}\left(\bar\lambda_A\gamma_iV_{Bi}\lambda_C\right)\,,
  \end{aligned}
  \tag{29.4.11}\label{eq:29.4.11}
\end{equation}
where \(\pi_{Ai}=L^3\partial_0V_{Ai}\) is the canonical conjugate to
\(V_{Ai}\).

The gauge field configurations with zero energy in the tree approximation
are those for which \(\sum_{BC}C_{ABC}V_{Bi}V_{Cj}=0\) for all \(A\),
\(i\), and \(j\). This condition is always satisfied if \(V_{Bi}\) vanishes
for all \(i\) except where \(t_B\) is in a Cartan subalgebra of the gauge
Lie algebra.\footnote{A Cartan subalgebra is any subalgebra spanned by
\(r\)-independent generators \(t_{\mathcal A}\) that commute with one
another, that is, for which \(C_{A\mathcal A\mathcal B}\) vanishes for all
\(t_A\) when \(t_{\mathcal A}\) and \(t_{\mathcal B}\) are in the Cartan
subalgebra, where \(r\) is the rank, the maximum number of such generators.
For instance, for the \(SU(3)\) symmetry of strong interactions discussed
in Section 19.7, the rank is \(r=2\), and the Cartan subalgebra can be
taken to consist of the third component of isospin \(t_3\) and the
hypercharge \(t_8\), which act on the light quarks with the commuting
matrices denoted \(\lambda_3\) and \(\lambda_8\) in
Eq.~\eqref{eq:19.7.2}.} For the unitary and symplectic groups and their
direct products this is the only way that it can be satisfied. The same is
true for the orthogonal gauge algebras \(O(N)\) with \(N\leq6\), which are
all equivalent to symplectic and/or unitary Lie algebras (see the Appendix
to Chapter 15), but not for orthogonal gauge algebras \(O(N)\) with
\(N\geq7\), which is why Witten's original calculation needed correction
for this case.

In the rest of this calculation we will only consider gauge algebras like
the symplectic and unitary algebras for which the condition of zero energy
in the tree approximation does require the \(V_{Bi}\) to vanish for all
\(i\) except where \(t_B\) is in a Cartan subalgebra of the gauge Lie
algebra. With all \(V_{Ai}=0\) except for the \(V_{\mathcal A i}\) for
which \(t_{\mathcal A}\) is in the Cartan subalgebra, the zero-energy modes
of the fermion field are those also with \(\lambda_A=0\), except for the
\(\lambda_{\mathcal A}\) for which \(t_{\mathcal A}\) is in the Cartan
subalgebra.

Now we must count these states. The eigenvalues of the \(t_{\mathcal A}\)
in any representation of a semi-simple Lie algebra are quantized, so by
gauge transformations all values of the non-zero gauge fields
\(V_{\mathcal A i}\) are equivalent to values in a finite box with periodic
boundary conditions. The quantization of these modes is just like that
carried out for the gauge field in the \(U(1)\) model considered above, so
the zero-energy state of these fields is again unique, with a constant wave
function in the box.

The multiplicity of states with zero energy in the tree approximation comes
entirely from the fermion degrees of freedom. It is convenient to use a
two-component notation, in which instead of a four-component Majorana field
\(\lambda_{\mathcal A\alpha}\) for each generator of the Cartan subalgebra,
we have two left-handed fields \(\lambda_{\mathcal A La}\) with
\(a=\pm1/2\), and their right-handed Hermitian adjoints
\(\lambda_{\mathcal A La}^\dagger\). These gaugino fields satisfy the canonical
anticommutation relations
\[
  \left\{
    \lambda_{\mathcal A La},\lambda_{\mathcal B Lb}^\dagger
  \right\}
  =\delta_{\mathcal A\mathcal B}\delta_{ab}
\]
and
\[
  \left\{
    \lambda_{\mathcal A La},\lambda_{\mathcal B Lb}
  \right\}
  =
  \left\{
    \lambda_{\mathcal A La}^\dagger,\lambda_{\mathcal B Lb}^\dagger
  \right\}
  =0\,.
\]
By operating on an arbitrary state vector with as many factors of
\(\lambda_{\mathcal A La}\) as necessary, we can construct a state vector
\(\ket{0}\) which is annihilated by all \(\lambda_{\mathcal A La}\). The
general zero-energy state vector is then a linear combination of products
of the \(\lambda_{\mathcal A La}^\dagger\) acting on \(\ket{0}\).

To see which of these states may be paired with each other by the action of
the supersymmetry generator, we must take account of a symmetry of the
theory. The zero-energy condition, that the gauge and gaugino fields lie
only in directions corresponding to the Cartan subalgebra, is invariant
under the subgroup of the original gauge group consisting of elements
\(h\) that leaves this subalgebra invariant, that is, for which
\(h^{-1}t_{\mathcal A}h\) is a linear combination of the
\(t_{\mathcal B}\). These form a finite group, known as the Weyl group.
For instance, in the defining representation of \(SU(N)\) the Weyl group
consists of permutations of the \(N\) coordinate axes, together with
multiplication by a phase needed to make the transformations unimodular.
These can be represented by products of the finite gauge transformations
\(W(i,j)=\exp(\ii\pi\sigma(ij)/2)=\ii\sigma(ij)\) with \(i\neq j\) that
permute the \(i\)th and \(j\)th coordinate axes, where \(\sigma(ij)\) is
the \(U(N)\) generator with
\([\sigma(ij)]^i{}_j=[\sigma(ij)]^j{}_i=1\), and with all other elements
zero. These transformations induce orthogonal transformations in the space
spanned by the diagonal traceless Hermitian matrices \(t_{\mathcal A}\)
that generate the Cartan subalgebra in the adjoint representation. For
instance, for the group \(SU(2)\) we can take the Cartan subalgebra to
consist of just \(t_3\), and the Weyl group then consists of the unit
element and a single non-trivial gauge transformation
\(W(1,2)=\ii\sigma(1,2)=\ii\sigma_1\), for which
\(W^{-1}t_3W=-t_3\). For \(SU(3)\) the Cartan subalgebra has the two
generators \(t_3\) and \(t_8\), and the Weyl group consists of the six
gauge transformations \(1\), \(W(1,2)\), \(W(2,3)\), \(W(1,3)\),
\(W(1,2)W(2,3)\), and \(W(2,3)W(1,2)\), which generate rotations by
multiples of \(60^\circ\) in the space spanned by \(t_3\) and \(t_8\).

Assuming the vacuum to be invariant under the Weyl group, it can be paired
by action of the supersymmetry generators only with other states that are
invariant under the Weyl group.\footnote{Witten remarked that physical
states are necessarily Weyl-invariant, but we will not need to go into this
here, because the Weyl invariance of the vacuum and of the supersymmetry
generators means that only Weyl-invariant states are relevant to spontaneous
supersymmetry breaking.} This may or may not include the previously
constructed zero-energy state \(\ket{0}\). The condition that the state
\(\ket{0}\) be annihilated by all \(\lambda_{\mathcal A La}\) is obviously
invariant under the Weyl group, so if it is unique this state must furnish a
one-dimensional representation of the Weyl group. The Weyl group always
acts on the generators of the Cartan subalgebra by orthogonal
transformations, so there are two such representations: the invariant
representation, in which each Weyl transformation is represented by unity,
and the pseudoinvariant representation, in which each Weyl transformation
is represented by the determinant of its action on the generators of the
Cartan subalgebra.

Let us first consider the case where \(\ket{0}\) is invariant under the Weyl
group. Obviously no linear combination of the one-fermion states
\(\lambda_{\mathcal A La}^\dagger\ket{0}\) can be invariant under the Weyl group.
There is just one Weyl-invariant linear combination of the two-fermion
states; it is of the form \(U\ket{0}\), where
\begin{equation}
  U\equiv
  \sum_{ab\mathcal A}
  e_{ab}\lambda_{\mathcal A La}^\dagger
  \lambda_{\mathcal A Lb}^\dagger\,.
  \tag{29.4.12}\label{eq:29.4.12}
\end{equation}
(The spin indices \(a\) and \(b\) are contracted with the antisymmetric
tensor \(e_{ab}\) defined by Eq.~\eqref{eq:25.2.9}, because the
anticommutation relations make the product
\(\lambda_{\mathcal A La}^\dagger\lambda_{\mathcal A Lb}^\dagger\)
antisymmetric in
\(a\) and \(b\).) There are various Weyl-invariant linear combinations of
products of three or more generators of the Cartan subalgebra, but the
anticommutation of the \(\lambda_{\mathcal A La}^\dagger\) makes all of them
vanish\footnote{To see this for \(SU(N)\), note that each generator
\(t_{\mathcal A}\) of its Cartan subalgebra may be written as a linear
combination \(t_{\mathcal A}=\sum_i c_{\mathcal A i}T_i\) of generators
\(T_i\) of the Cartan subalgebra of \(U(N)\), with the only non-zero
element of each \(T_i\) being \((T_i)^i{}_i=1\) (with indices not summed).
In order for the \(t_{\mathcal A}\) to be traceless, we must have
\(\sum_i c_{\mathcal A i}=0\). Since the fields
\(\lambda_{\mathcal A La}^\dagger\) transform under the Weyl group like the
\(t_{\mathcal A}\), it follows that for a function
\(\sum_{\mathcal A\mathcal B\cdots}
d_{\mathcal A\mathcal B\cdots}
\lambda_{\mathcal A La}^\dagger\lambda_{\mathcal B Lb}^\dagger\cdots\)
to be
Weyl-invariant, the coefficients \(d_{\mathcal A\mathcal B\cdots}\) must
take the form
\[
  d_{\mathcal A\mathcal B\cdots}
  =
  \sum_{ij\cdots}
  c_{\mathcal A i}c_{\mathcal B j}\cdots D_{ij\cdots}\,,
\]
where the \(D_{ij\cdots}\) are invariant tensors, in the sense that for any
vectors \(u_i\), \(v_j\), etc., the function
\(D(u,v,\ldots)\equiv\sum_{ij\cdots}D_{ij\cdots}u_iv_j\cdots\) is
invariant under permutations of the coordinate axes. The most general such
function is a linear combination of products of the function
\[
  S(x,y,z,\ldots)=\sum_i x_i y_i z_i\cdots\,,
\]
with arguments \(x,y,z,\ldots\) taken as various subsets of the \(u\),
\(v\), etc. But because \(\sum_i c_{\mathcal A i}=0\), in our case the
sum of the components of each vector vanishes, so \(D(u)=S(u)=0\);
\(D(u,v)\) is proportional to \(S(u,v)\); \(D(u,v,w)\) is proportional to
\(S(u,v,w)\); \(D(u,v,w,x)\) is a linear combination of
\(S(u,v,w,x)\), \(S(u,v)S(w,x)\), \(S(u,w)S(v,x)\), and
\(S(u,x)S(v,w)\); and so on. The important point is that, even though the
function \(D(u,v,\ldots)\) may not be symmetric in its arguments (because
different products of \(S\)s may appear with different coefficients), the
functions \(S(u,v,\ldots)\) are symmetric. In our case, the vectors are
the anticommuting quantities
\(u(a)_i=\sum_{\mathcal A}
\lambda_{\mathcal A La}^\dagger c_{\mathcal A i}\),
for which the only non-vanishing \(S\) function is
\(S(u(1/2),u(-1/2))\). With a suitable normalization of the generators,
this is the operator \(U\).} except for powers of \(U\). Also, in the
product of more than \(r\) of the \(U\)s some of the
\(\lambda_{\mathcal A La}^\dagger\) would have to appear twice, so
\(U^{r+1}=0\). We conclude then that the Weyl-invariant states are limited
to the \(r+1\) states
\begin{equation}
  \ket{0},\quad U\ket{0},\quad U^2\ket{0},\quad\ldots,\quad U^r\ket{0}\,.
  \tag{29.4.13}\label{eq:29.4.13}
\end{equation}
These are all bosonic states, and there are no fermionic states with zero
energy in the tree approximation with which they could be paired, so for
sufficiently weak coupling these states must have precisely zero energy,
and supersymmetry is unbroken. Also, the Weyl-invariant Witten index here
is \(r+1\), and this is independent of the coupling strength, so
\emph{supersymmetry is not spontaneously broken whatever the strength of
the gauge coupling}.

In the case where the state \(\ket{0}\) is pseudoinvariant, the only
Weyl-invariant states are of the form
\begin{equation}
  \sum_{\mathcal A,\mathcal B,\ldots}
  \epsilon_{\mathcal A,\mathcal B,\ldots}
  \lambda_{\mathcal A La}^\dagger
  \lambda_{\mathcal B Lb}^\dagger
  \cdots\ket{0}\,,
  \tag{29.4.14}\label{eq:29.4.14}
\end{equation}
where \(\epsilon_{\mathcal A,\mathcal B,\ldots}\) is the totally
antisymmetric tensor of rank \(r\). This is completely symmetric among the
\(a,b,\ldots\), so each state is characterized by the number of these
indices that are \(+1/2\) rather than \(-1/2\), a number that can take any
value from 0 to \(r\), so the number of independent states is \(r+1\).
Depending on whether \(r\) is even or odd, these states are either all
bosonic or all fermionic, so the Witten index here is \(\pm(r+1)\), and
again supersymmetry is not spontaneously broken.

There is an interesting relation between the value of the Witten index
obtained here and ideas of how certain global symmetries become broken.
The Lagrangian for a supersymmetric gauge theory is invariant under the
transformations of a global \(U(1)\) `\(R\)-symmetry,' which change the
left- and right-handed parts of the gaugino fields by opposite phases:
\begin{equation}
  \lambda_{AL}\longrightarrow e^{\ii\varphi}\lambda_{AL}\,,
  \qquad
  \lambda_{AR}\longrightarrow e^{-\ii\varphi}\lambda_{AR}\,,
  \tag{29.4.15}\label{eq:29.4.15}
\end{equation}
with \(\varphi\) an arbitrary real constant phase. The conservation of the
current \(J_5^\mu\) associated with this symmetry is violated by an anomaly
\begin{equation}
  \partial_\mu J_5^\mu
  =
  -\frac{1}{32\pi^2}
  \sum_{ABCD}
  C_{ACD}C_{BCD}
  f_A^{\mu\nu}f_B^{\rho\sigma}
  \epsilon_{\mu\nu\rho\sigma}\,,
  \tag{29.4.16}\label{eq:29.4.16}
\end{equation}
where as usual \(\epsilon_{\mu\nu\rho\sigma}\) is the totally
antisymmetric quantity with \(\epsilon^{0123}\equiv1\). (This is obtained
from Eq.~\eqref{eq:22.2.24} by taking the gauge group generators \(t_A\) as
\((t_A)_{BC}=-\ii C_{ABC}\), because the gauginos are in the adjoint
representation of the gauge group, and by multiplying the anomaly with a
factor \(1/2\), because the gauginos do not have distinct antiparticles.)
With the definition of the gauge coupling specified above,
Eq.~\eqref{eq:23.5.20} gives the integral of the product of field strengths
in the anomaly as
\begin{equation}
  \epsilon_{\mu\nu\rho\sigma}
  \int \dd^4x\sum_A f_A^{\mu\nu}f_A^{\rho\sigma}
  =64\pi^2\nu/g^2\,,
  \tag{29.4.17}\label{eq:29.4.17}
\end{equation}
where the `winding number' \(\nu\) is an integer characterizing the
topological class to which the gauge field belongs. Putting together
Eqs.~\eqref{eq:29.4.4}, \eqref{eq:29.4.16}, and \eqref{eq:29.4.17}, we
see that an instanton of winding number \(\nu\) induces a change in
\(R\equiv\int \dd^3x\,J_5^0\) given by
\begin{equation}
  \Delta R
  =
  \int \dd^4x\,\partial_\mu J_5^\mu
  =-2\nu C_1\,.
  \tag{29.4.18}\label{eq:29.4.18}
\end{equation}
That is, the effective action contains terms
\(\bigl(\sum_{Aab}\lambda_{ALa}\lambda_{ALb}e_{ab}\bigr)^{C_1}\) and
its integer powers that, instead of being invariant under the
\(R\)-symmetry transformations \eqref{eq:29.4.15}, are transformed by
integer powers of the phase \(\exp(2\ii\varphi C_1)\). Thus instantons
invalidate invariance under the general \(U(1)\) \(R\)-transformation
\eqref{eq:29.4.15}, reducing it to the group \(\mathbb{Z}_{2C_1}\) of
transformations \eqref{eq:29.4.15} with \(\varphi\) an integer multiple of
\(\pi/C_1\). We might expect that the growth of the gauge coupling at low
energy would lead (as in quantum chromodynamics) to the appearance of
vacuum expectation values for gaugino bilinears, which would mean that the
discrete symmetry group \(\mathbb{Z}_{2C_1}\) is spontaneously broken to
its \(\mathbb{Z}_2\) subgroup, generated by a simple sign change of the
gaugino multiplet. Then there would be \(C_1\) zero-energy states
\(\ket{n}\), given by acting on any one vacuum state \(\ket{X}\) with the
elements \(\exp(in\pi R/C_1)\) of \(\mathbb{Z}_{2C_1}\), with \(n\)
running only over the values \(0,1,\ldots,C_1-1\) because we treat as
equivalent any pair of states that differ only by action of the generator
\(\exp(\ii\pi R)\) of \(\mathbb{Z}_2\). Since \(\ket{X}\) can only be a
linear combination of states with even values of \(R\), we can form
\(C_1\) states with all values \(\mathcal{R}=0,2,\ldots,2C_1-2\) of \(R\)
by taking the linear combinations
\[
  \sum_n\exp\left(\frac{in\pi\mathcal{R}}{C_1}\right)\ket{n}\,.
\]
In particular, for \(SU(N)\) and \(USp(2N)\) these states are the same as
the Weyl-invariant states encountered in the calculation of the Witten
index in the case where the state \(\ket{0}\) is \(R\)-invariant, and
Weyl-invariant rather than pseudoinvariant. The operator \(U\) defined by
Eq.~\eqref{eq:29.4.12} has \(R=2\), so there are \(r+1=C_1\) states
\(U^n\ket{0}\) with \(R=2n\) running from zero to \(2r=2C_1-2\). More
generally, the presence of \(C_1\) zero-energy states with the same
statistics helps to explain why the Witten index is equal to \(\pm C_1\)
for the exceptional and orthogonal as well as the unitary and symplectic
groups.

The fact that the Witten index is non-vanishing for all pure gauge
supersymmetric theories means that to find examples of spontaneous
supersymmetry breaking we must add chiral superfields to the theory. It
does not help to add \emph{massive} chiral superfields to the theory, since
for weak coupling the introduction of massive fields does not change the
menu of zero-energy states. We have already seen an example of this: the
work of Affleck, Dine, and Seiberg reviewed in Section 29.3 showed that an
\(SU(N_c)\) gauge theory with \(N_f<N_c\) left-chiral superfields
\(Q_{ai}\) in the defining representation of \(SU(N_c)\) and an equal
number of left-chiral superfields \(\bar Q_{ai}\) in the complex conjugate
representation, with a mass term
\(\sum_{aij}m_{ij}\bar Q_{ai}Q_{aj}\), has \(N_c\) zero-energy bosonic
states and no zero-energy fermionic states. (This is one case where the
Witten index is not left unchanged when a mass term vanishes, because this
mass term is the term in the superpotential of highest order in the
superfields, so that a vanishing mass changes the behavior of the
superpotential for large superfields. In fact, Eq.~\eqref{eq:29.3.31}
shows that as the masses go to zero the scalar field values in the state of
zero energy go to infinity.)

On the other hand, there is no difficulty in finding theories with
left-chiral superfields subject to a symmetry that keeps them massless, in
which supersymmetry is dynamically broken. For instance, consider an
\(SU(N_c)\) gauge theory with \(N_f\) left-chiral superfields \(Q_{ai}\)
and \(N_f\) left-chiral superfields \(\bar Q_{ai}\) again in the \(N_c\)
and \(\bar N_c\) representations of the gauge group, but now also with
\(N_f\) left-chiral superfields \(L_i\) that are neutral under the gauge
group \(SU(N_c)\). Assume a global (or weakly coupled local) \(SU(N_f)\)
symmetry, acting on the `flavor' \(i\) indices of the \(Q\)s and the \(L\)s
but not the \(\bar Q\)s, which among other things forbids a mass term
linking the \(Q\)s and \(\bar Q\)s. We take \(Q_{ai}\) and \(L_i\) in the
representations \(N_f\) and \(\bar N_f\) of \(SU(N_f)\), respectively,
while the \(\bar Q\)s are taken to be \(SU(N_f)\) singlets. The only
renormalizable superpotential is then of the form
\begin{equation}
  f(Q,\bar Q,L)
  =
  \sum_{ija}\mathcal{G}_j\bar Q_{aj}Q_{ai}L_i\,,
  \tag{29.4.19}\label{eq:29.4.19}
\end{equation}
where the \(\mathcal{G}_j\) are a set of coupling constants, which by an
\(SU(N_f)\) rotation may be chosen to have only one non-vanishing
component, say the one with \(j=N_f\), which can also be chosen to be
positive. (This is a generalization of a model treated by Affleck, Dine,
and Seiberg~\hyperref[ch29-ref-8]{[8]}, in which they took \(N_c=3\) and
\(N_f=2\).) The gauge-neutral superfields \(L_i\) have no effect on the
non-perturbative terms in the effective superpotential, so we can use the
result of Section 29.3, that for \(N_c>N_f\) the gauge interactions yield a
total effective superpotential
\begin{equation}
  \begin{aligned}
  f_{\mathrm{total}}(Q,\bar Q,L)
  ={}&
  \sum_{ija}\mathcal{G}_j\bar Q_{aj}Q_{ai}L_i
  \\
  &+\mathcal{K}
  \left[
    \operatorname{Det}_{ij}\sum_a\bar Q_{aj}Q_{ai}
  \right]^{-1/(N_c-N_f)}
  \,,
  \end{aligned}
  \tag{29.4.20}\label{eq:29.4.20}
\end{equation}
with \(\mathcal{K}\) a constant. In order for supersymmetry to be unbroken
it is necessary (though not sufficient) that the scalar components
\(q_{aj}\), \(\bar q_{aj}\), and \(\ell_i\) of the chiral superfields
satisfy the condition
\(\partial f_{\mathrm{total}}(q,\bar q,\ell)/\partial\ell_i=0\), so that,
for all \(i\),
\begin{equation}
  \sum_{ja}\mathcal{G}_j\bar q_{aj}q_{ai}=0\,.
  \tag{29.4.21}\label{eq:29.4.21}
\end{equation}
But this tells us that the matrix \(\sum_a\bar q_{aj}q_{ai}\) has a zero
eigenvalue, and therefore has zero determinant, so this is a singular point
of the superpotential \eqref{eq:29.4.20}, at which it is impossible for
\(\partial f_{\mathrm{total}}/\partial q_{ai}\) or
\(\partial f_{\mathrm{total}}/\partial\bar q_{aj}\) to vanish.
Supersymmetry is therefore necessarily broken in this class of models.

For instance, Affleck, Dine, and Seiberg~\hyperref[ch29-ref-8]{[8]} found
the non-vanishing scalar components \(q_{aj}\), \(\bar q_{aj}\), and
\(\ell_i\) of the superfields \(\Phi_{ai}\), \(\bar\Phi_{ai}\), \(L_i\)
at the minimum of the potential in their model with \(N_c=3\) and
\(N_f=2\) to be at the values
\begin{equation}
  \begin{aligned}
    q_{11}=\bar q_{11}
      &=1.286\left(\mathcal{K}/2\mathcal{G}\right)^{1/7}\,,
    \\
    q_{22}=\bar q_{22}
      &=1.249\left(\mathcal{K}/2\mathcal{G}\right)^{1/7}\,,
    \\
    \ell_1&=\sqrt{q_{11}^2-q_{22}^2}\,,
  \end{aligned}
  \tag{29.4.22}\label{eq:29.4.22}
\end{equation}
where the vacuum energy density is
\begin{equation}
  \rho_{\mathrm{VAC}}
  =
  3.593\left(16\mathcal{G}^{10}/\mathcal{K}^4\right)^{1/7}\,.
  \tag{29.4.23}\label{eq:29.4.23}
\end{equation}
Also, \(\mathcal{K}=2\Lambda^7\) for \(\theta=0\).

The fact that supersymmetry is spontaneously broken in this case encourages
us to think that it will be broken by strong gauge forces in a wide range
of asymptotically free gauge theories, and thus lends legitimacy to the
speculations about supersymmetry breaking in Section 28.3.
