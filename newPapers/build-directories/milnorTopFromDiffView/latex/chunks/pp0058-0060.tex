% PDF page 58


\noindent\textit{Proof of Theorem A.}
Given any two regular values $y$ and $z$ for $f$, we can choose rotations
\[
r_t : S^p \to S^p
\]
so that $r_0$ is the identity and $r_1(y) = z$. Thus $f$ is homotopic to $r_1 \circ f$; hence $f^{-1}(z)$ is framed cobordant to
\[
(r_1 \circ f)^{-1}(z) = f^{-1} r_1^{-1}(z) = f^{-1}(y).
\]
This completes the proof of Theorem~A.

The proof of Theorem~C will be based on the following: Let $N \subset M$ be a framed submanifold of codimension $p$ with framing $\mathfrak{v}$. Assume that $N$ is compact and that $\partial N = \partial M = \varnothing$.

\noindent\textbf{Product Neighborhood Theorem.} \textit{Some neighborhood of $N$ in $M$ is diffeomorphic to the product $N \times \mathbb{R}^p$. Furthermore the diffeomorphism can be chosen so that each $x \in N$ corresponds to $(x, 0) \in N \times \mathbb{R}^p$ and so that each normal frame $\mathfrak{v}(x)$ corresponds to the standard basis for $\mathbb{R}^p$.}

\noindent\textbf{Remark.} Product neighborhoods do not exist for arbitrary submanifolds. (Compare Figure~\ref{fig:milnor-17}.)

\begin{figure}[htbp]
\centering
\setcounter{figure}{16}%
\IfFileExists{output/figures/fig17.tex}{\input{output/figures/fig17.tex}}{\input{figures/fig17.tex}}
\caption{An unframable submanifold.}\label{fig:milnor-17}
\end{figure}

\noindent\textit{Proof.}
First suppose that $M$ is the euclidean space $\mathbb{R}^{n+p}$. Consider the mapping $g : N \times \mathbb{R}^p \to M$, defined by
\[
g(x; t_1, \ldots, t_p) = x + t_1 v^1(x) + \cdots + t_p v^p(x).
\]
Clearly $dg_{(x; 0, \ldots, 0)}$ is nonsingular; hence $g$ maps some neighborhood of $(x, 0) \in N \times \mathbb{R}^p$ diffeomorphically onto an open set.

We will prove that $g$ is one-one on the entire neighborhood $N \times U_\epsilon$ of $N \times 0$, providing that $\epsilon > 0$ is sufficiently small; where $U_\epsilon$ denotes the $\epsilon$-neighborhood of $0$ in $\mathbb{R}^p$. For otherwise there would exist pairs $(x, u) \neq (x', u')$ in $N \times \mathbb{R}^p$ with $\|u\|$ and $\|u'\|$ arbitrarily small and with
\[
g(x, u) = g(x', u').
\]


% PDF page 59

Since $N$ is compact, we could choose a sequence of such pairs with $x$ converging, say to $x_0$, with $x'$ converging to $x_0'$, and with $u \to 0$ and $u' \to 0$. Then clearly $x_0 = x_0'$, and we have contradicted the statement that $g$ is one-one in a neighborhood of $(x_0, 0)$.

Thus $g$ maps $N \times U_\epsilon$ diffeomorphically onto an open set. But $U_\epsilon$ is diffeomorphic to the full euclidean space $\mathbb{R}^p$ under the correspondence
\[
u \mapsto u / (1 - \|u\|^2 / \epsilon^2).
\]
Since $g(x, 0) = x$, and since $dg_{(x, 0)}$ does what is expected of it, this proves the Product Neighborhood Theorem for the special case
\[
M = \mathbb{R}^{n+p}.
\]

For the general case it is necessary to replace straight lines in $\mathbb{R}^{n+p}$ by geodesics in $M$. More precisely let $g(x; t_1, \ldots, t_p)$ be the endpoint of the geodesic segment of length $\|t_1 v^1(x) + \cdots + t_p v^p(x)\|$ in $M$ which starts at $x$ with the initial velocity vector
\[
\frac{t_1 v^1(x) + \cdots + t_p v^p(x)}{\|t_1 v^1(x) + \cdots + t_p v^p(x)\|}.
\]
The reader who is familiar with geodesics will have no difficulty in checking that
\[
g : N \times U_\epsilon \to M
\]
is well defined and smooth, for $\epsilon$ sufficiently small. The remainder of the proof proceeds exactly as before.

\noindent\textit{Proof of Theorem C.}
Let $N \subset M$ be a compact, boundaryless, framed submanifold. Choose a product representation
\[
g : N \times \mathbb{R}^p \to V \subset M
\]
for a neighborhood $V$ of $N$, as above, and define the projection
\[
\pi : V \to \mathbb{R}^p
\]
by $\pi(g(x, y)) = y$. (See Figure~\ref{fig:milnor-18}.) Clearly $0$ is a regular value, and $\pi^{-1}(0)$ is precisely $N$ with its given framing.

\begin{figure}[htbp]
\centering
\setcounter{figure}{17}%
\IfFileExists{output/figures/fig18.tex}{\input{output/figures/fig18.tex}}{\input{figures/fig18.tex}}
\caption{Constructing a map with given Pontryagin manifold.}\label{fig:milnor-18}
\end{figure}


% PDF page 60


Now choose a smooth map $\varphi : \mathbb{R}^p \to S^p$ which maps every $x$ with $\|x\| \ge 1$ into a base point $s_0$, and maps the open unit ball in $\mathbb{R}^p$ diffeomorphically\footnote{For example, $\varphi(x) = h^{-1}(x / \lambda(\|x\|^2))$, where $h$ is the stereographic projection from $s_0$ and where $\lambda$ is a smooth monotone decreasing function with $\lambda(t) > 0$ for $t < 1$ and $\lambda(t) = 0$ for $t \ge 1$.} onto $S^p - s_0$. Define
\[
f : M \to S^p
\]
by
\[
f(x) = \varphi(\pi(x)) \quad \text{for } x \in V
\]
\[
f(x) = s_0 \quad \text{for } x \notin V.
\]
Clearly $f$ is smooth, and the point $\varphi(0)$ is a regular value of $f$. Since the corresponding Pontryagin manifold
\[
f^{-1}(\varphi(0)) = \pi^{-1}(0)
\]
is precisely equal to the framed manifold $N$, this completes the proof of Theorem~C.

In order to prove Theorem~B we must first show that the Pontryagin manifold of a map determines its homotopy class. Let $f, g : M \to S^p$ be smooth maps with a common regular value $y$.

\noindent\textbf{Lemma 4.} \textit{If the framed manifold $(f^{-1}(y), f^*\mathfrak{v})$ is equal to $(g^{-1}(y), g^*\mathfrak{v})$, then $f$ is smoothly homotopic to $g$.}

\noindent\textit{Proof.}
It will be convenient to set $N = f^{-1}(y)$. The hypothesis that $f^*\mathfrak{v} = g^*\mathfrak{v}$ means that $df_x = dg_x$ for all $x \in N$.

First suppose that $f$ actually coincides with $g$ throughout an entire neighborhood $V$ of $N$. Let $h : S^p - y \to \mathbb{R}^p$ be stereographic projection. Then the homotopy
\[
F(x, t) = f(x) \quad \text{for } x \in V
\]
\[
F(x, t) = h^{-1}[t \cdot h(f(x)) + (1 - t) \cdot h(g(x))] \quad \text{for } x \in M - N
\]
proves that $f$ is smoothly homotopic to $g$.

Thus it suffices to deform $f$ so that it coincides with $g$ in some small neighborhood of $N$, being careful not to map any new points into $y$ during the deformation. Choose a product representation
\[
N \times \mathbb{R}^p \to V \subset M
\]
for a neighborhood $V$ of $N$, where $V$ is small enough so that $f(V)$ and $g(V)$ do not contain the antipode $\bar{y}$ of $y$.

