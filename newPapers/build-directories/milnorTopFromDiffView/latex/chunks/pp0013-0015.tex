% PDF page 13

\section{Smooth Manifolds and Smooth Maps}

\textsc{First} let us explain some of our terms. $\mathbb{R}^{k}$ denotes the $k$-dimensional euclidean space; thus a point $x \in \mathbb{R}^{k}$ is an $k$-tuple $x = (x_1, \cdots, x_k)$ of real numbers.

Let $U \subset \mathbb{R}^{k}$ and $V \subset \mathbb{R}^{l}$ be open sets. A mapping $f$ from $U$ to $V$ (written $f : U \to V$) is called \textit{smooth} if all of the partial derivatives $\partial^n f / \partial x_{i_1} \cdots \partial x_{i_n}$ exist and are continuous.

More generally let $X \subset \mathbb{R}^{k}$ and $Y \subset \mathbb{R}^{l}$ be arbitrary subsets of euclidean spaces. A map $f : X \to Y$ is called \textit{smooth} if for each $x \in X$ there exist an open set $U \subset \mathbb{R}^{k}$ containing $x$ and a smooth mapping $F : U \to \mathbb{R}^{l}$ that coincides with $f$ throughout $U \cap X$.

If $f : X \to Y$ and $g : Y \to Z$ are smooth, note that the composition $g \circ f : X \to Z$ is also smooth. The identity map of any set $X$ is automatically smooth.

\textsc{Definition.} A map $f : X \to Y$ is called a \textit{diffeomorphism} if $f$ carries $X$ homeomorphically onto $Y$ and if both $f$ and $f^{-1}$ are smooth.

We can now indicate roughly what \textit{differential topology} is about by saying that it studies those properties of a set $X \subset \mathbb{R}^{k}$ which are invariant under diffeomorphism.

We do not, however, want to look at completely arbitrary sets $X$. The following definition singles out a particularly attractive and useful class.

\textsc{Definition.} A subset $M \subset \mathbb{R}^{k}$ is called a \textit{smooth manifold of dimension $m$} if each $x \in M$ has a neighborhood $W \cap M$ that is diffeomorphic to an open subset $U$ of the euclidean space $\mathbb{R}^{m}$.

Any particular diffeomorphism $g : U \to W \cap M$ is called a \textit{parametrization} of the region $W \cap M$. (The inverse diffeomorphism $W \cap M \to U$ is called a \textit{system of coordinates} on $W \cap M$.)


% PDF page 14

\begin{figure}[htbp]
\centering
\setcounter{figure}{0}%
\IfFileExists{output/figures/fig01.tex}{\input{output/figures/fig01.tex}}{\input{figures/fig01.tex}}
\caption{Parametrization of a region in $M$.}\label{fig:milnor-01}
\end{figure}

Sometimes we will need to look at manifolds of dimension zero. By definition, $M$ is a manifold of dimension zero if each $x \in M$ has a neighborhood $W \cap M$ consisting of $x$ alone.

\textsc{Examples.} The unit sphere $S^2$, consisting of all $(x, y, z) \in \mathbb{R}^{3}$ with
\[
x^2 + y^2 + z^2 = 1,
\]
is a smooth manifold of dimension 2. In fact the diffeomorphism
\[
(x, y) \mapsto (x, y, \sqrt{1 - x^2 - y^2}),
\]
for $x^2 + y^2 < 1$, parametrizes the region $z > 0$ of $S^2$. By interchanging the roles of $x$, $y$, $z$, and changing the signs of the variables, we obtain similar parametrizations of the regions $x > 0$, $y > 0$, $x < 0$, $y < 0$, and $z < 0$. Since these cover $S^2$, it follows that $S^2$ is a smooth manifold.

More generally the sphere $S^{n-1} \subset \mathbb{R}^{n}$, consisting of all $(x_1, \ldots, x_n)$ with $\sum x_i^2 = 1$, is a smooth manifold of dimension $n - 1$. For example $S^0 \subset \mathbb{R}^{1}$ is a manifold consisting of just two points.

A somewhat wilder example of a smooth manifold is given by the set of all $(x, y) \in \mathbb{R}^{2}$ with $x \neq 0$ and $y = \sin(1/x)$.

\subsection{Tangent Spaces and Derivatives}

To define the notion of \textit{derivative} $\dd f_x$ for a smooth map $f : M \to N$ of smooth manifolds, we first associate with each $x \in M \subset \mathbb{R}^{k}$ a linear subspace $TM_x \subset \mathbb{R}^{k}$ of dimension $m$ called the \textit{tangent space} of $M$ at $x$. Then $\dd f_x$ will be a linear mapping from $TM_x$ to $TN_y$, where $y = f(x)$. Elements of the vector space $TM_x$ are called \textit{tangent vectors} to $M$ at $x$.

Intuitively one thinks of the $m$-dimensional hyperplane in $\mathbb{R}^{k}$ which best approximates $M$ near $x$; then $TM_x$ is the hyperplane through the origin that is parallel to this.


% PDF page 15

(Compare Figures~\ref{fig:milnor-01} and~\ref{fig:milnor-02}.) Similarly one thinks of the nonhomogeneous linear mapping from the tangent hyperplane at $x$ to the tangent hyperplane at $y$ which best approximates $f$. Translating both hyperplanes to the origin, one obtains $\dd f_x$.

Before giving the actual definition, we must study the special case of mappings between open sets. For any open set $U \subset \mathbb{R}^k$ the tangent space $TU_x$ is defined to be the entire vector space $\mathbb{R}^k$. For any smooth map $f: U \to V$ the derivative
\[
\dd f_x : \mathbb{R}^k \to \mathbb{R}^l
\]
is defined by the formula
\[
\dd f_x(h) = \lim_{t \to 0} \frac{f(x + th) - f(x)}{t}
\]
for $x \in U$, $h \in \mathbb{R}^k$. Clearly $\dd f_x(h)$ is a linear function of $h$. (In fact $\dd f_x$ is just that linear mapping which corresponds to the $l \times k$ matrix $(\partial f_i / \partial x_j)_x$ of first partial derivatives, evaluated at $x$.)

Here are two fundamental properties of the derivative operation:

\begin{enumerate}
\item \textbf{(Chain rule).} If $f : U \to V$ and $g : V \to W$ are smooth maps, with $f(x) = y$, then
\[
\dd(g \circ f)_x = \dd g_y \circ \dd f_x.
\]
In other words, to every commutative triangle
\[
\begin{tikzcd}[column sep=large, row sep=large]
U \arrow[r, "f"] \arrow[dr, "{g \circ f}"'] & V \arrow[d, "g"] \\
& W
\end{tikzcd}
\]
of smooth maps between open subsets of $\mathbb{R}^k$, $\mathbb{R}^l$, $\mathbb{R}^m$ there corresponds a commutative triangle of linear maps
\[
\begin{tikzcd}[column sep=large, row sep=large]
\mathbb{R}^k \arrow[r, "\dd f_x"] \arrow[dr, "{\dd(g \circ f)_x}"'] & \mathbb{R}^l \arrow[d, "\dd g_y"] \\
& \mathbb{R}^m
\end{tikzcd}
\]
\item If $I$ is the identity map of $U$, then $\dd I_x$ is the identity map of $\mathbb{R}^k$.
\end{enumerate}

More generally, if $U \subset U'$ are open sets and
\[
i: U \to U'
\]
is the inclusion map, then again $\dd i_x$ is the identity map of $\mathbb{R}^k$.

Note also:

\begin{enumerate}
\setcounter{enumi}{2}
\item If $L : \mathbb{R}^k \to \mathbb{R}^l$ is a linear mapping, then $\dd L_x = L$.
\end{enumerate}

As a simple application of the two properties one has the following:

If $f$ is a diffeomorphism between open sets $U \subset \mathbb{R}^k$ and $V \subset \mathbb{R}^l$, then $k$ must equal $l$, and the linear mapping
\[
\dd f_x : \mathbb{R}^k \to \mathbb{R}^l
\]
must be nonsingular.

\noindent\textit{Proof.}
The composition $f^{-1} \circ f$ is the identity map of $U$; hence
$\dd(f^{-1})_y \circ \dd f_x$ is the identity map of $\mathbb{R}^k$. Similarly $\dd f_x \circ \dd(f^{-1})_y$ is the identity map of $\mathbb{R}^l$. Thus $\dd f_x$ has a two-sided inverse, and it follows that $k = l$.

A partial converse to this assertion is valid. Let $f : U \to \mathbb{R}^k$ be a smooth map, with $U$ open in $\mathbb{R}^k$.

\noindent\textbf{Inverse Function Theorem.} \textit{If the derivative $\dd f_x : \mathbb{R}^k \to \mathbb{R}^k$ is nonsingular, then $f$ maps any sufficiently small open set $U'$ about $x$ diffeomorphically onto an open set $f(U')$.} (See Apostol [2] or Dieudonn\'e [7].)

Note that $f$ may not be one-one in the large, even if every $\dd f_x$ is nonsingular. (An instructive example is provided by the exponential mapping of the complex plane into itself.)

Now let us define the tangent space $TM_x$ for an arbitrary smooth manifold $M \subset \mathbb{R}^k$. Choose a parametrization
\[
g: U \to M \subset \mathbb{R}^k
\]
of a neighborhood $g(U)$ of $x$ in $M$, with $g(u) = x$. Here $U$ is an open subset of $\mathbb{R}^m$. Think of $g$ as a mapping from $U$ to $\mathbb{R}^k$, so that the derivative
\[
\dd g_u : \mathbb{R}^m \to \mathbb{R}^k
\]
is defined. Set $TM_x$ equal to the image $\dd g_u(\mathbb{R}^m)$ of $\dd g_u$. (Compare Figure~\ref{fig:milnor-01}.)

We must prove that this construction does not depend on the particular choice of parametrization $g$. Let $h : V \to M \subset \mathbb{R}^k$ be another parametrization of a neighborhood $h(V)$ of $x$ in $M$, and let $v = h^{-1}(x)$. Then $h^{-1} \circ g$ maps some neighborhood $U_1$ of $u$ diffeomorphically onto a neighborhood $V_1$ of $v$. The commutative diagram of smooth maps

