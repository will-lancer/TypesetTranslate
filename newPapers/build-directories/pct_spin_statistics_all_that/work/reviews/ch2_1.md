# Chapter 2, Section 2-1 review

Status: transcribed from the scanned source and checked against the page images.

Source packet:

- PDF pages 43--54
- Printed pages 31--42
- Images: `work/source-pages/pdf-043.jpg` through `pdf-054.jpg`
- Image inspection: original-detail review of every page, with enlarged crops for displays and footnotes
- Text extraction: OCR used only as a locator; the page images control the transcription

Files:

- `latex/chapters/chapter02/opening.tex`: Chapter 2 title, Jost epigraph, and opening paragraph from printed page 31
- `latex/chapters/chapter02/sec2_1.tex`: Section 2-1 through the last visible line of printed page 42

## Coverage

The transcription includes the Chapter 2 opening, the section heading, the Dirac delta definition, linear functionals, functions as distributions, support, tempered distributions, Schwartz seminorms and convergence, continuity, the representation by functions of polynomial growth, compactly supported test functions, convergence and bounded sets, distributional derivatives, translations and affine transformations, translation-invariant distributions, multiplication by functions, tensor products, convolution, regularization, and the opening of the Schwartz nuclear theorem.

Equation tags `(2-1)` through `(2-32)` are retained. Displays without a printed number remain unnumbered. The final source sentence deliberately stops at “The only proof of the nuclear theorem which is of a sufficiently”; printed page 43 continues that sentence and belongs to the next packet.

## Visual checks and readings

- Printed page 31, equation (2-1): the final derivative is `(-1)^n` times `d^n f/dx^n` evaluated at `x=0`; the intermediate ellipsis is retained.
- Printed page 32, equation (2-2): both linearity identities and the bracketed explanatory sentence are retained. The support footnote is transcribed at the page bottom.
- Printed page 33, equations (2-4)--(2-7): the multi-index products, derivative operator, double seminorm sum, and limit condition were checked in enlarged crops.
- Printed page 34, equations (2-8)--(2-12): the separate implication display before (2-8), the bracketed consequence of (2-10), the superscript source reference `3` after Gårding--Lions, and the polynomial bound were checked against the scan.
- Printed page 35, equation (2-13): the symbolic integration-by-parts form uses `(-1)^{|k|}` and the range `0 <= |k| <= s`. The compactness footnote and the unnumbered sum of delta derivatives were checked.
- Printed pages 36--37: the notation is the source's script `S` and `D`; the native files use `\mathcal{S}` and `\mathcal{D}` as the project notation conversion. The source's `D' subset S'` relation is transcribed as `\mathcal{S}'\subset\mathcal{D}'`.
- Printed page 38, equations (2-16)--(2-17): the translated distribution is `T_{a_j}`, with the component shift in `f_{-a_j}(x)` retained. The affine action uses `|\det L|^{-1}`.
- Printed page 39, equations (2-18)--(2-23): the common four-vector translation, center/difference coordinates, four components `mu=0,1,2,3`, and the zero-integral test function `chi` were checked in enlarged crops.
- Printed page 40, equations (2-24)--(2-26): the reduction to `T_2`, multiplication `(gT)(f)=T(gf)`, and polynomial derivative bound were checked.
- Printed page 41, equations (2-27)--(2-31): tensor-product association subscripts `S_x(T_y(...))`, convolution with `xi`, the hat on `f`, and the three-line verification were checked against the source image.
- Printed page 42: `h_1(x,y)=h(x+y)`, the cutoff `chi` argument, `h_2(x,y)=chi(x)h(x+y)`, the regularization identity, and the opening of the Schwartz nuclear theorem were checked. Equation (2-32) uses the source's `f'_j` and `f''_j` arguments.

## Editorial and notation decisions

The source's calligraphic/script test-function spaces are rendered as
`\mathcal{S}` and `\mathcal{D}` in keeping with the Weinberg house notation.
No Hilbert-space state vectors occur in this packet, so no Dirac state-ket
conversion is needed. Source line-break hyphenation was reflowed; compound
hyphens such as “quantum-theoretic” were retained. The source's “euclidean”
capitalization and wording remain unchanged.

The superscript `3` after Gårding--Lions is a source bibliographic reference,
not a page footnote, and is retained as `\textsuperscript{3}`. The two actual
dagger footnotes, on printed pages 32 and 35, are retained as LaTeX footnotes.

## Open items

No unresolved scan ambiguity remains for this packet. The only boundary item is
the sentence continuation on printed page 43. The next packet should continue it
before starting Section 2-2, Fourier Transforms.

## Later-audit disposition

`audit_ch2_early.md` independently checked PDF 043--058 and explicitly records
the continuation on PDF 055, the complete Theorem 2-1 statement, its example,
and the Section 2-2 opening. The equation inventory contains `(2-1)` through
`(2-32)` for this packet, with the source markers carried through the boundary.
The focused layout record `layout_sec2_1.md` closes the pilot overfull box after
Eq. (2-25): the local reflow preserves the inline identity and the full draft
log reports zero overfull and underfull entries for the affected section.

Unresolved blockers: none
