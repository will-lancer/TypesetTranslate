# Mathematical review: Chapter 1 written edition

Review status: PASS

Chapter SHA-256:
`df431b6e60ee29d489a91eaa4beb9fc9c61ec198b0cdf5acd8d393996090ec82`

The written-prose revision preserves the reconciled mathematical layer. I
checked every displayed equation against `notes-exact.tex`, the page audits,
and the formula decisions in `reconciliation-notes.md`.

The review covered the vacuum and multiparticle Hamiltonians, oscillator
commutators, state normalization, the interaction examples, the Poincare
coordinate map, the metric-preserving condition, scalar covariance, the
infinitesimal generators, the Poincare algebra, microcausality, the free scalar
expansion, the invariant commutator measure, and the spacelike-commutator
argument.

The current chapter keeps the following reviewed decisions:

- arbitrary spacetime dimension uses
  `\operatorname{diag}(-1,1,\ldots,1)`;
- the handwritten creator notation `a^+` is identified with `a^\dagger`;
- spatial vectors use `\mathbf` throughout;
- the covariant commutator uses the normalized measure `d^D p`, with the
  literal handwritten `d^D p^\mu` preserved only in the exact-note layer;
- the invariant-measure statement is restricted to the proper orthochronous
  Lorentz group;
- the spacelike argument uses simultaneous Lorentz transformation of the
  separation and integration variable.

The voice-restoration pass changed prose only. Every displayed formula and its
placement were checked again after that pass. The mathematical layer still
matches the reviewed note transcription and normalization decisions.

Unresolved blockers: none
