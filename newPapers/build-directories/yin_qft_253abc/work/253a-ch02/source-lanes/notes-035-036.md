# Xi Yin QFT 253a Chapter 2 source capture, physical pages 35-36

## Source identity

The source is physical pages 35 and 36 of
/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf.

The PDF SHA-256 is
9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21, matching
SOURCE_MANIFEST.yaml. PDF text extraction returns an empty result for these
pages. A fresh Poppler render of physical pages 35 and 36 matches the
supplied full-page renders in content and layout.

These pages are handwritten source material. Black ink carries the equations
and labels. Page 35 has blue markup. Page 36 adds blue, purple, and red markup.
Visible handwritten page label: none. The page numbers used here are physical
combined-PDF numbers.

## Page 35

### Exact transcription

The heading reads:

> Consider a general Gaussian integral

The first displayed equation reads:

$$
Z[\vec J]
= \int d^N\vec q\,
e^{-\frac{1}{2}\vec q^{\,T}A\vec q+\vec J\cdot\vec q}
$$

A blue curved arrow points to \(A\). Its label reads 'symmetric matrix'.

The evaluation is written on the next line:

$$
= (2\pi)^{\frac{N}{2}}\cdot(\det A)^{-\frac{1}{2}}\cdot
e^{\frac{1}{2}\vec J^{\,T}A^{-1}\vec J}.
$$

The expectation-value definition follows:

$$
\langle F(\vec q)\rangle :=
\frac{1}{Z[0]}\int d^N\vec q\,
e^{-\frac{1}{2}\vec q^{\,T}A\vec q}\cdot F(\vec q)
$$

$$
= \left.\frac{1}{Z[0]}
F\left(\frac{\partial}{\partial\vec J}\right)\cdot Z[\vec J]
\right|_{J=0}.
$$

The source writes the evaluation subscript as \(J=0\), with the vector arrow
left off the subscript. The derivative in
\(F(\partial/\partial\vec J)\) carries the vector arrow over \(J\).

The example label and one-point function read:

> e.g.

$$
\langle q_n\rangle=0.
$$

The two-point calculation reads:

$$
\langle q_n q_m\rangle
= \left.\frac{\partial}{\partial J_n}
\frac{\partial}{\partial J_m}
e^{\frac{1}{2}J_p(A^{-1})_{pq}J_q}\right|_{J=0}
$$

$$
= (A^{-1})_{nm}.
$$

The four-point calculation begins at the bottom of the page:

$$
\langle q_n q_m q_r q_s\rangle
= \left.\frac{\partial}{\partial J_n}
\frac{\partial}{\partial J_m}
\frac{\partial}{\partial J_r}
\frac{\partial}{\partial J_s}
e^{\frac{1}{2}J^T A^{-1}J}\right|_{J=0}
$$

The vertical evaluation bar and its \(J=0\) subscript reach the lower edge of
the page. The next equality continues at the top of physical page 36.

### Visual semantics

The heading sits near the upper center. The Gaussian integral occupies the
upper half of the page. The blue arrow curves upward and terminates at the
matrix \(A\) in the exponent. The words 'symmetric matrix' sit to the right
of the arrow in blue ink.

The expectation definition appears below the Gaussian evaluation. A centered
black multiplication dot separates the Gaussian weight from \(F(\vec q)\), and
another centered dot separates \(F(\partial/\partial\vec J)\) from
\(Z[\vec J]\). The evaluation bar stands at the far right of the second line.

The example and the two-point result occupy the lower middle. The four-point
line begins below them and runs toward the lower right edge. The page leaves
white space around every equation. Crossed-out terms: none visible.
Correction marks: none visible. Graphs: none visible. Additional prose
fragments: none visible.

### Ambiguities and settled readings

The looped handwritten variable is read as \(q\) throughout. The same glyph
appears in \(\vec q\), \(F(\vec q)\), \(q_n\), and the Wick rule on page 36.
Reading confidence: high.

The vector arrows over \(\vec q\) and \(\vec J\) in the Gaussian definition and
the first expectation formula are source notation. The component formulas use
plain \(J_n,J_m,J_p,J_q\), and \(J=0\) as written.

The source labels \(A\) as a symmetric matrix. Its displayed factors include
\(\det A\) and \(A^{-1}\). Sign or definiteness conditions for \(A\) are
unrecorded on this page.

The factor dots in the Gaussian result and the expectation definition are
centered multiplication dots. The small terminal dots after the second-line
Gaussian result and the two-point result are sentence punctuation.

The page-35 four-point exponent uses plain \(J^T A^{-1}J\). The initial
generating function uses vector arrows on its \(J\) and \(q\). This notation is
retained as written.

## Page 36

### Exact transcription

The page opens with the continuation of the four-point calculation:

$$
= \frac{\partial}{\partial J_n}
\frac{\partial}{\partial J_m}
\frac{\partial}{\partial J_r}
\frac{\partial}{\partial J_s}\cdot
\frac{1}{2}\left(\frac{1}{2}J^T A^{-1}J\right)^2.
$$

The handwritten diagram below this line is transcribed algebraically as:

$$
\frac{1}{8}\times
\left(
\sum_{\text{all possible ways to connect}}
\begin{array}{c}
 n\;--\;\underset{A^{-1}}{\bullet}\;--\;r\\[2pt]
 m\;--\;\underset{A^{-1}}{\bullet}\;--\;s
\end{array}
\right)
\quad\longrightarrow\quad 4!=24\ \text{terms}.
$$

The double hyphens and the two black dots in this display stand for the
hand-drawn contraction bars. The algebraic display records their role. The
colored connecting paths remain part of the visual source record.

The resulting three distinct pairings read:

$$
= (A^{-1})_{nm}(A^{-1})_{rs}
+ (A^{-1})_{nr}(A^{-1})_{ms}
+ (A^{-1})_{ns}(A^{-1})_{mr}.
$$

The next source fragment is a bullet followed by the heading:

> • Wick contraction rule:

The rule is written schematically as:

$$
\langle q_n q_m q_r\cdots\rangle
= \sum_{\text{all inequivalent Wick contractions}}
\quad
\text{(hand-drawn contraction arcs over the displayed }q\text{'s)}.
$$

The final definition reads, with the source over-arc represented by a frown
accent:

$$
\overset{\frown}{q_n\,q_m}:=(A^{-1})_{nm}.
$$

### Visual semantics

The page begins with the continuation equality in black ink. The derivative
operators are followed by a centered black dot and the factor
\(\frac12(\frac12 J^T A^{-1}J)^2\).

The next diagram is enclosed by large black parentheses. A black
\(\frac18\) and a black multiplication sign precede the opening parenthesis.
Inside it, a large purple summation sign sits at the left. Blue labels \(n\)
and \(m\) sit on the left of the diagram. Blue labels \(r\) and \(s\) sit on
the right. Short blue horizontal strokes extend from each label.

Two black horizontal contraction bars lie across the middle. Each bar has a
black dot at its center. The upper bar is labeled \(A^{-1}\) above the bar;
the lower bar is labeled \(A^{-1}\) below it. Purple paths loop between the
four endpoint labels to show the possible connections. The purple annotation
below the diagram reads on two lines:

> all possible
> ways to connect

A red rightward arrow points from the annotation toward the red text:

> 4! = 24 terms

The three-term pairing result is written in black below the colored diagram.
The bullet and 'Wick contraction rule:' are black. The generic rule places the
expectation on the left, a large black summation sign below the equals sign,
and the two-line black caption 'all inequivalent' / 'Wick contractions'
under that summation sign. A separate black schematic at the right shows
several nested over-arching contraction marks above \(q_n q_m q_r\ldots\).

The final contraction mark above \(q_n q_m\) is a flat-topped hand-drawn cap.
It sits immediately before ':=' and \((A^{-1})_{nm}\). The lower portion of
the page remains blank after this definition. Crossed-out terms: none visible.
Correction marks: none visible. Graphs: none visible.

### Ambiguities and settled readings

The colored pairing sketch gives the instruction to sum over all connections.
The overlap of individual purple paths leaves their stroke-by-stroke pairing
order unresolved. The three distinct algebraic pairings below it are clear and
are transcribed exactly.

The source writes '4! = 24 terms' in red. The factor \(1/8\) is black. The page
gives the visual and algebraic sequence without a prose explanation for the
reduction from 24 terms to the three displayed pairings.

The ellipsis after \(q_r\) in the generic Wick-rule expectation has a long
handwritten run of dots. Its exact dot count is indeterminate. It is rendered
as \(\cdots\) in the TeX transcription.

The contraction cap is a hand-drawn horizontal arch with short vertical ends.
\(\overset{\frown}{q_n\,q_m}\) is a TeX surrogate for that mark. A macro name
or formal diagram environment is absent from the source.

The \(A^{-1}\) labels inside the colored sketch are attached to the two black
contraction bars. The final rule identifies a contraction of \(q_n\) with
\(q_m\) as \((A^{-1})_{nm}\).

## Topic boundary and page transition

Pages 35 and 36 form one continuous topic unit. The unit starts with the
general finite-dimensional Gaussian integral, turns the generating function
into an expectation-value operator, evaluates the one-point and two-point
examples, and finishes the four-point example with Wick contractions.

The page break falls inside the four-point derivation after
\(e^{\frac12 J^T A^{-1}J}\big|_{J=0}\). Physical page 36 begins with the
continuation equality and proceeds directly to the general Wick contraction
rule. The transition proceeds without a new heading or notation change.

## Recommended dispositions

| Source item | Recommended disposition |
|---|---|
| 'Consider a general Gaussian integral' | Retain as the opening heading for this source unit. |
| Gaussian integral and determinant evaluation | Preserve in the exact note-equation layer with the vector arrows, centered multiplication dots, the symmetric-matrix annotation, and source order. |
| Expectation definition and generating-function derivative identity | Preserve as an exact equation sequence. Keep \(J=0\) on the evaluation bars as written. |
| One-point and two-point examples | Retain as source examples. The omitted normalization in the displayed derivative evaluation remains source-faithful. |
| Four-point derivation across the page break | Merge into one continuous equation record while retaining physical-page provenance for the two page segments. |
| Colored \(1/8\) pairing diagram | Retain as a visual annotation record with the blue, purple, and red marks. Attach the algebraic three-pairing equation as its exact companion. |
| '• Wick contraction rule:' and the generic contraction sketch | Retain as the introduction of the general rule. Preserve the nested contraction arcs as a diagram source item. |
| Final contraction cap | Retain the source mark in the visual layer. Use the recorded TeX surrogate as a searchable transcription. |
| Source corrections and page labels | Record the visible absence of correction marks and handwritten page labels. |

For later drafting, treat the two pages as one source-composite argument. Keep
the source diagrams available beside the equation layer. Speech or lecture
caption alignment belongs to a separate lane. This file contains the visual
source capture for the two note pages.
