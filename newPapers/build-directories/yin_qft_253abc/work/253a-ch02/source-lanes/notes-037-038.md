# Pass 1 source capture: physical pages 37-38

## Source identity

- Combined source: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`
- Combined source SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`
- Physical pages inspected: 37 and 38
- Local renders inspected: `tmp/pdfs/253a-ch02-source/page-037.png` and `page-038.png`
- The source PDF was rendered directly at 150 dpi and 300 dpi. Both raster pairs match the attached full-page renders in content and page layout.
- `pdftotext -f 37 -l 38 -layout` returns form-feed-only output. The source is a scanned handwritten page, so visual reading governs.

## Physical page 37

### Exact visible transcription

Black bullet and heading:

`Generalization to Gaussian functional integral`

The first mapping uses a vector with an arrow over `q`, a hand-drawn squiggly right arrow, and the function `g`:

```tex
\vec{q}\rightsquigarrow g(\tau) \in \text{linear space of real functions},
\qquad (\text{defined with a suitable measure})
```

The visible prose inside the parenthesis is exactly:

`(defined with a suitable measure)`

The second mapping reads:

```tex
\vec{q}\cdot\vec{J}\rightsquigarrow \int d\tau\,g(\tau)J(\tau).
```

The sentence below it reads:

`Consider the linear operator A,`

The operator definition is:

```tex
A\cdot g(\tau):=(-\partial_\tau^2+\omega^2)g(\tau).
```

The inverse operator is written:

```tex
A^{-1}\cdot g(\tau)=\int d\tau'\,G(\tau,\tau')g(\tau').
```

Blue annotation beside the inverse operator:

`G obeys`

Blue equation:

```tex
(-\partial_\tau^2+\omega^2)G(\tau,\tau')=\delta(\tau-\tau').
```

The Euclidean action line reads:

```tex
S^E=\frac{1}{2}(g,Ag)=\frac{1}{2}\int d\tau\,g(\tau)A\cdot g(\tau)
```

The two-point expectation is written as a fraction. The visible measure in the numerator is `Dg`; the visible measure in the denominator is `Dx`:

```tex
\left\langle g(\tau_1)g(\tau_2)\right\rangle
=
\frac{
  \int Dg\,e^{-\frac{1}{2}(g,Ag)}g(\tau_1)g(\tau_2)
}{
  \int Dx\,e^{-\frac{1}{2}(g,Ag)}
}
```

The source uses a hand-drawn measure `D` rather than a printed calligraphic font. The code above preserves the visible variables. Square brackets are not visible around the page-37 numerator or denominator measures.

### Visual semantics

- The heading has a solid black bullet at the far left.
- Primary writing is black. The `G obeys` label, its curved arrow, and its Green-function equation are blue.
- The first two arrows are hand-drawn squiggly arrows with right-pointing arrowheads. `\rightsquigarrow` records their shape; the exact source macro is unavailable.
- The blue curved arrow starts below the inverse-operator line and points to `G(\tau,\tau')`.
- The parenthetical measure qualification sits below the function-space line and is enclosed by large black parentheses.
- There is no diagram, graph, brace, table, or geometric sketch on this page.
- Visible correction or crossed-out material: none.
- Visible example: none.
- The normalization measure in the displayed expectation is a source-level inconsistency: `Dg` appears above the fraction bar and `Dx` below it.
- Original handwritten page label or handwritten page number: none visible.

## Physical page 38

### Exact visible transcription

The page begins as a continuation of the functional-integral calculation. The prose reads:

`To avoid confusion with normalization in discretizing the integral in \tau, we can obtain the answer using an ∫-by-part trick;`

The sign in `∫-by-part` is visibly an integral sign. The first displayed line has no visible leading equality sign:

```tex
\int [Dg]\,
e^{-\frac{1}{2}\int d\tau\,g(\tau)(Ag)(\tau)}
g(\tau_1)g(\tau_2)
```

The next line begins with an equality sign and reads:

```tex
=\int [Dg]\,g(\tau_2)
\left(-A^{-1}\cdot\frac{\delta}{\delta g(\tau_1)}\right)
e^{-\frac{1}{2}\int d\tau\,g(\tau)Ag(\tau)}
```

Blue annotation above the derivative:

`functional derivative`

The blue annotation has a short curved arrow pointing to `\delta/\delta g(\tau_1)`.

At the left of the following line, black handwriting says `∫ by part` and has a double underline. The formula on that line reads:

```tex
\int [Dg]\,
e^{-\frac{1}{2}\int d\tau\,g(\tau)Ag(\tau)}
A^{-1}\cdot\frac{\delta}{\delta g(\tau_1)}g(\tau_2)
```

The source line has no separately visible leading equality sign immediately before this integral. Its equality relation comes from the preceding displayed line and the by-parts annotation.

Blue callout for the functional derivative acting on `g(\tau_2)`:

```tex
A^{-1}\cdot\delta(\tau_1-\tau_2)
=\int d\tau'\,G(\tau_1,\tau')\delta(\tau'-\tau_2)
=G(\tau_1,\tau_2).
```

The final black conclusion reads:

```tex
\Rightarrow
\left\langle g(\tau_1)g(\tau_2)\right\rangle
=\overline{g(\tau_1)g(\tau_2)}
=G(\tau_1,\tau_2).
```

### Visual semantics

- The page is written in black and blue ink. The prose, integral calculation, by-parts label, and final result are black.
- The `functional derivative` label is blue and points downward to the functional derivative.
- The words `∫ by part` are black and double-underlined at the left margin of the relevant calculation line.
- A blue curved bracket or callout marks the black operator expression `A^{-1}\cdot(\delta/\delta g(\tau_1))g(\tau_2)`.
- The blue kernel calculation is placed beneath that callout and ends with `=G(\tau_1,\tau_2).`
- The final black line uses a rightward implication arrow, angle brackets for the expectation, and an overbar over `g(\tau_1)g(\tau_2)` before the equality to `G(\tau_1,\tau_2)`.
- There is no diagram, graph, table, or independent worked example.
- Visible correction or crossed-out material: none.
- Qualification on this page: the normalization warning and the use of the integral-by-part trick.
- Original handwritten page label or handwritten page number: none visible.

## Ambiguities and source cautions

1. The page-37 normalized expectation visibly uses `Dg` in the numerator and `Dx` in the denominator. Treat this as a `SOURCE_CONFLICT` until an independent source resolves it. The capture preserves both variables.
2. The page-38 functional measure is enclosed in square brackets, written visibly as `[Dg]`. The page-37 fraction uses `Dg` and `Dx` without visible square brackets. Preserve the page-local forms.
3. The source alternates among `A\cdot g(\tau)`, `(Ag)(\tau)`, and `Ag(\tau)`. These are copied at their source locations. No notation normalization belongs in this lane.
4. The mapping arrows are wavy. `\rightsquigarrow` is a faithful TeX surrogate for the visual mark, with medium confidence in the macro choice.
5. The `∫-by-part trick` wording uses the integral glyph itself. The source spelling is captured as written. A later typeset form should preserve the pun or record its removal as an editorial decision.
6. The lower-case function letter is read as `g` throughout. The adjacent finite-dimensional notes use `q` for the vector and the local formulas consistently use `g` for the function. Confidence: high.
7. The overbar in the final line spans the product `g(\tau_1)g(\tau_2)`. It denotes the averaged product as drawn. The angle-bracket expression and the overbar expression are both retained.

## Topic boundaries and page transitions

- Physical page 36 ends the finite-dimensional discussion with the heading `Wick contraction rule:` and the definition of the contraction. Physical page 37 starts a new black-bullet item, `Generalization to Gaussian functional integral`.
- Page 37 moves from the vector-to-function replacement and the source pairing to the differential operator `A`, its inverse kernel `G`, the Euclidean action, and the normalized two-point functional integral.
- Page 38 continues the page-37 expectation without a new heading. It introduces the normalization warning, differentiates the exponential functionally, performs the marked by-parts step, evaluates the inverse operator on a delta function, and closes the coordinate-space result.
- Physical page 39 begins a new route with `Alternatively, can work with the Fourier transformed variable`. Its Fourier-transform formulas and residue evaluation lie outside this lane. The page-38 result is the input to that next calculation.

## Recommended dispositions

### Page 37

- Include the heading, function-space replacement, source pairing, operator definition, inverse-kernel definition, Green-function equation, Euclidean action, and two-point functional-integral expression in the Chapter 2 Gaussian-functional-integral argument.
- Preserve the parenthetical `(defined with a suitable measure)` as a mathematical qualification.
- Preserve the blue `G obeys` annotation as source evidence attached to the Green-function equation. Its content belongs in the derivation.
- Preserve the page-local `Dg` and `Dx` readings in the frozen source packet. Resolve the denominator before any canonical mathematical typesetting.
- Carry the page-37-to-page-38 equality into the next source unit. The normalized expectation is introduced here and evaluated on page 38.

### Page 38

- Merge the page into the same Gaussian-functional-integral argument. Keep the source order from the normalization warning through the final equality.
- Preserve the functional derivative, the by-parts step, the delta-function action of `A^{-1}`, and the final identification with `G(\tau_1,\tau_2)` as substantive mathematics.
- Record `functional derivative`, `∫ by part`, and the blue operator callout as handwritten explanatory annotations. A later prose pass can decide how much of their visual wording enters print.
- Preserve the `∫-by-part trick` phrase for voice review. It carries source personality and names the operative calculation.
- Mark the page-38 conclusion as the coordinate-space Green-function result feeding the Fourier-space calculation on page 39.

## Lane disposition

- Source pages covered: physical pages 37-38
- Written-use outcome: included in the Gaussian functional integral argument, with the measure inconsistency held for source-conflict review
- Source material withheld from interpretation: only the unresolved identity of the page-37 denominator measure
- Figures or diagrams requiring separate capture: none
- Original handwritten page labels requiring recovery: none visible
