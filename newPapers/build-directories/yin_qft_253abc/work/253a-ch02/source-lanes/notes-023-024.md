# Pass 1 source capture: combined PDF physical pages 23-24

## Evidence and identity

- Combined source PDF: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`
- Source PDF SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`
- Physical pages inspected: 23 and 24.
- Full-page renders: `tmp/pdfs/253a-ch02-source/page-023.png` and `tmp/pdfs/253a-ch02-source/page-024.png`.
- Direct renders made from the source PDF with Poppler match the attached full-page renders.
- `pdftotext` returns no text for physical pages 23-24. The rendered handwriting governs this capture.
- Original handwritten page label: no page number or other handwritten page label is visible on either page.

The pages continue the derivation begun on physical page 22. Page 22 ends with time evolution by T in N steps and the interval T/N; page 23 begins with the transition amplitude. Page 23 ends with the discretized phase-space expression and its O(hbar,(T/N)^2)-type remainder; page 24 takes the formal N→∞ limit. Page 24 ends with two time-lattice prescriptions and the associated H, H' ordering issue. Physical page 25 resumes with the case in which H(q,p) is quadratic in p and Gaussian p(t) integration.

## Physical page 23

### Page-level visual record

- Primary writing: black ink.
- Explanatory additions: blue ink.
- The label `classical Hamiltonian` and its arrow are magenta.
- The page has a single continuous equation sequence. There is no title, section heading, graph, or standalone diagram. Explicit strikeouts, overwrites, and correction marks are absent.
- A long blue curved arrow runs from the product of short-time matrix elements to the phase-space insertion line below it.
- A blue brace under the short-time matrix element marks the factor expanded by momentum completeness.
- Blue braces under the two factors in the momentum-inserted line identify the two overlaps/matrix elements. The first brace has a small blue mark that looks like `11`; the second has a small double-wavy mark resembling `\(\approx\)` or a handwritten label. Their exact intended labels are unclear.

### Exact handwritten transcription, in page order

The opening black amplitude is:

```tex
\langle q_f\mid U(T)\mid q_i\rangle
```

The next black line reads:

```tex
= \int d^D q_1\,\cdots\,d^D q_{N-1}\,
\langle q_f\mid e^{-\frac{i}{\hbar}\hat H\,\cdot\,\frac{T}{N}}\mid q_{N-1}\rangle
\,\cdots\,
\langle q_1\mid e^{-\frac{i}{\hbar}\hat H\,\cdot\,\frac{T}{N}}\mid q_i\rangle
```

The blue bracketed annotation reads exactly:

```text
[ denote q_f ≡ q_N, q_i ≡ q_0 ]
```

The mathematical fragment inside the bracket is:

```tex
q_f\equiv q_N,\qquad q_i\equiv q_0
```

The next black line is:

```tex
= \int \prod_{n=1}^{N-1} d^D q_n\;\cdot\;
\prod_{n=0}^{N-1}
\langle q_{n+1}\mid e^{-\frac{i}{\hbar}\hat H\,\cdot\,\frac{T}{N}}\mid q_n\rangle
```

The blue one-step momentum insertion written below the long arrow is:

```tex
\int \frac{d^D p_n}{(2\pi\hbar)^D}
\langle q_{n+1}\mid p_n\rangle
\langle p_n\mid e^{-\frac{i}{\hbar}\hat H\,\frac{T}{N}}\mid q_n\rangle
```

The first blue underbrace is under `\(\langle q_{n+1}\mid p_n\rangle\)`. Its displayed evaluation is:

```tex
\langle q_{n+1}\mid p_n\rangle
= e^{\frac{i}{\hbar}p_n\cdot q_{n+1}}
```

The second blue underbrace is under `\(\langle p_n\mid e^{-\frac{i}{\hbar}\hat H\frac{T}{N}}\mid q_n\rangle\)`. The source writes an approximation sign above the resulting factorization and gives:

```tex
\langle p_n\mid e^{-\frac{i}{\hbar}\hat H\,\frac{T}{N}}\mid q_n\rangle
\approx
e^{-\frac{i}{\hbar}H(q_n,p_n)\frac{T}{N}}
e^{-\frac{i}{\hbar}p_n\cdot q_n}
```

The magenta arrow points to `\(H(q_n,p_n)\)` and the magenta annotation reads:

```text
classical Hamiltonian
```

The lower black measure line is:

```tex
= \int \prod_{n=1}^{N-1} d^D q_n
\prod_{n=0}^{N-1} \frac{d^D p_n}{(2\pi\hbar)^D}
```

The final black exponent line is:

```tex
\cdot\exp\left\{
\frac{i}{\hbar}\sum_{n=0}^{N-1}
\left[
p_n\cdot(q_{n+1}-q_n)
-H(q_n,p_n)\frac{T}{N}
+O\left(\hbar,\left(\frac{T}{N}\right)^2\right)
\right]
\right\}
```

The source places the O-term inside the square brackets under the n-sum. The black multiplication dot between hat H and T/N is visibly present in the first displayed kernel line; the blue short-time factor uses tight juxtaposition. The capture keeps that visible distinction.

### Visual semantics and source meaning

The black sequence inserts N-1 intermediate q-states into the amplitude. The blue bracket fixes the endpoint notation q_f=q_N and q_i=q_0. The blue curved arrow selects one short-time factor and inserts a momentum resolution of identity with measure d^D p_n/(2\pi\hbar)^D. The two blue braces separate the position-momentum overlap from the short-time Hamiltonian matrix element. The overlap produces the e^{+ip_n·q_{n+1}/\hbar} factor. The Hamiltonian matrix element is evaluated to first short-time order using the classical Hamiltonian H(q_n,p_n), with the second overlap producing e^{-ip_n·q_n/\hbar}. Their product gives the discrete phase-space term p_n·(q_{n+1}-q_n). The lower black line assembles the factors over all time slices.

The page contains no graph. The blue arrows and braces are derivational annotations. The magenta label identifies the classical Hamiltonian. The source gives no equation number.

## Physical page 24

### Page-level visual record

- The formal continuum expression and the prose immediately around it are black ink.
- The wavy underline under the phase-space measure is blue, with a blue arrow to its definition note.
- The wavy underline under H(q,p) is blue, with a blue arrow to the ambiguity note.
- The regularization note is blue and reads `defined via regularization` followed by `e.g. discretizing time`.
- The ambiguity note is blue and has two hyphen-led lines after the first statement. It includes quoted `counter terms`.
- The alternative time-lattice example is written in gray/graphite.
- The first exact-kernel case is marked with a purple bracket and a purple Hamiltonian matrix-element formula.
- The second exact-kernel case is marked with a blue bracket and a blue Hamiltonian matrix-element formula.
- The final comparison writes H in purple, H' in blue, and the remaining prose in gray/graphite.
- There is no graph, standalone diagram, equation number, or handwritten page label. Explicit strikeouts, overwrites, and correction marks are absent.

### Exact handwritten transcription, in page order

The black introductory line reads:

```text
formally, in N → ∞ "limit"
```

The limit fragment in TeX is:

```tex
N\to\infty
```

The formal black path-integral expression is:

```tex
= \int \mathcal D q\,\mathcal D p\;
e^{\frac{i}{\hbar}\int_0^T dt\,(p\cdot\dot q-H(q,p))}
```

The blue annotation under the phase-space measure reads:

```text
defined via
regularization
e.g. discretizing time
```

The blue annotation pointing to H(q,p) reads:

```text
Contains O(ℏ) ambiguities
- "counter terms"
- these ambiguities
  are tied to the scheme
  of regularization.
```

The source glyph in `O(ℏ)` resembles a small `t` with an `h`-like component. The capture uses `\(\hbar\)` because the same glyph occurs in the page-23 remainder and the surrounding path-integral notation. This remains a source ambiguity.

The gray example label reads:

```text
An example of alternative time-lattice regularization:
```

The first gray time-lattice replacement is:

```tex
\int_0^T dt\;p\cdot\dot q
\longrightarrow
\sum_{n=0}^{N-1}p_n\cdot(q_{n+1}-q_n)
```

The source arrow is a loose right arrow with a wavy lead. `\longrightarrow` records its mathematical direction; the drawn glyph is not a typeset arrow.

The gray `versus` line and the second replacement are:

```tex
\text{versus}
\qquad
\sum_{n=0}^{N-1}p_{n+1}\cdot(q_{n+1}-q_n)
```

The gray prose below the two prescriptions reads:

```text
The exact representation of the time evolution is
```

The first exact-kernel sequence is:

```tex
\cdots\,
\langle q_{n+1}\mid p_n\rangle
\langle p_n\mid e^{-\frac{i}{\hbar}\hat H\,\frac{T}{N}}\mid q_n\rangle
\,\cdots
```

The purple bracket under the second factor is accompanied by the gray phrase:

```text
in the former case, and
```

The purple matrix-element relation is:

```tex
\langle p_n\mid\hat H\mid q_n\rangle
= H(p_n,q_n)\langle p_n\mid q_n\rangle
```

The second exact-kernel sequence is:

```tex
\cdots\,
\langle p_{n+1}\mid q_n\rangle
\langle q_n\mid e^{-\frac{i}{\hbar}\hat H\,\frac{T}{N}}\mid p_n\rangle
\,\cdots
```

The blue bracket under the second factor is accompanied by:

```text
in the latter case
```

The blue matrix-element relation is:

```tex
\langle q_n\mid\hat H\mid p_n\rangle
= H'(p_n,q_n)\langle q_n\mid p_n\rangle
```

The final colored comparison reads:

```tex
H(p_n,q_n) \text{ and } H'(p_n,q_n) \text{ can differ at order } O(\hbar).
```

In the handwriting this last line is prose with H(p_n,q_n) in purple, H'(p_n,q_n) in blue, and `can differ at order O(ℏ).` in gray/graphite.

### Visual semantics and source meaning

The phrase `formally, in N → ∞ "limit"` marks the passage from the finite-N expression on page 23 to a continuum-looking phase-space path integral. The blue measure annotation makes the path-integral measure regulator-defined, with time discretization as the example. The blue H(q,p) annotation records O(hbar) ambiguities, associates them with counterterms, and ties them to the regularization scheme.

The gray time-lattice example samples p·dot q at p_n or at p_{n+1}. The first exact-kernel sequence uses the position-momentum overlap followed by the hat H matrix element. The purple relation gives the corresponding Hamiltonian symbol. The second sequence reverses the order of the overlap and Hamiltonian matrix element. The blue relation gives the primed Hamiltonian symbol. The final line records an order-hbar difference between the two symbols.

## Ambiguities and corrections to carry forward

1. The source writes a glyph that visually resembles `t_h` in the O-terms. The mathematical reading is hbar in page-23 `O(ℏ,(T/N)^2)`, page-24 `Contains O(ℏ) ambiguities`, and the final `O(ℏ)` comparison. Keep the visual ambiguity open for reconciliation.
2. Page 23 writes H(q_n,p_n) in the discrete exponent and in the blue short-time factor. Page 24 visually reads H(p_n,q_n) in the purple matrix-element relation and H'(p_n,q_n) in the blue relation. Preserve the argument order exactly at the source layer. Treat the apparent order change as a source-level notation issue.
3. The first page-23 underbrace carries a small blue mark resembling `11`; the second carries a double-wavy mark. Their labels have no recoverable textual reading.
4. Multiplication dots after hat H appear in the black page-23 kernel lines and are visually compressed in the blue and page-24 kernel lines. Keep the captured formulas separate until notation reconciliation.
5. The page-24 replacement arrow is handwritten and loosely wavy. Its direction is clear; its exact typeset glyph is not specified by the source.
6. The continuum measure is explicitly defined through regularization. No normalization or regulator prescription beyond time discretization is supplied on these pages.

## Topic boundaries and recommended dispositions

- Physical page 23: retain as exact notes evidence for insertion of intermediate q-states, insertion of intermediate p-states, short-time kernel factorization, the classical Hamiltonian identification, and the finite-N phase-space exponent.
- Physical page 24: retain as exact notes evidence for the formal continuum path integral, regulator-defined measure, counterterm qualification, alternative time-lattice prescriptions, and the H versus H' ordering ambiguity.
- Page boundary 22→23: attach page 23 to the preceding time-slicing diagram and the T/N interval definition.
- Page boundary 23→24: keep the finite-N remainder visible before the formal N→∞ line.
- Page boundary 24→25: hand off the regularization discussion to the Gaussian momentum-integration case beginning on page 25.
- Recommended disposition: `NOTES_EXACT` for every displayed formula and every visible annotation. Use `SOURCE_CONFLICT` for the H(q_n,p_n) versus H(p_n,q_n) argument-order issue until a later reconciliation lane resolves it. Preserve the O(hbar) glyph as an ambiguity record rather than silently normalizing it.
