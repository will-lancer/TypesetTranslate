# Pass 1 source capture: physical PDF pages 43-44

## Evidence record

- Chapter span: Physics 253a, Chapter 2, Lagrangian Quantum Mechanics, Path Integrals, and Perturbation Theory.
- Combined source PDF: /Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf
- Source PDF SHA-256: 9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21
- Physical pages inspected: 43 and 44.
- Supplied renders: tmp/pdfs/253a-ch02-source/page-043.png and tmp/pdfs/253a-ch02-source/page-044.png.
- The supplied renders and direct Poppler renders of source-PDF pages 43 and 44 agree in page content and placement.
- Source PDF text extraction: empty for these handwritten pages. Visual inspection governs this capture.
- Original handwritten page label: none visible on either page. The numbers 43 and 44 here are combined-PDF physical page numbers.

## Physical page 43

### Source order and exact prose

The page begins with the right-hand side of an equation continued from physical page 42. The visible equation is followed by a blue qualification. The page then moves to the \(g^2\) term, introduces two Wick-contraction classes, and begins a general statement at order \(g^n\).

The black prose fragments, retained in source order and with source capitalization, are:

    At order g^2.

    Two types of Wick contractions:

    disconnected

    and connected

    More generally, at order g^n,
    each set of Wick contractions can be
    represented by a graph that consists of

The displayed \(g^2\) formula lies between “At order \(g^2\).” and “Two types of Wick contractions:”.

### Exact TeX: continuation equation

~~~tex
= -\frac{g}{8}\int d\tau\,\bigl(G_0(\tau,\tau)\bigr)^2
~~~

The source has an unbounded \(\int d\tau\). The blue arrow points upward to the integral measure. A blue brace sits under \(G_0(\tau,\tau)\), with blue quotation-mark strokes above or beside the word “const”. The handwritten blue annotation reads:

    diverges as \tau -> \pm\infty,
    will interpret later.

The arrow and annotation qualify the integral. The annotation is source ink, separate from the black formula.

### Exact TeX: order \(g^2\)

~~~tex
\frac{1}{2}\left(-\frac{g}{4!}\right)^2
\int d\tau_1,d\tau_2\,
\left\langle
\bigl(\phi(\tau_1)\bigr)^4
\bigl(\phi(\tau_2)\bigr)^4
\right\rangle_0
~~~

The expectation-value subscript is a handwritten \(0\). The comma-like marks between \(d\tau_1\) and \(d\tau_2\) are retained as commas in the source-faithful TeX above.

### Wick-contraction diagrams

The heading is exactly “Two types of Wick contractions:”. The labels are lower-case “disconnected” and lower-case “and connected”.

- The disconnected row shows two separate black X-shaped vertices. Each vertex has four black half-edges meeting at a black central point. Purple contraction curves pair the legs at each individual vertex, giving two purple self-contraction loops per vertex. The two examples are followed by a black plus sign and a hand-drawn ellipsis.
- The connected row shows two black X-shaped vertices joined by purple contraction curves. The first example has an outer purple self-contraction at each vertex and two purple curves running between the vertices. The second example is a connected four-contraction arrangement with long outer purple arcs around the pair and shorter inner arcs between the vertices. The two examples are followed by a black plus sign and a hand-drawn ellipsis.
- The X vertices and all ordinary prose are black. The Wick-contraction curves are purple or magenta. Diagram arrows, labels, and numerical weights: none visible.
- The figure is a visual list of representatives. Separate caption or legend: none visible.

For a diagram-only literal inventory, the two rows read:

    disconnected: [single-vertex double-self-contraction graph]
                  [single-vertex double-self-contraction graph] + ...

    and connected: [two-vertex connected graph with outer self-contractions]
                   [two-vertex connected graph with inter-vertex contractions] + ...

The bracketed descriptions are capture metadata. The handwritten source text appears in the labels and prose transcribed above.

### Page-bottom continuation

The final three black lines are:

    More generally, at order g^n,
    each set of Wick contractions can be
    represented by a graph that consists of

The sentence continues at the top of physical page 44.

### Color, arrows, annotations, and corrections

- Black ink carries the equations, prose, vertex crosses, plus signs, and ellipses.
- Blue ink carries the curved arrow to \(\int d\tau\), the brace under \(G_0(\tau,\tau)\), the “const” note, and the divergence qualification.
- Purple or magenta ink carries every contraction curve in the two rows of diagrams.
- The blue divergence note is the page’s qualification. A forward-looking phrase, “will interpret later.”, is written directly below it.
- Correction marks: none visible. Crossed-out formulas, erased alternatives, and overwritten prose fragments are absent from the page.

## Physical page 44

### Source order and exact prose

The page continues the page-43 sentence:

    K connected components, among them
    there are m_\ell connected graphs that contain
    \ell vertices each, \ell = 1, 2, 3, ...

The next prose block reads:

    For a given such partition of n,
    specified by m_\ell's, the number of ways
    of grouping the n vertices in this pattern
    is

The source then writes a separate line:

    Total contribution is

The lower formula follows immediately.

### Exact TeX: component counts

~~~tex
\sum_{\ell\ge 1}m_\ell=K,\qquad
\sum_{\ell\ge 1}m_\ell\cdot\ell=n.
~~~

The two sums sit on one line. The first states the total number \(K\) of connected components. The second states the total number \(n\) of vertices. A long blue curved arrow rises from the prose below and terminates at the right-hand sum, near its \(n\).

### Exact TeX: number of groupings

~~~tex
\frac{n!}{\prod_{\ell\ge 1}(\ell!)^{m_\ell}\cdot m_\ell!}
~~~

The numerator \(n!\) is centered above a horizontal fraction bar. The denominator begins with \(\prod_{\ell\ge 1}\), followed by \((\ell!)^{m_\ell}\), a centered dot, and \(m_\ell!\). The handwritten denominator scope has parentheses absent around the complete product.

### Exact TeX: total contribution

~~~tex
\frac{1}{n!}\,
\frac{n!}{\prod_{\ell\ge 1}(\ell!)^{m_\ell}\cdot m_\ell!}\,
\prod_{\ell\ge 1}\left(G^{(\ell)}\right)^{m_\ell}
~~~

The source line begins with \(1/n!\), then the grouping-count fraction, then \(\prod_{\ell\ge 1}(G^{(\ell)})^{m_\ell}\).

The final equality is:

~~~tex
=\prod_{\ell\ge 1}\frac{1}{m_\ell!}
\left(\frac{1}{\ell!}G^{(\ell)}\right)^{m_\ell}
~~~

A small blue arrow points upward to \(G^{(\ell)}\) in this final line. The explanatory blue definition starts on physical page 45, outside this lane.

### Visual semantics

- Black ink carries the prose and all formulas.
- Blue ink carries the long arrow to the right-hand component-count equation and the small arrow to \(G^{(\ell)}\).
- Wick-contraction drawing: absent from page 44. The graph content is represented symbolically through \(K\), \(m_\ell\), and \(G^{(\ell)}\).
- Correction marks: none visible. The formulas show continuous handwriting; alternate line: none visible.
- Original handwritten page label: none visible.

## Ambiguities and exact-character flags

1. The field symbol in the \(g^2\) expectation is transcribed as \(\phi\). Its handwritten loop-and-stem shape can also be read as a \(\varphi\)-style glyph at reduced resolution. The surrounding pages use \(\phi\); canonical notation should retain the decision.
2. The first blue annotation has two quotation-mark strokes above “const”. The visible text is captured as “const” with the quote marks preserved in the visual description. The status of an opening or closing quote is unresolved.
3. The blue limit annotation is \(\tau\to\pm\infty\). Enlarged inspection resolves the compact handwritten symbol as \(\pm\).
4. The brace under the first formula appears to cover \(G_0(\tau,\tau)\), with the exponent \(2\) outside the brace. The exact brace endpoints are hand-drawn; retain the mark as a visual annotation, separate from the formula.
5. The source uses comma-like separators in the multiple-\(\tau\) measure. The TeX capture retains \(d\tau_1,d\tau_2\). Spacing-only interpretation can be reviewed against the neighboring source pages.
6. In \(\sum_{\ell\ge 1}m_\ell\cdot\ell=n\), the right-hand glyph is the same \(n\) used in “partition of n” and “n vertices”. Its handwritten shape can resemble \(m\) in the page render. The source context fixes the primary reading as \(n\).
7. The centered dot in \((\ell!)^{m_\ell}\cdot m_\ell!\) is visible. Its intended scope is the product over each \(\ell\), while the handwritten \(\prod\) leaves enclosing braces absent. Keep the visible source form and defer scope normalization.
8. The ellipses after \(\ell=1,2,3\) and after each diagram row are hand-drawn. The notes layer should use \(\ldots\) as the TeX approximation and preserve the approximate dot count in the figure record.
9. Subscript \(0\) is absent from the visible \(G^{(\ell)}\) glyph on page 44. The blue arrow identifies this symbol as a later-defined connected-graph contribution. Keep it distinct from \(G_0(\tau,\tau)\) on page 43.

## Topic boundaries

- Physical page 43 opens in the evaluation of the order-\(g\) vacuum contribution begun on page 42. Its blue note records the large-\(\tau\) divergence and postpones interpretation.
- The page then starts the order-\(g^2\) vacuum expectation and classifies Wick contractions by disconnected and connected graph structure.
- The page-43 bottom sentence and page-44 top continuation form one prose unit about a graph with \(K\) connected components.
- Page 44 turns that graph description into the partition constraints for \(m_\ell\), counts the vertex groupings, and writes the contribution associated with a fixed component pattern.
- The page-44 final \(G^{(\ell)}\) arrow points across the page boundary. Physical page 45 visibly begins the definition “\(G^{(\ell)}\) is the contribution from connected Wick contraction of \(\ell\) vertices”, so the definition belongs to the next source lane.

## Recommended dispositions

### Physical page 43

- Freeze the equations and prose as a NOTES_EXACT source unit.
- Merge the top continuation equation with the order-\(g\) evaluation on physical page 42.
- Keep the blue divergence qualification in the exact-note layer and carry it as an annotation during later drafting.
- Keep “Two types of Wick contractions:” and the lower-case labels as source text.
- The redraw of the purple and black graph examples belongs to a later canonical figure pass. Preserve the two rows, representative ordering, color roles, plus signs, and ellipses.
- Join “represented by a graph that consists of” to the page-44 continuation during argument mapping. Retain the page boundary in provenance.

### Physical page 44

- Freeze the component-count equations, grouping fraction, total-contribution line, and final equality as NOTES_EXACT material.
- Treat the page-43/page-44 sentence as one graph-combinatorics argument unit.
- Keep the visible denominator dot and product scope available for mathematical review before any canonical normalization.
- Carry the arrow from \(G^{(\ell)}\) to the page-45 definition as a cross-page source pointer.
- Use this page as the source boundary immediately before the connected-graph exponentiation and the definition of \(G^{(\ell)}\) on page 45.
