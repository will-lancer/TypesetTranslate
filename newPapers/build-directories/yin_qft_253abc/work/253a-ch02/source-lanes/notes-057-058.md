# 253a Chapter 2 source capture: physical pages 57–58

## Evidence and scope

- Combined source PDF: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`.
- Manifest SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`.
- Verified SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`.
- Scope: combined PDF physical pages 57 and 58, with physical pages 56 and 59 inspected only for the two page boundaries.
- The attached full-page renders are `tmp/pdfs/253a-ch02-source/page-057.png` and `tmp/pdfs/253a-ch02-source/page-058.png`. Their visual content agrees with fresh source-PDF renders.
- `pdftotext -layout` returns no characters for either source page. The raster images govern this capture.
- The sheets are color handwritten notes on white paper. Neither sheet has a visible handwritten page number, folio, date, heading, footer, or equation number.
- No lecture audio, captions, or video frames accompany this lane. The source record below comes from the handwritten pages and the adjacent-page boundary check.

## Physical page 57

### Exact transcription in reading order

At the upper left, above the first algebraic line, there is a disconnected tall blue curved stroke. It resembles an open left parenthesis or a continuation mark. It has no attached text.

The first line begins with violet `2 ×`, followed by blue algebra:

$$
2\times g(\tau)\,g(0)\cdot\frac{g}{8}\int d\tau'\,g(\tau-\tau')\,g(\tau')\,\partial g(\tau')\,\partial g(\tau')
$$

The two derivative factors visibly carry the symbol `\partial` with no written subscript. Four magenta over-bracket-like marks sit above the line. One long mark covers the left and middle portion. A shorter mark lies beneath it as a nested grouping. A small separate mark sits over the middle-right factor. A fourth magenta mark spans the two rightmost derivative factors. These marks are colored grouping or contraction annotations rather than printed delimiters.

The next black line reads:

$$
=\frac{g}{4}\int d\tau'\,G_0(\tau-\tau')G_0(\tau')\left(-\partial^2G_0(0)\right)
$$

The following black line reads:

$$
=\frac{g}{4}\int\frac{dk}{2\pi}\frac{e^{ik\tau}}{(k^2+1)^2}\int\frac{dk'}{2\pi}\frac{k'^2}{k'^2+1}.
$$

A bright-blue curly brace under the entire `k'` integral marks it as `divergent ?!` in bright blue. Beneath that annotation, in dark indigo, the page gives:

$$
G_0(\tau)=\frac{1}{2}e^{-|\tau|}.
$$

$$
-\partial^2G_0(0)=\infty
$$

A red `?!` sits to the right of the last line. The source places the divergence question beside the coincident-point derivative rather than inside the black algebra.

The next gray prose fragment reads exactly:

> The other contributions are finite:

The first tadpole diagram under this sentence has a black horizontal line with a filled black dot at each endpoint. A single black loop rises from and returns to a central vertex on the line. Two short blue insertion marks sit on the horizontal line, one on each side of the central vertex. Blue labels beneath them read `\partial_{\tau'}` and `\partial_{\tau'}`. The diagram is followed by:

$$
=\frac{g}{4}\int\frac{dk}{2\pi}\frac{e^{ik\tau}}{(k^2+1)^2}\cdot k^2\int\frac{dk'}{2\pi}\frac{1}{k'^2+1}.
$$

The next row shows two equivalent tadpole diagrams separated by a black equality sign. In the left diagram, a blue diagonal insertion mark lies just left of the central vertex and a blue vertical mark lies just right of it. In the right diagram, the two blue marks exchange positions. The accompanying gray equation reads:

$$
=\frac{g}{2}\int\frac{dk}{2\pi}\frac{e^{ik\tau}}{(k^2+1)^2}\cdot ik\int\frac{dk'}{2\pi}\frac{ik'}{k'^2+1}=0
$$

The gray words `by symmetry` sit below and to the right of the equality to zero.

The final gray prose fragment on the page reads `Same for`. It is followed by two smaller tadpole diagrams joined by the gray word `and`. The left small diagram has a blue diagonal mark on the left side of its central vertex and a blue vertical mark on the right. The right small diagram has the reversed blue marks. No further formula follows these two diagrams on page 57.

### Visual semantics

- Black ink carries the main equalities, propagator lines, loop diagrams, endpoints, vertices, and the central algebraic evaluation.
- The first contraction line is royal blue. Violet `2 ×` and magenta grouping strokes identify the contraction arrangement around that line.
- The divergence brace and the word `divergent ?!` are bright blue. The formulas for `G_0(\tau)` and `-\partial^2G_0(0)` are darker indigo. The red `?!` is a separate question mark annotation.
- Gray ink carries `The other contributions are finite:`, the finite integral, `by symmetry`, `Same for`, the diagram equality, and the symmetry-vanishing expression.
- Every tadpole drawing uses a horizontal propagator line with two filled endpoints and a loop attached at a filled central point. Blue short marks identify derivative insertions. The first diagram writes both derivative labels; the later diagrams use marks alone.
- The page contains no graph, axes, plotted curve, table, or coordinate grid.

### Ambiguities and source cautions

- The field symbol is visibly a handwritten `g`, consistent with the surrounding page-56 and page-58 notation. The exact capture uses `g` rather than substituting another field symbol.
- The top blue line visibly writes `\partial g(\tau')\,\partial g(\tau')` without visible `\tau'` subscripts on the derivative symbols. The page-56 expansion uses `(\partial_{\tau'}g(\tau'))^2`, and the first diagram labels both insertions `\partial_{\tau'}`. The raw exact line therefore keeps the unsubscripted `\partial`; a later equation layer may record `\partial_{\tau'}` as a source-supported interpretation.
- The black factor `-\partial^2G_0(0)` has no coordinate subscript on `\partial^2`. The exact capture keeps that form.
- The magenta marks above the first blue line have no labels. Their endpoints suggest contraction groupings, yet the page supplies no legend. Preserve the marks as visual evidence rather than assigning each mark a formal Wick-pairing label.
- The isolated blue curved stroke at the top may continue a mark from page 56. Its visible page-57 fragment has no recoverable mathematical attachment.
- The `k'^2` numerator and `k'^2+1` denominator in the divergent loop integral are clear. The source uses the prime on the integration variable throughout the displayed line.

### Topic boundary and recommended disposition

Page 57 continues the page-56 expansion of `\langle g(\tau)g(0)\rangle` at first order in the coupling. The top line evaluates the contraction whose coincident derivative factor produces the divergent momentum integral. The page then turns to the other contractions, marks their finite value, and shows a pair of symmetry-odd contributions that integrate to zero.

Recommended written-use disposition: merge the contraction chain and the finite-contribution calculation into the adjacent perturbation-theory argument. Retain every displayed equation as `NOTES_EXACT` evidence. Retain the tadpole diagrams as equation-audit figures or as a source figure candidate. Preserve the colored divergence question, grouping strokes, and derivative labels in the source packet. The written chapter may state the symmetry result from the exact equation; the colored marks do not supply independent prose.

## Physical page 58

### Exact transcription in reading order

The page begins with the black prose line:

> The divergence is due to `\int` at large `k`.

The next two black lines read:

> The origin of this divergence lies in
>
> the path integral measure

The displayed measure equation is:

$$
[Dg]=\prod_k d\tilde{g}_k
$$

To the right, in light gray parentheses, the page gives the Fourier convention:

$$
g(\tau)=\int\frac{dk}{2\pi}\,\tilde{g}(k)e^{ik\tau}
$$

The light-gray parentheses belong to the handwritten annotation. The measure uses a subscript `k` on `\tilde{g}_k`; the side formula uses the argument form `\tilde{g}(k)`.

In red, the word `regularization` is followed by a red downward arrow. The arrow points to the replacement measure:

$$
\prod_{|k|<\Lambda}d\tilde{g}_k.
$$

The `\Lambda` in this displayed replacement is red. The product sign, restriction, differential, and tilde-bearing coefficient are black.

Lower on the page, a black tadpole diagram has a horizontal line with filled endpoints and a loop attached at a central filled vertex. Three blue momentum arrows point left: two arrows lie on the horizontal line and are labeled `k`, while one arrow follows the loop and is labeled `k'`. The diagram is followed on the right by the black prose, written on two lines:

> Effectively cuts off the k'
>
> `"loop"` integral by `|k'| < \Lambda`,

The `\Lambda` in the inequality is red. A large magenta square-bracket annotation beneath the prose reads exactly:

> [will eventually take `\Lambda → ∞` ...]

The handwritten ellipsis contains three dots. The bracket and all words in this annotation are magenta.

At the bottom, the black definition of the full two-point function reads:

$$
\langle g(\tau)g(0)\rangle\equiv G(\tau)=\int\frac{dk}{2\pi}\frac{e^{ik\tau}}{k^2+1-\Sigma(k)}
$$

The denominator is visibly `k^2+1-\Sigma(k)`. The formula reaches the lower content area without a visible terminal period or equation number.

### Visual semantics

- Black ink carries the prose, the path-integral measure, the tadpole drawing, and the final dressed-propagator definition.
- Gray ink carries only the parenthesized Fourier representation.
- Red ink marks the word `regularization`, its downward arrow, and the cutoff symbols `\Lambda` in the regularized measure and the momentum inequality.
- Blue ink marks the three leftward momentum arrows. The loop arrow is labeled `k'`; the two line arrows are labeled `k`.
- Magenta ink encloses the future-limit note `[will eventually take \Lambda → ∞ ...]` in square brackets.
- The tadpole is a schematic momentum-flow diagram. It has no axes, graph, plot, legend, or numerical scale.

### Ambiguities and source cautions

- The first sentence contains a bare integral sign: `The divergence is due to ∫ at large k.` No integrand is written in that sentence. Preserve the bare symbol and its wording.
- The measure is written with square brackets `[Dg]`, an ordinary `D`, and a product over `k`. The source does not visibly use a calligraphic `\mathcal{D}` in this line.
- The measure coefficient is written `\tilde{g}_k`, while the gray Fourier reminder writes `\tilde{g}(k)`. Preserve that distinction.
- The self-energy symbol in the final denominator is a capital Greek sigma. The adjacent page 59 begins with an explicit `\Sigma(k)=...`, which confirms the reading `\Sigma(k)` for the page-58 denominator.
- The red cutoff glyph is the same `\Lambda` used in the magenta future-limit note. Its color is annotation-level information, not a change in the mathematical symbol.
- The phrase break after `k'` follows the handwritten line break. The next line begins with the opening quotation mark before `loop`.

### Topic boundary and recommended disposition

Page 58 explains the ultraviolet divergence through the path-integral measure, introduces a hard momentum cutoff, and defines the full propagator with a self-energy correction. The page-57 divergence question becomes the page-58 regularization step. The bottom definition leads directly into page 59, which begins `With the cutoff regularization,` and evaluates `\Sigma(k)`.

Recommended written-use disposition: merge the measure equation, cutoff replacement, momentum-flow sketch, and dressed-propagator definition into the adjacent regularization argument. Retain the bare `\int` sentence and the qualification that the cutoff will eventually be taken to infinity. Preserve the gray Fourier reminder and the momentum labels as notation evidence. Treat the colored arrows, red cutoff symbols, and magenta future-limit bracket as source annotations; a later printed figure can encode their momentum-flow content without reproducing the ink colors unless the figure audit requires it.

## Cross-page transition and completeness record

- Page 56 ends with the first-order expansion of `\langle g(\tau)g(0)\rangle`, including the derivative interaction and several contraction diagrams. Page 57 starts inside that calculation, with a continuation mark and the blue contraction line.
- Page 57 shifts from the divergent coincident derivative contraction to the finite remaining contributions. Its final `Same for` pair has no continuation formula on the sheet.
- Page 58 starts with the cause of the divergence and the path-integral measure. It ends with the full propagator written in terms of `\Sigma(k)`.
- Page 59 begins the explicit cutoff-regularized self-energy computation. The page-58 bottom definition therefore belongs to the opening boundary of the self-energy subsection.
- Physical page 57 contains seven displayed mathematical groups, one prose sentence, one prose label, five tadpole diagrams, and the colored grouping and divergence annotations described above.
- Physical page 58 contains four displayed mathematical groups, the cutoff inequality inside prose, one tadpole diagram with three momentum arrows, four prose fragments, and the colored regularization annotations described above.
- No heading, graph, axis, table, page label, equation number, standalone example, or additional handwritten prose is visible within the two requested pages. The visible correction and qualification marks are the red and blue `?!` annotations, `by symmetry`, the finite-contribution statement, and the magenta future-cutoff note.
