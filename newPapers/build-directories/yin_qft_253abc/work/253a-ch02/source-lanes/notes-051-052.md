# Pass 1 source capture: physical pages 51-52

Project: `yin_qft_253abc`  
Course/chapter: Physics 253a, Chapter 2, *Lagrangian Quantum Mechanics, Path Integrals, and Perturbation Theory*  
Combined source: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`  
Source SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`  
Physical pages captured: 51-52  
Source renders: `tmp/pdfs/253a-ch02-source/page-051.png`, `tmp/pdfs/253a-ch02-source/page-052.png`

The two supplied PNGs match fresh Poppler renders of physical pages 51 and 52 byte for byte. The PDF text extraction is empty for these handwritten pages. The visual source governs this lane.

The chapter divider on physical page 20 identifies the enclosed notes as `253a/lecture_notes.pdf, pages 10-51`. Physical page 21 begins source-note page 10. The captured pages therefore map to source-note pages 40 and 41 by sequence:

- physical page 51: inferred source-note page 40;
- physical page 52: inferred source-note page 41.

The inferred source-note numbers come from the divider and page sequence. An original handwritten page numeral is absent from both captured page images.

## Physical page 51 / inferred source-note page 40

### Exact source transcription, in visual order

1. At the top is a black-and-red propagator diagram. A horizontal black line runs from a red endpoint labeled `τ` on the left to a red endpoint labeled `0` on the right. A black vertex at the middle is labeled `τ'` below the line. A single black loop rises from and returns to that vertex.

   The diagram is followed by:

   ```tex
   = -\frac{g}{2}\int d\tau'\,G_0(\tau-\tau')G_0(\tau')G_0(0)
   ```

   The source writes the factors consecutively, with visible spaces between the three Green functions and no visible multiplication dots in this line.

2. The next black handwritten prose fragment reads exactly:

   ```text
   convenient to evaluation using Fourier transf.
   ```

   The lower-case grammar and the abbreviation `transf.` are retained as written.

3. The next displayed calculation is black. Its first line reads:

   ```tex
   = -\frac{g}{2}\int d\tau'\int\frac{dk}{2\pi}\int\frac{dk'}{2\pi}
     \widetilde{G}_0(k)\widetilde{G}_0(k')e^{ik(\tau-\tau')+ik'\tau'}
   ```

   The continuation on the following line reads:

   ```tex
   \cdot\int\frac{dk''}{2\pi}\cdot\widetilde{G}_0(k'')
   ```

   The first visible centered dot begins the continuation from the exponential. A second centered dot lies between the `dk''/(2\pi)` measure and `\widetilde{G}_0(k'')`. The source has three independent Fourier integrations: over `k`, `k'`, and `k''`.

4. The following black line reduces the integral to:

   ```tex
   = -\frac{g}{2}\cdot\frac{1}{2}\int\frac{dk}{2\pi}\cdot
     \frac{e^{ik\tau}}{(k^2+1)^2}
   ```

   The source visibly writes the coefficient as `-g/2 · 1/2`, with a centered dot between the factors. A second centered dot lies between the Fourier measure and the exponential fraction. The denominator is `(k^2+1)^2`.

5. To the right of this reduction is a blue momentum-routing diagram. It has a horizontal black line with black endpoints and one central black vertex carrying a black loop. A blue arrow on the left horizontal segment points left and is labeled `k` below. A blue arrow on the right horizontal segment also points left and is labeled `k'=k` below. The loop has a blue arrow drawn on its upper-left branch and the blue label `k''` above it. The horizontal arrows and loop arrow are annotations to the black diagram; the endpoint dots in this routing sketch are black rather than red.

6. The next black prose reads:

   ```text
   At order g^2, we have
   ```

   The `2` is written as a superscript on `g`.

7. Three black diagrams follow in one horizontal row. Each has a horizontal external line with red endpoint dots.

   - The left diagram has two separate vertices along the line. Each vertex carries one single loop rising above it. The two tadpole-like loops are sequential along the same line.
   - The middle diagram has one central vertex on the line and two loops attached at that same vertex, arranged vertically as a smaller upper loop over a larger lower loop.
   - The right diagram has two vertices on the line. An upper curved line and a lower curved line join the two vertices, while the straight horizontal segment between the vertices remains visible. The two external endpoints are red.

8. A green square-bracket annotation beneath the three diagrams reads exactly:

   ```text
   [Calculate these in Pset 2]
   ```

   The opening and closing square brackets, the capital `C`, and the capitalization `Pset 2` are green.

9. The lower portion begins a new black heading and statement:

   ```text
   The full (perturbative) 2-point function
   ⟨q(τ) q(0)⟩ has the structure
   ```

   In exact TeX form for the mathematical line:

   ```tex
   \langle q(\tau)q(0)\rangle\ \text{has the structure}
   ```

   The source glyphs here are unaccented `q` symbols. The operator hats used on the preceding physical page are absent from this line.

### Page-51 visual semantics

- Ink colors: black for the derivation, diagrams, heading, labels, and equation; blue for the momentum routing and its labels; green for the Pset 2 bracket.
- Red endpoint dots occur on the first tadpole diagram and on all three order-`g^2` diagrams. The blue momentum-routing sketch uses black endpoint dots.
- The first diagram is a one-loop tadpole insertion between external Euclidean times `τ` and `0`, with the vertex integration variable `τ'`.
- The Fourier calculation keeps the three momentum variables visible before collapsing to one external momentum integral. The blue sketch records the routing relation `k'=k` and the loop momentum `k''`.
- The three order-`g^2` graph topologies are presented as source material for Pset 2. The page gives no graph names.
- The heading at the bottom begins the full perturbative two-point-function resummation and continues directly on physical page 52.

### Page-51 ambiguities

- The bottom two-point-function line is transcribed with unaccented `q`. The glyphs do not show a separate hat at the page-51 resolution or in the high-resolution source render. This is worth checking against the later equation layer because physical page 50 writes `\hat q` explicitly.
- The multiplication dots in the Fourier continuation and final reduction are visible. The exact handwritten spacing around them is represented with `\cdot` above.
- The blue loop arrow has a clear arrowhead on the upper-left branch. Its full clockwise or counterclockwise orientation is less legible than the arrowhead location; the label `k''` is clear.
- The three graph drawings have no labels beyond the source prose. Their topologies are recorded visually rather than assigned names.
- Visible corrections, erasures, strike-throughs, or overwritten mathematical characters: none recovered on this page.

### Page-51 recommended dispositions

- The tadpole equation and its Fourier reduction: `NOTES_EXACT`; include or merge into the adjacent self-energy derivation with the measures, signs, factors of `1/2`, and denominator preserved.
- `convenient to evaluation using Fourier transf.`: retain in the exact source layer. In written prose, merge its mathematical function into the Fourier-transform transition.
- The blue routing sketch: retain as equation/figure provenance. Reconstruct the arrows and labels if this derivation is shown as a figure.
- The order-`g^2` graph row: retain as exact source evidence. Later written disposition can be `classroom material removed` for the Pset callout while the graph content remains available to the self-energy argument or exercise lane.
- `[Calculate these in Pset 2]`: source evidence for an exercise pointer. Route to the later problem-set integration lane; retain the exact wording in the pass-1 record.
- `The full (perturbative) 2-point function ...`: topic boundary and opening statement. Merge with the physical-page-52 Dyson-series material.

## Physical page 52 / inferred source-note page 41

### Exact source transcription, in visual order

1. At the top is a black diagram expansion. The leftmost diagram is a horizontal line with red endpoints and a large black oval around the central part. The oval contains several diagonal black hatch strokes. It is followed by an equals sign and the following terms:

   - a bare horizontal line with red endpoints and a blue `G_0` label above its center;
   - a horizontal line with red endpoints, a central black oval labeled `1PI`, and blue `G_0` labels above the left and right external line segments;
   - a horizontal line with red endpoints and two adjacent black ovals, each labeled `1PI`, separated by the line segment between them;
   - a final plus sign followed by a handwritten ellipsis.

   The top-line diagrammatic transcription is:

   ```text
   full two-point diagram = G_0 diagram + G_0--1PI--G_0 diagram
                           + 1PI--1PI chain + ....
   ```

   The source uses diagram labels and colors rather than a symbolic algebraic equation for this line. The final mark appears as a hand-drawn ellipsis with a dot count that is less certain than the preceding terms; `+\cdots` is the semantic TeX representation.

2. A magenta curved arrow points upward to the single central `1PI` oval in the second term. The magenta quoted label reads:

   ```text
   "1-particle-irreducible"
   ```

3. Below this is a large light-gray square-bracket annotation. Its exact prose reads:

   ```text
   would remain connected after cutting
   one propagator, i.e.
   ```

   Two light-gray schematic diagrams sit inside the bracket. The left schematic is a horizontal line with a single loop/tadpole attached at one point. The right schematic is a horizontal line passing through a larger oval loop, with a short interior vertical mark visible near the center. The source gives these as examples under the definition; it gives them no labels.

4. The first black algebraic line under the gray annotation reads:

   ```tex
   = \int\frac{dk}{2\pi}e^{ik\tau}\widetilde{G}_0(k)
     \sum_{n=0}^{\infty}
     \left(\widetilde{G}_0(k)\cdot\Sigma(k)\right)^n
   ```

   The sum has lower limit `n=0` and upper limit `∞`. The source draws a centered dot between `\widetilde{G}_0(k)` and `\Sigma(k)` inside the powered parentheses.

5. The next black line sums the geometric series:

   ```tex
   = \int\frac{dk}{2\pi}e^{ik\tau}
     \frac{1}{k^2+1-\Sigma(k)}
   ```

6. The next black definition reads:

   ```tex
   \Sigma(k) =
   ```

   It is followed by a gray-line diagram consisting of a gray external segment, a central black oval labeled `1PI`, and a gray external segment. Two magenta curved arrows point upward to the two gray external segments. The magenta annotation under the diagram reads on two lines:

   ```text
   not including external propagators
   "amputated graph"
   ```

   The words `amputated graph` are enclosed in magenta quotation marks. The gray line segments and the magenta arrows visually mark the external propagators that are excluded from `\Sigma(k)`.

7. A second black line expands `\Sigma(k)` diagrammatically:

   ```text
   = one-loop tadpole diagram + two-loop same-vertex diagram
     + two-vertex upper-and-lower-connection diagram + ....
   ```

   The diagrams have gray horizontal line segments and black interaction vertices/loops. The first has one loop attached at one central vertex. The second has two vertically stacked loops attached at the same central vertex. The third has two central vertices joined by an upper curved connection and a lower curved connection, with the straight line segment between the vertices visible. A plus sign and a hand-drawn ellipsis follow.

8. A green brace beneath the second and third diagrams is labeled:

   ```text
   homework
   ```

   A second green brace lies above the order-`g^2` term in the final equation and marks the same homework contribution.

9. The final black algebraic line reads:

   ```tex
   = -\frac{g}{4}
     +\frac{g^2}{8}\left(\frac{1}{4}+\frac{1}{k^2+9}\right)
     +O(g^3).
   ```

   The green brace above the middle term covers the `g^2` contribution in parentheses. The `+O(g^3)` term is black and lies outside the green brace.

### Page-52 visual semantics

- Ink colors: black for the main diagrams, `1PI` labels, equations, and `\Sigma(k)` label; blue for `G_0` labels in the top expansion; magenta for the `1-particle-irreducible` arrow, the amputated-graph annotation, and the two arrows marking the excluded external segments; light gray for the connectedness definition and its example diagrams; green for the homework braces and the word `homework`; red for external endpoint dots in the top expansion.
- The large hatched oval at the upper left stands for the full perturbative two-point function introduced at the bottom of physical page 51.
- The second top-line term factors the full insertion into a left free propagator, a `1PI` insertion, and a right free propagator. The third term chains two `1PI` insertions with free-propagator segments between the external points and insertions.
- The magenta arrow identifies the central insertion as `1-particle-irreducible`.
- The gray bracket supplies the connectedness-after-cutting-one-propagator qualification. Its two gray diagrams are explanatory sketches rather than separately labeled equations.
- The self-energy definition explicitly removes the two external propagators. The magenta arrows point to those line segments, and the quoted phrase identifies the remaining `1PI` object as an `amputated graph`.
- The first `\Sigma(k)` graph is the one-loop order-`g` term. The two graphs bracketed as `homework` are the order-`g^2` terms. The final expression records the computed result through `O(g^3)`.

### Page-52 ambiguities

- The top and self-energy diagram ellipses are hand-drawn dot sequences. They are recorded semantically as `+\cdots`; the exact number and spacing of dots are uncertain.
- The first large top-left oval contains diagonal hatching. The exact hatch-stroke count carries no stated mathematical content.
- The two light-gray connectedness examples are schematic. The left loop attachment and the right oval-with-line drawing are clear at the topology level; the short interior mark in the right example may represent the cut location or a stroke in the sketch.
- The ordinary `O(g^3)` glyph is transcribed as a plain `O`. The source does not visibly distinguish it as a calligraphic `\mathcal{O}`.
- The `1PI` labels are clear. The source gives no expansion equation number and no separate symbol for the diagrammatic full two-point function.
- Visible corrections, erasures, strike-throughs, or overwritten mathematical characters: none recovered on this page.

### Page-52 recommended dispositions

- The top diagrammatic expansion and the `1PI` definition: `NOTES_EXACT`; include or merge into the written Dyson/self-energy discussion, preserving the insertion order and the external `G_0` factors.
- The gray connectedness annotation: merge the exact qualification into the definition of one-particle irreducibility. Preserve the two gray sketches as figure provenance if a diagram is reconstructed.
- The geometric-series formula and denominator: `NOTES_EXACT`; preserve `k^2+1-\Sigma(k)`, the `n=0` lower limit, the Fourier measure, and the exponent.
- The amputated-graph annotation: include the definition of `\Sigma(k)` and preserve the distinction between the 1PI object and its external propagators.
- The `homework` braces: `classroom material removed` from the main exposition. Keep the bracketed order-`g^2` graphs and their exact result in the exercise/source layer.
- The final self-energy formula: `NOTES_EXACT`; include or merge into the perturbative self-energy calculation with the sign, factors, denominator `k^2+9`, and `O(g^3)` term unchanged.

## Topic boundaries and page transitions

### Incoming transition from physical page 50

Physical page 50 ends the free-propagator setup. It gives the Euclidean Green function diagram, `G_0(\tau)=\frac12e^{-|\tau|}`, and the Fourier representation `\widetilde{G}_0(k)=1/(k^2+1)`. Physical page 51 begins the one-loop tadpole correction obtained by inserting one interaction vertex at `\tau'`.

### Page 51 to physical page 52

Physical page 51 moves from the explicit first-order tadpole integral to the order-`g^2` graph inventory, then introduces the full perturbative two-point function. Physical page 52 continues immediately with its diagrammatic expansion into free propagators and `1PI` insertions, sums the geometric series, and defines the self-energy.

### Outgoing transition to physical page 53

Physical page 52 ends with the perturbative self-energy result. Physical page 53 begins `In conclusion:` and substitutes the self-energy into the full propagator, writing `\langle q(\tau)q(0)\rangle\equiv G(\tau)` and the corresponding Fourier-space `\widetilde G(k)`. The page-52 final `\Sigma(k)` equation is therefore the input to the page-53 conclusion.

## Capture completeness and disposition index

The captured material contains two explicit prose fragments on page 51, the full first-order tadpole equation and Fourier reduction, one momentum-routing sketch, one order-`g^2` graph row, one Pset annotation, the full two-point-function boundary statement, the page-52 diagrammatic Dyson expansion, the quoted `1-particle-irreducible` definition, the gray connectedness qualification and two examples, the geometric-series equation, the denominator equation, the amputated-graph definition of `\Sigma(k)`, the self-energy graph expansion, the `homework` annotations, and the final perturbative self-energy formula. All visible colors, endpoint marks, arrows, loop structures, brackets, labels, quotation marks, and ellipses are recorded above.

Recommended later handling: use the equations and definitions as exact note evidence; merge the page-51 Fourier transition and page-52 1PI/self-energy material into the surrounding written argument; route the Pset 2 and `homework` markings to the exercise/classroom-material disposition; preserve the graph topology and momentum labels in figure provenance; carry the listed uncertainties into the equation and figure reconciliation passes.
