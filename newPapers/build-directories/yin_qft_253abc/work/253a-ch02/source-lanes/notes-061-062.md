# Pass 1 source capture: Xi Yin QFT Chapter 2, physical pages 61-62

Source: `qft_253abc_book.pdf`, combined PDF physical pages 61-62. `CHAPTER_PLAN.md` places pages 20-62 in Chapter 2, with the assignment boundary at page 63.

Evidence inspected: the two supplied full-page renders at `tmp/pdfs/253a-ch02-source/page-061.png` and `page-062.png`, plus fresh raster renders from the manifest PDF at 300 dpi. The supplied renders and the source-PDF renders carry the same page content. `pdftotext -f 61 -l 62 -layout` returns empty text apart from page separators, so the raster handwriting controls this capture.

Original handwritten page labels: no page label is visible on either physical page. There is no heading on either page.

## Physical page 61

### Exact source transcription

The line breaks below follow the handwritten layout. TeX is used for the mathematical marks.

```text
* The path integral is only defined provided
  regularization via the cutoff \Lambda.

Choosing c = \frac{\Lambda}{4\pi} + finite, and
take the limit \Lambda \to \infty in the end,
we find a finite result for \Sigma(k).

* The finite part of c represents an
  ambiguity in quantization, i.e. the
  possibility of adjusting the quantum
  Hamiltonian by a term of order \hbar.

\widetilde{G}(k) = \frac{\hbar}{k^2+1-\Sigma(k)} has pole at k=i\omega_1,

\omega_1 = 1 + \hbar g\left(-\frac{\Lambda}{8\pi}+\frac{c}{2}+\frac{1}{8}\right) + O(g^2).

= \widehat{E}_1-\widehat{E}_0
```

### Formula layer

```tex
\Lambda
c = \frac{\Lambda}{4\pi} + \text{finite}
\Lambda \to \infty
\Sigma(k)
\hbar
\widetilde{G}(k) = \frac{\hbar}{k^2+1-\Sigma(k)}
k = i\omega_1
\omega_1 = 1 + \hbar g\left(-\frac{\Lambda}{8\pi}+\frac{c}{2}+\frac{1}{8}\right) + O(g^2)
\widehat{E}_1-\widehat{E}_0
```

The final relation is written on the source as `= \widehat{E}_1-\widehat{E}_0`, following the `\omega_1` line above it. Both energy symbols carry visible hats.

### Visual semantics and annotations

The page uses a white field with black handwritten text as the base layer. The first and second prose blocks begin with black asterisks. The propagator and energy equations are written without a bullet.

The word `defined` is red. The cutoff `\Lambda` in the first sentence is red. In the `c` choice, the standalone `c` is purple or magenta, the numerator `\Lambda` is red, and `4\pi` plus the fraction bar are black. In the limit sentence, `\Lambda` is red and the arrow `\to` is black. The `c` in “The finite part of c” is purple or magenta.

The `\hbar` in the numerator of `\widetilde{G}(k)` is light gray. The `\hbar` multiplying `g` in the `\omega_1` expansion is light gray. The `\hbar` in “a term of order \hbar” is black. The remaining equation marks, including the `\Lambda` in the `\omega_1` expansion and the `c/2` term, are black at the render resolution.

The only arrow is the `\Lambda \to \infty` arrow. There is no diagram, graph, table, example figure, margin label, strikeout, or erased alternative. The colored marks are visible interventions in the source page; the page does not state whether each is a correction or a later emphasis.

### Page transition

Page 61 opens inside the regularized path-integral and counterterm discussion. It ends with the energy relation `\omega_1 = \widehat{E}_1-\widehat{E}_0`. Page 62 begins with the hyphenated continuation “- energy gap,” which attaches directly to that relation.

## Physical page 62

### Exact source transcription

```text
- energy gap, unambiguous physical
  observable regardless of the interpretation
  of g(c).

* The Counter terms, together with
  regularization scheme, should be viewed as
  part of the definition of the path integral.

[long horizontal line under the preceding sentence; two short marks follow at the right]

To fix the finite part of the Counter
terms require additional physical input,
e.g. symmetries of the quantum Hamiltonian
In general QM, anything goes in H
In QFT, Poincaré sym + Causality
highly restrictive !
```

### Formula layer

```tex
g(c)
H
\text{Poincaré sym} + \text{Causality}
```

`g(c)` is the coupling/function expression in the energy-gap qualification. `H` is the Hamiltonian symbol in the general-QM line. The plus sign in “Poincaré sym + Causality” is written as a literal plus sign, not as a diagrammatic connector.

### Visual semantics and annotations

All visible ink on page 62 is black. The first line begins with a short hyphen and continues the page-61 energy statement. The counterterm paragraph begins with a black asterisk.

After “part of the definition of the path integral.” there is a long hand-drawn horizontal rule. Two short marks sit just after its right endpoint. Their character and function are unresolved. They are recorded as a visual annotation rather than assigned punctuation.

The page contains no colored intervention, arrow, diagram, graph, table, example figure, margin label, or visible original handwritten page number. The `e.g.` line supplies the source's example of additional physical input. The final line has a visible space before the exclamation point: “highly restrictive !”

### Page transition and chapter boundary

The top three lines complete the page-61 energy-gap statement. The counterterm paragraph then starts a new topic unit. The bottom statement ends with “highly restrictive !” and no continuation mark is visible. The chapter plan places the assignment boundary after page 62, at physical page 63.

## Uncertainties and source-resolvable decisions

`\widetilde{G}` has a clear tilde, and `\widehat{E}_1-\widehat{E}_0` has clear hats over both `E` symbols. Those accents are retained in the TeX layer.

The two marks after the page-62 horizontal rule resemble short comma or quotation strokes. No reliable character reading is available. The exact capture leaves them as `[two short marks; unresolved]` in the visual layer.

The initial letter in both visible instances of “Counter” is drawn in the capital form and is transcribed as `Counter`. The handwriting leaves the intended capitalization as a small editorial uncertainty. The page's grammar is retained verbatim: “regularization scheme” has no article, and “terms require additional physical input” has no inserted verb.

`O(g^2)` is a plain capital `O` in the source. The `\omega_1` line uses a parenthesized `-\Lambda/(8\pi)+c/2+1/8` correction. No calligraphic `\mathcal{O}` or alternate denominator is visible.

## Topic boundaries and recommended dispositions

Page 61 groups the cutoff requirement, the choice `c=\Lambda/(4\pi)+\text{finite}`, the finite-part ambiguity, the propagator pole, and the energy gap. Keep this as one continuous source unit with the equation order unchanged.

Page 62 completes the energy-gap qualification and then moves to the status of counterterms and regularization as part of the path-integral definition. Merge the opening hyphenated lines directly with the page-61 energy relation. Preserve “regardless of the interpretation of g(c)” as a qualification.

The physical-input paragraph should remain a separate conceptual unit. Retain the `e.g.` example and the handwritten QM/QFT statements in the source packet. The source-capture disposition is `retain`; later prose work may decide paragraph shaping after the complete chapter is assembled.

The final “highly restrictive !” line is the terminal material in this page lane. The disposition is `chapter boundary at PDF page 62 / assignment material begins at PDF page 63`.
