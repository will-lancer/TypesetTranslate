# Pass 1 source capture: physical pages 25-26

## Capture record

- Project: Xi Yin, Physics 253a, Chapter 2.
- Chapter title in the plan: Lagrangian Quantum Mechanics, Path Integrals, and Perturbation Theory.
- Combined source PDF: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`.
- Source PDF SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`.
- Physical pages captured: 25 and 26.
- Local renders inspected: `tmp/pdfs/253a-ch02-source/page-025.png` and `tmp/pdfs/253a-ch02-source/page-026.png`.
- Fresh renders from the source PDF were pixel-identical to the two local renders.
- The source PDF has no usable text layer for these scanned pages. The capture rests on the full-page renders and enlarged crops.
- Original handwritten page label: absent from both pages. The physical page identity comes from the source PDF and render filenames.
- Formal heading: absent on both pages. Page 26 begins with a leading dot before the first sentence.
- Source-preservation status: wording, line breaks, ink colors, equations, arrows, braces, marginal labels, and visible uncertainties are recorded before any cleanup.

## Physical page 25

### Exact transcription, top to bottom

Black ink prose:

```text
If H(q,p) is quadratic in p.

e.g. H(q,p) = p^2/(2m) + V(q),

we can integrate out P(t) as Gaussian,

leaving
```

Exact TeX for the formula in the second line is:

```tex
H(q,p)=\frac{p^2}{2m}+V(q)
```

The case of the first letter in `P(t)` is recorded as it appears. The mathematical context suggests the phase-space momentum variable (p(t)); see the ambiguity ledger below.

Black ink equation, with the right-hand condition written after a vertical evaluation bar:

```tex
\int [D\xi]\,
e^{\frac{i}{\hbar}\int_0^T dt\,(p\dot{\xi}-H)}
\bigg|_{\dot{\xi}=\frac{\partial H}{\partial p}}
```

Blue annotation attached to the measure:

```text
absorbs Gaussian
determinant of P(t)
```

A blue curved arrow points from this annotation to `[Dξ]`. The arrow identifies the configuration-space measure as the place that absorbs the Gaussian determinant from the momentum path (P(t)).

A blue curved brace or underline sits below the constrained phase-space integrand. Two short blue stroke marks sit beneath the brace. The blue label below it reads:

```tex
L(\xi,\dot{\xi})
```

The label identifies the constrained expression ((p\dot\xi-H)|_{\dot\xi=\partial H/\partial p}) with the Lagrangian.

Gray ink prose:

```text
More generally, Lagrangian in generalized coordinates
typically takes the form
```

Gray ink equation:

```tex
L=\frac{1}{2}G_{\alpha\beta}(\xi)\dot{\xi}^{\alpha}\dot{\xi}^{\beta}-V(\xi)
```

Gray square-bracket annotation at the right, arranged on two lines:

```text
[ e.g. rotating
  3D rigid body ]
```

The brackets enclose the example. The bracket strokes and the example are gray, matching the generalized-coordinate passage.

Gray ink Hamiltonian relation, with the (+O(\hbar)) term in blue:

```tex
\Longleftrightarrow\quad
H=\frac{1}{2}\bigl(G^{-1}(\xi)\bigr)^{\alpha\beta}p_{\alpha}p_{\beta}+V(\xi)+O(\hbar)
```

The source writes the (+O(\hbar)) correction at the right in pale blue. The source shape is an ordinary `O`, with the possibility of a script-(\mathcal O) left open in the ambiguity ledger.

Gray ink label and path-integral equation:

```text
Path integral
```

```tex
\int \mathcal{D}p\,\mathcal{D}\xi\,
e^{\frac{i}{\hbar}\int dt\,(p_{\alpha}\dot{\xi}^{\alpha}-H(p,\xi))}
=
\int [D\xi]\,
e^{\frac{i}{\hbar}\int dt\,L(\xi,\dot{\xi})}
```

The handwritten measure looks like `Dp Dξ`. The TeX above represents the visible measure symbols without deciding whether the (p) and (\xi) are subscripts or arguments of a differential-measure symbol.

Gray ink discretized-measure line:

```tex
[D\xi]\;\leadsto\;
\prod_{n=1}^{\widetilde N}
\frac{d\xi_n^{\alpha}}
{\sqrt{2\pi i\hbar\,\frac{T}{N}}}
\sqrt{\det G_{\alpha\beta}(\xi_n)}\,.
```

The rightward mark between `[Dξ]` and the product is a hand-drawn squiggly arrow. The product has a tilde over its upper-limit (N). The denominator visibly contains (T/N) under the square root. The final punctuation is a small mark after the determinant factor.

### Page 25 visual semantics

- The upper prose and first path-integral equation are black.
- The Gaussian-determinant annotation, its arrow, the brace beneath the action, and (L(\xi,\dot\xi)) are blue.
- The generalized-coordinate prose, the metric Lagrangian, the rigid-body example, the Hamiltonian relation, the path-integral rewrite, and the discretized measure are gray.
- The (+O(\hbar)) correction is written in pale blue within the gray Hamiltonian line.
- The arrow from `[Dξ]` to the product is a visual measure-discretization relation, rather than a graph or physical-space diagram.
- Diagram or graph: absent.
- Original handwritten page number: absent.

### Page 25 topic boundary and disposition

The page continues the phase-space path integral discussion from physical page 24. Its opening move integrates the momentum path as a Gaussian when (H(q,p)) is quadratic in (p). The middle passage changes to generalized coordinates (\xi^\alpha), gives the metric kinetic term, records the Hamiltonian relation with an (O(\hbar)) correction, and writes the configuration-space measure. The bottom line gives a discrete measure with the metric determinant.

Recommended disposition: include the prose and equations in the same argument unit covering the passage from the phase-space path integral to the Lagrangian path integral. Keep the generalized-coordinate example as a source-backed example. Keep the (O(\hbar)) term and the metric determinant in the exact equation layer. Preserve the blue determinant note and the (L(\xi,\dot\xi)) identification in source annotations. Resolve the measure typography during the equation audit.

The page ends after the discretized measure. The following page opens a new correlation-function bullet, so the page break is a topic boundary between path-integral measure construction and spectral analysis of a ground-state correlator.

## Physical page 26

### Exact transcription, top to bottom

Black ink prose, including the leading dot at the left margin:

```text
. Consider correlation function in
ground state |0>,    e.g.
```

Black ink definition:

```tex
G(t)\equiv\langle 0|\hat q_{\beta}(t)\,\hat q_{\beta}(0)|0\rangle
```

Black ink insertion of intermediate states:

```tex
=\sum_n\langle 0|\hat q_{\beta}(t)|n\rangle
\langle n|\hat q_{\beta}(0)|0\rangle
```

Blue annotation at the inserted state ( |n\rangle ):

```text
a basis of H-hat - eigenstates
H-hat |n> = E_n |n>.
```

The source writes the hats and bras and kets by hand. Exact TeX for the blue equation is:

```tex
\hat H|n\rangle=E_n|n\rangle.
```

The phrase `a basis of H-hat - eigenstates` is written in blue, with a blue squiggle under the displayed ( |n\rangle ) and a curved arrow from the squiggle to the phrase. The hyphen between `H-hat` and `eigenstates` is part of the visible annotation.

Black ink Heisenberg-operator expansion:

```tex
=\sum_n
\langle 0|e^{\frac{i}{\hbar}\hat Ht}\hat q_{\beta}(0)
e^{-\frac{i}{\hbar}\hat Ht}|n\rangle
\langle n|\hat q_{\beta}(0)|0\rangle
```

The (i/\hbar) factors in the two exponentials carry visible blue pen accents. The (\hat Ht) portions and the surrounding matrix elements remain black.

Black ink spectral form:

```tex
=\sum_n
\langle 0|\hat q_{\beta}(0)|n\rangle
\langle n|\hat q_{\beta}(0)|0\rangle\,\cdot\,
e^{-\frac{i}{\hbar}(E_n-E_0)t}.
```

A blue brace runs below the product of matrix elements

```tex
\langle 0|\hat q_{\beta}(0)|n\rangle
\langle n|\hat q_{\beta}(0)|0\rangle
```

Three short blue stroke marks sit above the coefficient label. The label reads:

```text
β_n, some number
```

Exact TeX for the provisional coefficient label is:

```tex
\beta_n,\ \text{some number}
```

The source coefficient is captured as (\beta_n) in the TeX layer, with the first glyph flagged as a possible cursive (g) below. The phrase `some number` is blue and is part of the annotation.

Black ink convergence prose:

```text
As E_n-E_0>0, can analytically continue
G(t) as a function of complex t,
provided Im t<0.  So that Σ_n converges.
```

Exact TeX for the mathematical portions of that prose is:

```tex
E_n-E_0>0,
\qquad G(t),
\qquad \operatorname{Im}t<0,
\qquad \sum_n
```

The source writes `Im t` by hand and the summation as a large (\sum) with a small (n) below. The strict inequalities (E_n-E_0>0) and (\operatorname{Im}t<0) are visually legible.

Gray square-bracket annotation at the bottom:

```text
[Paley-Wiener theorem]
```

The hyphen in `Paley-Wiener` is visible. The entire theorem label is gray.

### Page 26 visual semantics

- The prose and equations are black, with blue additions around the state insertion, the eigenvalue relation, selected (i/\hbar) factors, and the spectral coefficient.
- The blue state annotation uses a squiggle, a curved arrow, and the phrase `a basis of H-hat - eigenstates`.
- The blue coefficient annotation uses a brace, three short stroke marks, and `β_n, some number`.
- The gray `[Paley-Wiener theorem]` label is a marginal theorem cue rather than a displayed derivation.
- Diagram or graph: absent.
- Original handwritten page number: absent.

### Page 26 topic boundary and disposition

The page starts a ground-state two-point correlation function. It inserts a basis of (\hat H)-eigenstates, converts the time dependence to exponentials, identifies the matrix-element product as a coefficient, and states the lower-half-plane convergence condition for analytic continuation. The gray Paley-Wiener label marks the theorem context.

Recommended disposition: include the full spectral derivation in one argument unit. Preserve the state-insertion annotation and the eigenvalue equation in the exact note layer. Carry the coefficient label into the ambiguity ledger until its first glyph is settled. Retain the strict condition (\operatorname{Im}t<0). Treat `[Paley-Wiener theorem]` as a source citation cue whose printed use awaits the surrounding theorem context.

The page break after the convergence statement leads into the next page's explicit Euclidean continuation, which begins with `e.g. t=-iτ, τ∈R_+` and the handwritten label `Wick rotation`. Page 26 therefore closes the spectral-analyticity setup.

## Ambiguity and correction ledger

| ID | Visible source feature | Capture used here | Pass 2 follow-up |
|---|---|---|---|
| P25-A1 | The prose and blue determinant note use a tall, capital-looking `P(t)`. | Preserve literal `P(t)` in the transcription. | Compare against the adjacent phase-space notation and audio; likely (p(t)), but the source capture keeps the visible case. |
| P25-A2 | The handwritten measure appears as `Dp Dξ` and `[Dξ]`; the calligraphic status and whether the variable is a subscript are not explicit. | Use `\mathcal{D}p\,\mathcal{D}\xi` for the phase-space measure and `[D\xi]` for the bracketed configuration measure. | Preserve the source notation in the exact equation layer and record any normalization separately. |
| P25-A3 | The generalized-coordinate symbol is (\xi), distinct from the opening (q) in (H(q,p)) and (V(q)). | Use (\xi), (\dot\xi), and (G_{\alpha\beta}(\xi)) throughout the generalized-coordinate block. | Confirm against the lecture or adjacent note pages if the canonical source layer needs a notation comment. |
| P25-A4 | The blue correction reads `+ O(hbar)`; script-(\mathcal O) is not visually forced. | Capture (+O(\hbar)). | Keep the source form until the equation audit decides the TeX glyph. |
| P25-A5 | The discrete product upper limit has a tilde over (N), while the denominator visibly contains (T/N). | Capture `\widetilde N` in the product and `T/N` in the square root. | Verify whether the denominator is (T/N) or (T/\widetilde N) from the lecture or the equation layer. |
| P25-A6 | The evaluation bar after the exponent is handwritten at the right of the full phase-space expression. | Place `\big|_{\dot\xi=\partial H/\partial p}` after the exponential expression. | Check whether the bar is intended to act on the exponent, the action integrand, or the entire path-integral factor. |
| P26-A1 | The operator glyph reads (q) with a lower (\beta); the generalized-coordinate block on page 25 uses (\xi). | Capture (\hat q_\beta). | Adjacent page 27 uses the same (\hat q_\beta) notation, supporting this reading. |
| P26-A2 | The blue coefficient label looks like (\beta_n), with a possible cursive (g_n) reading. | Capture (\beta_n) and flag the first glyph. | Resolve from audio, adjacent formulas, or a later clean equation. |
| P26-A3 | The energy symbols are capital (E_n) and (E_0); their top horizontal strokes can resemble bars at reduced scale. | Capture (E_n-E_0), supported by the blue eigenvalue equation and page 27. | No mathematical change is recommended unless a separate source makes a barred energy explicit. |
| P26-A4 | Blue ink accents the (i/\hbar) factors in the time-evolution exponentials. | Record the accents as source annotations, without assigning correction status. | Check whether the blue ink was added to repair omitted factors or to highlight them. |
| P26-A5 | The first character is a margin dot before `Consider`. | Preserve the dot as a source mark. | Treat it as a bullet or sentence mark only after page-sequence reconciliation. |

## Page transition ledger

- Physical page 24 to 25: page 24 ends with the regularized phase-space path integral and its (O(\hbar)) ambiguity. Page 25 begins the Gaussian integration statement for a Hamiltonian quadratic in momentum.
- Physical page 25 within-page boundary: the Gaussian reduction gives way to generalized coordinates and the metric path-integral measure.
- Physical page 25 to 26: the discretized configuration-space measure ends page 25. Page 26 begins a new ground-state correlation-function bullet.
- Physical page 26 to 27: the analytic-continuation condition and Paley-Wiener cue end page 26. Page 27 begins with the explicit choice (t=-i\tau), (\tau\in\mathbb R_+), and the handwritten label `Wick rotation`.

## Recommended source dispositions

| Source span | Recommended outcome | Reason |
|---|---|---|
| Page 25 opening prose and Gaussian-reduction equation | Included and merged into the phase-space to configuration-space path-integral argument | It states the condition and the resulting operation. |
| Page 25 blue determinant and Lagrangian annotations | Retained in the exact note layer; used as equation provenance | The marks explain the measure and the Legendre-transform identification. |
| Page 25 generalized-coordinate block and rigid-body example | Included in the adjacent Lagrangian-mechanics argument | The block supplies the metric kinetic term and its physical example. |
| Page 25 (O(\hbar)) term and discretized measure | Included as source-exact mathematics, with ambiguity flags | The correction and determinant carry measure and ordering information. |
| Page 26 spectral insertion and time-evolution equations | Included in one correlation-function argument unit | The equations form a continuous derivation. |
| Page 26 `β_n, some number` annotation | Retained as an ambiguity record and provisional coefficient label | The first character remains visually uncertain. |
| Page 26 convergence prose and `[Paley-Wiener theorem]` | Included as the analytic-continuation qualification; theorem label held as provenance | The condition controls the convergence claim and points toward the next page's Wick rotation. |

All spans on these two pages have a recommended written-use disposition. The source annotation colors and marginal marks remain evidence-layer material until a later pass assigns their printed role.
