# Pass 1 source capture: physical pages 45-46

## Capture basis

The source is `qft_253abc_book.pdf`, physical pages 45 and 46. The manifest
hash is confirmed as
`9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`.

The visual inspection used the supplied full-page renders:

```text
tmp/pdfs/253a-ch02-source/page-045.png
tmp/pdfs/253a-ch02-source/page-046.png
```

The PDF itself was rendered at higher resolution for character checks. The
page-number field is blank in the visible margins of both pages. Original
handwritten page label: absent from the visible margins.

Black carries the primary handwriting. Blue marks explanations, arrows,
underlines, and the worked example on page 45. Purple marks the contraction
lines in the graph on page 45.

## Physical page 45

### Page transition and topic boundary

Page 45 follows the combinatorial partition argument at the bottom of page 44.
The page opens with a definition of \(G^{(\ell)}\), then repeats the partition
sum and turns it into a product of exponentials. The middle of the page gives
the connected-graph interpretation and a three-vertex example. The final line
records the remaining extensive divergence. Physical page 46 begins a fresh
heading, `Interpretation of`.

### Exact transcription in source order

A separate blue stroke sits near the upper margin. It rises toward the right
and ends in a hooked turn. The stroke stands alone in the visible render, with
words and an arrowhead absent.

The first blue annotation reads, with the shown line breaks:

> `G^{(\ell)} is the contribution from`
>
> `connected Wick contraction of`
>
> `\ell vertices`

The first black displayed formula is:

$$
\frac{Z_g}{Z_0}
=
\sum_{\{m_\ell\}}
\prod_{\ell\ge 1}
\frac{1}{m_\ell!}
\left(\frac{1}{\ell!}G^{(\ell)}\right)^{m_\ell}
$$

The next black line is:

$$
=\prod_{\ell\ge 1}\exp\left(\frac{1}{\ell!}G^{(\ell)}\right)
$$

The following black line is:

$$
=\exp\left(\sum_{\ell\ge 1}\frac{1}{\ell!}G^{(\ell)}\right)
$$

The blue brace beneath the summand spans the connected-graph contribution.
Its annotation reads on two lines:

> `contribution from all`
>
> `connected graphs`

The blue `e.g.` sits to the left of the graph. The graph has three black filled
nodes arranged horizontally. Each node is drawn with an X-like four-leg
vertex. Purple arcs show two contractions between each pair of nodes. The
adjacent pairs use short upper and lower arcs. The first and third nodes are
also joined by a large purple arc above the graph and another below it. The
black vertex strokes remain visible beneath the purple arcs. The graph shows
unlabelled vertices and arcs, with arrowheads absent.

The worked example begins with a blue equals sign at the left. The source line
break places a centered multiplication dot at the start of the second line:

$$
\begin{aligned}
={}&\frac{1}{3!}\left(-\frac{g}{4!}\right)^3
\int d\tau_1\,d\tau_2\,d\tau_3\,
\left(G_0(\tau_1,\tau_2)\right)^2\\
&\cdot\left(G_0(\tau_2,\tau_3)\right)^2
\left(G_0(\tau_3,\tau_1)\right)^2.
\end{aligned}
$$

The final blue line reads:

> `Still diverges like \(\int d\tau\cdot(\mathrm{Const})\).`

The parenthetical word is handwritten as `Const` or `const`; the capital form
is used above as the closest visual transcription.

### Visual semantics and dispositions

The three black equations form one continuous derivation. The source writes
`exp` as a handwritten operator, with the first expression summing over the
multiplicities \(m_\ell\), the second expression factorizing by \(\ell\), and
the third expression collecting connected contributions in one exponent. Keep
the equations together in the exact-note layer as `NOTES_EXACT`. In the chapter
argument map, place them in the connected-graph exponentiation unit.

The blue definition of \(G^{(\ell)}\), its brace, and the phrase `connected
graphs` carry the interpretation of the exponent. Preserve each annotation in
the note layer. The graph and its worked formula belong to the same source
unit. Retain the graph as a source figure and retain the coefficient
\(\frac{1}{3!}(-\frac{g}{4!})^3\) with the three \(G_0\) factors during the
mathematical reconciliation pass.

`Still diverges like` is a qualification attached to the example. Retain it
with the displayed integral and keep the extensive behavior explicit. The
blue upper stroke has source identity as a visible mark. Record it in the
visual ledger while leaving its function open.

## Physical page 46

### Page transition and topic boundary

The top heading is a new interpretive unit after the connected-graph example:

> `Interpretation of`

The path-integral expression is followed by a matrix-element expression. The
blue annotation specifies the endpoint data in imaginary time. The next black
formula extracts the ground-state energy from the large-\(T\) behavior of
\(Z\). The prose then returns to the perturbative connected-graph expansion.
The bottom starred sentence continues onto physical page 47.

### Exact transcription in source order

The black path-integral display reads:

$$
\begin{aligned}
Z &= \int [Dq]\,e^{-\int d\tau\,L^E}\\
  &= \langle q_f\,|\,e^{-HT}\,|\,q_i\rangle
\end{aligned}
$$

The source places the \(E\) high and to the right of \(L\), so the exact visual
reading is \(L^E\). The blue arrows point from the endpoint labels \(q_f\) and
\(q_i\) upward toward the matrix element. The blue annotation reads:

> `boundary`
>
> `condition at initial and final`
>
> `imaginary time \(\tau=0, T\),`
>
> `unspecified as we take \(T\to\infty\)`

The next black display is:

$$
\log Z=-E_0\cdot T+O(1)
$$

The blue mark beneath \(E_0\) is a short wavy underline. A blue arrow points
to it from the annotation `ground state energy`.

The black prose follows in two handwritten lines:

> `Indeed, we have seen in perturbation`
>
> `theory.`

The next black display is:

$$
\log\frac{Z_g}{Z_0}
=\sum_{\ell=1}^{\infty}\frac{1}{\ell!}G^{(\ell)}
$$

The blue mark beneath \(G^{(\ell)}\) is a wavy underline with an upward-curving
arrow. Its annotation reads on two lines:

> `sum of all Wick contractions`
>
> `of \ell vertices via connected graphs`

The word `connected` is underlined in blue.

The final visible black fragment is a starred sentence beginning:

> `* One often absorbs the combinatorial`

The continuation on physical page 47 begins `factor \(1/\ell!\) into the
"symmetry factor" of the graph itself.` This is boundary evidence for the
sentence and belongs to the same source unit.

### Visual semantics and dispositions

The heading, path-integral display, endpoint annotation, and ground-state
formula form the interpretation unit. Preserve the endpoint labels, the
imaginary-time values \(\tau=0,T\), and the large-\(T\) qualification in the
exact-note layer as `NOTES_EXACT`. The path-integral expression should enter the
argument map as a source equation with an open notation review for \(L^E\).

The line beginning `Indeed` marks the return to perturbation theory. The
connected-graph logarithm and its blue explanatory annotation belong with the
page-45 exponentiation argument. Retain the starred sentence across the page
break. Its wording carries the combinatorial-factor explanation and its
quotation marks around `symmetry factor`.

## Ambiguities and review flags

| Source mark | Reading for the exact layer | Review flag |
|---|---|---|
| `L^E` in \(e^{-\int d\tau L^E}\) | The \(E\) is visibly raised to the right of \(L\). | Preserve \(L^E\) as the source reading. Check whether the intended notation is \(L_E\) or \(\mathcal L_E\) before any canonical LaTeX is written. |
| `Const` in the final line of page 45 | The parenthetical begins with a large C-shaped glyph. | Preserve `Const` in the exact transcription and mark the case as unresolved. |
| The isolated blue stroke at the top of page 45 | A curved mark with a hooked right end. | Keep as an unattached annotation. Its relation to the preceding page is unresolved. |
| `Dq`, \(q_f\), and \(q_i\) | The variable has a loop with a descending tail and agrees across the display. | Confidence high. Use \(q\) in the exact layer. |
| \(G_0\) factors in the example | The subscript is a rounded zero on all three factors. | Confidence high. Keep the subscript \(0\). |
| \(O(1)\) in \(\log Z\) | The first glyph is an uppercase O. | Keep uppercase \(O\) as a big-O symbol. |

Correction field: clean page surface with strike-throughs, erasures, and
overwrites absent on both target pages. The blue underlines, braces, and arrows
serve explanatory roles.

## Recommended page dispositions

Physical page 45 receives `written-use: merged into an adjacent argument` for
the partition sum, product, exponential, and connected-graph explanation. The
graph receives `written-use: retained as a figure` with its formula attached.
The divergence line receives `written-use: retained as a qualification`. The
upper blue stroke receives `written-use: visual ledger only` pending a source
boundary decision.

Physical page 46 receives `written-use: merged into an adjacent argument` for
the path-integral interpretation and the return to perturbation theory. The
equations receive `NOTES_EXACT` provenance. The \(L^E\) glyph receives an open
notation review. The starred sentence receives `written-use: merged across the
page boundary`, with its page-47 continuation preserved verbatim in the
boundary record.
