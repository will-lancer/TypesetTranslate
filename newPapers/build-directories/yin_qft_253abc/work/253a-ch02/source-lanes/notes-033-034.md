# Xi Yin QFT Chapter 2 source capture: physical pages 33-34

## Capture record

- Combined source: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`
- Source SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`
- Physical pages captured: 33 and 34
- Attached renders inspected: `tmp/pdfs/253a-ch02-source/page-033.png` and `tmp/pdfs/253a-ch02-source/page-034.png`
- Direct Poppler renders from the source PDF inspected at 160 dpi. Their visible content matches the attached renders.
- `pdftotext -f 33 -l 34 -layout` returns no text. The capture therefore follows the handwritten visual source.
- No original handwritten page number or other page label is visible on either page.
- No formal section heading is visible. The page-034 phrase `Back to` is a transition into the correlator calculation.

## Physical page 33

### Exact transcription in reading order

The page begins with the continuation of an equation. The first visible mark is an equals sign.

Black ink:

$$
=\frac{\hbar}{\pi}\int_{-\infty}^{\infty} dk\cdot\frac{1}{k^2+\omega^2}
\left[
\frac{1}{4}e^{ik(\tau_1-\tau_2)}
+\frac{1}{4}e^{-ik(\tau_1-\tau_2)}
-\frac{1}{4}e^{ik(\tau_1+\tau_2+2T)}
-\frac{1}{4}e^{-ik(\tau_1+\tau_2+2T)}
\right]
$$

The lower `-\infty` on this integral is written in magenta. The rest of this displayed equation is black.

Blue handwriting:

```text
Consider
```

Then the displayed integral

$$
\int_{-\infty}^{\infty} dk\,\frac{e^{ik\tau}}{k^2+\omega^2}
$$

Blue handwriting continues:

```text
integrand as an analytic function in k
has poles at k = ± ω i
```

The exact TeX form of the pole statement is `k=\pm\omega i`.

The source order in the pole statement is `± ω i`. It is captured below without changing it to `± iω`.

Black handwriting at the right, with the first line in magenta:

```text
If τ>0,
|e^{ikτ}| = e^{-Im k·τ}
exponentially suppressed
at large Im k,
can close contour
via large semi-circle
on UHP.
```

The visual mathematical transcription of the second line is

$$
\left|e^{ik\tau}\right|=e^{-\operatorname{Im} k\cdot\tau}.
$$

The source writes `Im k·τ` without parentheses around `Im k`.

### Visual semantics

- The blue integral has a curved blue arrow rising from below the explanatory sentence and pointing toward the fraction containing the integrand.
- A dark blue or purple rectangular frame encloses a contour sketch. Its upper-left label reads `Complex k-plane`.
- The horizontal real axis is green. A green arrow points to the right. A green label at the right edge reads `R` with a double-struck-looking stroke, captured as `\mathbb{R}` in the diagram notation.
- A light lavender semicircular contour closes through the upper half-plane. Its arrowhead is on the left side of the semicircle and points down-left, giving the contour the orientation shown in the source.
- The upper pole is marked by a red cross and the red label `iω`.
- The lower pole is marked by a red cross and the red label `-iω`.
- The red pole marks lie inside the framed complex `k`-plane sketch. The upper pole lies inside the upper contour; the lower pole lies below the real axis.
- The word `UHP` in the right-hand prose is black. The condition `If τ>0,` is magenta.
- No correction, strikeout, example label, or graph is visible on this page.

### Page transition and topic boundary

The preceding physical page ends with the sine-product integral that is expanded into the four exponentials at the top of this page. Physical page 33 then isolates the Fourier integral with numerator `e^{ikτ}` and introduces its pole structure and upper-half-plane contour for positive `τ`. The page ends inside that contour argument, after the sentence explaining suppression at large imaginary `k`.

### Ambiguities and source constraints

- The page begins with an equals sign, so no preceding expression is reconstructed here.
- The handwritten pole statement is `k = ± ω i`. The order is retained in the exact capture.
- The expression `e^{-\operatorname{Im} k\cdot\tau}` has handwritten `Im k·τ` and no visible parentheses. Parentheses are not added.
- The green axis label is visually `R` with a double-struck appearance. The exact font choice is uncertain in the handwriting; the contour meaning is the real axis.
- The source uses the labels `iω` and `-iω` beside red crosses. The pole positions are clear.
- The source gives no definition of `τ_{12}` on this page. Later use of that symbol is kept unresolved rather than expanded into `τ_1-τ_2`.

### Recommended disposition

- Preserve the top equation as `NOTES_EXACT`, including its four exponential terms, the `dk\cdot` placement, and the magenta lower integration bound.
- Preserve the blue explanatory lines and the right-hand contour explanation as exact note evidence. They can be merged into the adjacent contour-integral argument during a later drafting pass.
- Retain the contour sketch as a source diagram. Its pole positions, colors, contour arrow, real-axis arrow, frame, and `UHP` qualification belong to the source record.
- Carry the `k=±\omega i` and `\operatorname{Im} k\cdot\tau` notation into the ambiguity ledger without normalization.

## Physical page 34

### Exact transcription in reading order

Blue handwriting resumes the contour calculation with a rightward implication arrow:

$$
\Rightarrow\quad
\int_{-\infty}^{\infty}dk\,\frac{e^{ik\tau}}{k^2+\omega^2}
\mathrel{\overset{\tau>0}{=}}
2\pi i\,\operatorname{Res}_{k\to i\omega}
\frac{e^{ik\tau}}{k^2+\omega^2}
$$

The source writes `τ>0` above the equality sign. It writes `k→iω` below `Res`.

Blue handwriting continues:

$$
=\frac{\pi}{\omega}e^{-\omega\tau}.
$$

Black handwriting:

```text
Similarly, for τ<0,
the result is π/ω e^{ωτ}.
```

The exact TeX form of the second line is

$$
\text{the result is }\frac{\pi}{\omega}e^{\omega\tau}.
$$

Black handwriting then reads:

```text
Back to
```

The correlator written below it is

$$
\langle g(\tau_1)\,g(\tau_2)\rangle
$$

The next black display is

$$
\begin{aligned}
\langle g(\tau_1)\,g(\tau_2)\rangle
={}&\frac{\hbar}{\pi}\int_{-\infty}^{\infty}dk\cdot\frac{1}{k^2+\omega^2}
\left[
\frac{1}{4}e^{ik(\tau_1-\tau_2)}
+\frac{1}{4}e^{-ik(\tau_1-\tau_2)}\\
&\qquad-\frac{1}{4}e^{ik(\tau_1+\tau_2+2T)}
-\frac{1}{4}e^{-ik(\tau_1+\tau_2+2T)}
\right].
\end{aligned}
$$

The source then gives

$$
=\frac{\hbar}{\omega}\cdot
\left(
\frac{1}{2}e^{-\omega|\tau_{12}|}
-\frac{1}{2}e^{-\omega(\tau_1+\tau_2+2T)}
\right)
$$

A magenta diagonal arrow points from the second exponential term toward a magenta `0`, with the magenta annotation

```text
as T→∞
```

The final black line is

$$
=\frac{\hbar}{2\omega}e^{-\omega|\tau_{12}|}.
$$

A blue check mark is drawn to the right of the final result.

### Visual semantics

- The residue calculation at the top is blue. The condition `τ>0` sits above the equality, and `k→iω` sits below `Res`.
- The line for `τ<0` and its result are black. It records the complementary sign case in prose and formula form.
- `Back to` is a black transition phrase, followed by the black correlator `⟨g(τ_1) g(τ_2)⟩`.
- The repeated four-term integral is black and repeats the equation that began on physical page 33.
- The simplified correlator is black. A magenta diagonal arrow crosses toward the second exponential term and ends beside the magenta `0`; the magenta words `as T→∞` identify the limit in which that term vanishes.
- The final result is black. A blue check mark appears beside it as a handwritten verification annotation.
- No separate figure, axis, or page label appears on physical page 34.
- No correction, strikeout, example label, or graph is visible. The blue check mark is a verification annotation, not a correction mark.

### Page transition and topic boundary

Physical page 34 begins by evaluating the page-33 Fourier integral at the pole `k=iω` for `τ>0`. The following black prose records the `τ<0` result. `Back to` returns to the finite-`T` correlator, whose image term is then sent to zero as `T→∞`. The page ends with the checked expression `\frac{\hbar}{2\omega}e^{-\omega|\tau_{12}|}`. The next physical page begins a new topic with the heading-like line `Consider a general Gaussian integral`.

### Ambiguities and source constraints

- The residue notation is captured as `\operatorname{Res}_{k\to i\omega}` because the source writes an arrow under `Res`. It is not normalized to `k=i\omega`.
- The source writes `g(τ_1)` and `g(τ_2)`. The letter is a lowercase `g`, with the descender visible in both occurrences.
- The final correlator uses `|τ_{12}|`. No definition of `τ_{12}` appears on pages 33-34. Keep the symbol exactly and defer any identification with `τ_1-τ_2` to an upstream source check.
- The finite-`T` image term is written as `e^{-\omega(τ_1+τ_2+2T)}`. The magenta zero arrow is an explanatory annotation attached to this term, not a replacement of the source term.
- The equality condition for the residue result is `τ>0`. The separate prose result uses `τ<0`. Both sign cases are retained.

### Recommended disposition

- Preserve the residue calculation, both sign cases, the correlator display, the finite-`T` simplification, the `T→∞` annotation, and the final checked result as `NOTES_EXACT`.
- Merge the repeated integral and the two evaluated sign cases into the preceding contour-integral argument during drafting. Preserve the source order.
- Retain `\tau_{12}` in the exact equation layer and record its missing local definition as an unresolved ambiguity. Use `uncertainty withheld` for any later attempt to replace it with a difference of the two times.
- Keep the magenta zero arrow and blue check in the visual source record. They are source annotations rather than chapter prose.
- Treat the next-page line `Consider a general Gaussian integral` as the boundary to the following topic. The new topic is outside this two-page disposition except for the boundary marker.

## Cross-page disposition

Pages 33-34 form one continuous source unit. The unit evaluates the Fourier integral with denominator `k^2+\omega^2` by contour integration, applies the positive- and negative-`τ` cases, returns to `\langle g(\tau_1)g(\tau_2)\rangle`, and takes `T\to\infty`. The exact equations and visible annotations belong in the immutable note-evidence layer. The contour sketch can be reconstructed later only with its arrow directions, pole labels, half-plane qualification, and color semantics preserved. No source prose is polished in this capture.
