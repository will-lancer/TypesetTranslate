# Xi Yin QFT 253a, Chapter 2 source capture

## Source identity and visual check

- Combined source: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`.
- Combined physical pages: 29 and 30.
- Source PDF SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`.
- Rendered page files inspected: `tmp/pdfs/253a-ch02-source/page-029.png` and `tmp/pdfs/253a-ch02-source/page-030.png`.
- The attached full-page renders and fresh renders from the source PDF agree in page content and layout.
- The PDF pages are image-only for this range; `pdftotext` returns no page text.
- The page images show a white ground with black main writing, blue annotations, and a magenta/purple graph trace.
- Original handwritten page label: none visible on either physical page.

## Physical page 29

### Exact reading order and transcription

Blue marginal annotation near the top, with a curved arrow rising toward the upper margin:

```text
still use the same notation
```

The first black formula is centered beneath it:

```tex
\partial_t \dot{q}=+i\partial_\tau\dot{q}.
```

The word `Define` appears to the left of the next formula:

```tex
L[q,\dot{q}]\equiv -L^E[q,\partial_\tau q]
```

The next prose fragment reads:

```text
so that
```

The following transformation is written in black, with the fractions in the exponents written in blue. The connecting arrow is a wavy right-pointing arrow:

```tex
e^{\frac{i}{\hbar}\int L[q,\dot{q}]\,dt}
\rightsquigarrow
e^{-\frac{1}{\hbar}\int L^E[q,\partial_\tau q]\,d\tau}.
```

A blue brace-like mark sits over the right-hand integral and labels it:

```tex
S^E
```

The next prose fragment is:

```text
e.g.
```

The two example formulas follow:

```tex
L[q,\dot{q}]=\frac{1}{2}\dot{q}^{2}-V(q)
```

```tex
L^E[q,\partial_\tau q]=\frac{1}{2}(\partial_\tau q)^{2}+V(q)
```

A black bullet precedes the question:

```text
How do we evaluate the path integral?
```

The following two lines read:

```text
the key is to find a convenient regularized
expression of the measure [Dq].
```

The next example begins with `e.g.` and gives a mode expansion. The sum has a visible lower `n` and no visible upper bound on this page:

```tex
q(\tau)=\sum_n q_n f_n(\tau)
```

The `f_n(\tau)` term has a blue wavy underline. A blue arrow points up to that term, followed by the blue annotation:

```text
a suitable basis of
functions
```

The page ends with the following black measure sketch. Its arrow is wavy and points to the right. A question mark follows the product:

```tex
[Dq]\rightsquigarrow\prod_{n=1}^{\infty}dq_n\ ?
```

### Visual semantics

- The blue top annotation is a carry-forward note about notation. Its arrow curves upward from the annotation into the blank upper margin.
- `Define`, `so that`, `e.g.`, the question about the path integral, and the phrase about a convenient regularized measure are black handwritten prose.
- The Wick-rotation exponent factors `i/\hbar` and `-1/\hbar` are visibly blue. The surrounding exponentials, integrals, Lagrangians, and differentials are black.
- The blue `S^E` label is attached to the right-hand Euclidean action integral by a broad brace-like stroke.
- The two Lagrangian example lines are black. The first has the kinetic term with a minus potential, and the second has the Euclidean kinetic term with a plus potential.
- The mode expansion is black. The `f_n(\tau)` target is marked by a blue underline and arrow, and the blue note identifies it as a suitable basis of functions.
- The final `[Dq]` to product sketch is an open question in the source. The question mark is part of the handwritten line.

### Corrections, qualifications, and ambiguities

- The blue phrase `still use the same notation` is an annotation, not a new body-text sentence. It likely points back to notation established on an earlier page.
- The q-like glyph throughout the page is captured as `q`. Its loop and descender are clear in the oscillator context on page 30.
- The large handwritten L is captured as plain `L`; the image does not force a `\mathcal{L}` reading. The superscript `E` in `L^E` is clear.
- The first exponent has a blue `i/\hbar`, and the second has a blue `-1/\hbar`. Their blue ink suggests a source-layer correction or emphasis. The sign `+i` in the derivative relation is black and visibly written.
- The page-29 mode sum visibly has a lower `n` with no recoverable upper limit. Page 30 supplies the explicit `n=1` to `\infty` limits for the same expansion.
- The wavy arrows are transcribed as `\rightsquigarrow` to preserve their drawn character. Their exact intended relation is schematic.
- The brace-like blue mark identifies the right-hand integral with `S^E`; the exact equality or annotation syntax is not written.
- The final product line ends with a handwritten question mark. The source leaves the functional measure unresolved at this point.

### Topic boundary and disposition

This page continues the Wick rotation from Lorentzian time to imaginary time, defines the Euclidean Lagrangian, and introduces the functional-measure problem. The mode expansion begins the proposed regularization. The page should remain in the same argument unit as page 30, with the measure question carried into the worked oscillator example.

Recommended disposition: retain the equations and the boundary-to-measure question as exact note evidence. Preserve the blue notation note, the `S^E` marking, and the basis annotation in the source packet. Keep the question mark after the product until a later page resolves the measure. Do not infer a functional determinant or a normalized product from this page.

## Physical page 30

### Heading and exact reading order

The page begins with the underlined heading:

```text
Example 1:
```

The underline is a single black horizontal stroke below the heading.

The first formula is a Hamiltonian-Lagrangian correspondence. The central connector is a double-headed horizontal arrow:

```tex
H=\frac{p^{2}+\omega^{2}q^{2}}{2}
\longleftrightarrow
L=\frac{1}{2}\dot{q}^{2}-\frac{1}{2}\omega^{2}q^{2}.
```

The next line reads:

```text
Wick rotation t = -i\tau
```

Here `t = -i\tau` is mathematical notation inside the handwritten prose.

The next prose fragment is:

```text
To evaluate
```

The displayed path-integral sketch reads:

```tex
\int[Dq]e^{-\frac{1}{\hbar}S^E}\ \text{....}
```

The source shows a trailing cluster of four dot-like marks after `S^E`.

The next two lines read:

```text
choose boundary condition in imaginary time
e.g. q(-T) = q(T) = 0 .
```

The page contains a graph labeled `q(\tau)` on the vertical axis and `\tau` on the horizontal axis. The endpoint labels are blue:

```text
-T                         T       \tau
```

The graph is followed by:

```text
Expand q(\tau) on a Fourier basis
```

The black mode expansion is:

```tex
q(\tau)=\sum_{n=1}^{\infty}q_n f_n(\tau)
```

A blue arrow points to `f_n(\tau)`. The blue basis function written at the bottom is:

```tex
f_n(\tau)=\frac{1}{\sqrt{T}}\sin\frac{n\pi(\tau+T)}{2T}
```

### Visual semantics

- `Example 1:` is black and underlined.
- The Hamiltonian and Lagrangian formula is black. The connector has a left arrowhead and a right arrowhead.
- `Wick rotation t = -i\tau`, `To evaluate`, and the boundary-condition instruction are black handwritten text.
- The path-integral sketch is black. Its exponent contains `S^E`, followed by four black dot-like marks that indicate omitted continuation.
- The graph has black horizontal and vertical axes, each with an arrowhead. The vertical label is `q(\tau)`, and the horizontal label is `\tau`.
- The plotted curve is magenta/purple. It starts at zero at `-T`, rises above the axis, drops below the axis to a negative trough, crosses back upward, reaches a broader positive region, has small visible wiggles, and returns to zero at `T`.
- Blue marks identify `-T` and `T` on the graph. Small blue endpoint strokes sit at the two boundary locations.
- `Expand q(\tau) on a Fourier basis` and the mode expansion are black. The arrow to `f_n(\tau)` and the full basis-function formula are blue.

### Corrections, qualifications, and ambiguities

- The page presents `q(-T)=q(T)=0` as an example boundary condition in imaginary time. The endpoint equality and the zero value are clear.
- The graph is qualitative. Its intermediate purple path is not an exact data set and should not be reconstructed as a prescribed function.
- The path-integral line ends in four visible dots. Their intended TeX could be an ellipsis, but the source capture preserves the dot count in the transcription.
- The formula uses the same q and L notation as page 29. The oscillator q glyph is the clearest evidence for the q reading.
- The lower and upper limits `n=1` and `\infty` are explicit on this page. They clarify the page-29 mode-sum sketch.
- The basis formula is visibly blue and reads with numerator `n\pi(\tau+T)` and denominator `2T`. The sine argument has no additional square brackets or parentheses beyond the displayed fraction and the parentheses around `\tau+T`.

### Topic boundary and disposition

Page 30 opens the first worked example: the harmonic oscillator, Wick rotation, a Euclidean path integral, an imaginary-time boundary condition, and a Fourier-mode basis. It resolves the mode bounds left open on page 29 and continues the measure regularization setup.

Recommended disposition: keep `Example 1` as the section boundary for the oscillator example. Retain the Hamiltonian-Lagrangian formula, the Wick rotation, the boundary condition, the qualitative graph, the explicit sine basis, and the source ellipsis. Treat the graph as a visual explanation of the endpoint condition. Carry the four-dot continuation as unresolved source punctuation until the adjacent pages identify the omitted calculation.

## Page transition 29 to 30

Page 29 ends with the open schematic question

```tex
[Dq]\rightsquigarrow\prod_{n=1}^{\infty}dq_n\ ?
```

Page 30 begins immediately with `Example 1:` and applies the question to the harmonic oscillator. The transition is continuous: a general Euclidean path-integral measure problem becomes a finite-interval oscillator example, then a Fourier expansion with Dirichlet endpoint data. Preserve this order in the source map and in later written use.

## Consolidated recommended dispositions

- Page 29: `NOTES_EXACT` for all displayed formulas and the literal prose fragments; retain blue annotations as source annotations attached to the same argument unit.
- Page 30: `NOTES_EXACT` for the oscillator equations, boundary condition, and Fourier basis; retain the graph as a source figure or figure description.
- Measure arrow: preserve as a schematic open question. Later derivation may answer it only with adjacent source evidence.
- Page-29 summation bounds: preserve the visible `\sum_n` form and use page 30 as the source-backed explicit bounds.
- Trailing dots after `S^E`: preserve the four-dot source mark in the evidence layer. Leave the missing continuation unresolved here.
- Page transition: merge into one conceptual source unit covering Wick rotation, Euclidean action, path-integral measure regularization, and the harmonic-oscillator Fourier basis.
