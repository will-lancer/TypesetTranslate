# Pass 1 source capture: physical pages 053-054

## Capture identity

- Project: `yin_qft_253abc`
- Lane: handwritten-note pages 053-054
- Combined source PDF: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`
- Source PDF SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`
- Source PDF physical pages: 786
- Physical pages captured: 53 and 54
- Attached full-page renders: `tmp/pdfs/253a-ch02-source/page-053.png` and `tmp/pdfs/253a-ch02-source/page-054.png`
- Source-PDF visual check: fresh 300 dpi renders of physical pages 53-54 from the manifest PDF
- Text extraction check: `pdftotext` returns no page text. The pages are scanned handwriting, so visual inspection governs this capture.
- Visible printed or handwritten page number: none on either page.
- Project mapping only: if Chapter 2 note pages begin at physical page 20 as note page 10, these are inferred note pages 43 and 44. The number mapping is not visible ink on the captured pages.

The two attached renders and the fresh source-PDF renders agree in content and placement. This lane records the note evidence. Speech, captions, and video timestamps remain for the separate lecture lane.

## Physical page 053

### Exact visible transcription, reading order

```text
In conclusion :

<q(τ) q(0)> ≡ G(τ) = ∫ dk/(2π) G̃(k) e^{ikτ}

G̃(k) = 1 / [ k² + 1 + g/4 - g²/8 (1/4 + 1/(k²+9)) + O(g³) ].

On the other hand, we know

<0| q-hat(τ) q-hat(0) |0>                         (for τ > 0)
= Σ_n |<n| q-hat(0) |0>|² e^{-(Ē_n-Ē_0)τ}.

Consider the analytic continuation of
G̃(k) to the complex k-plane:
```

The angle brackets, kets, hats, overbars, tilde, integral, and exponent are all present in the source. The display has no visible integral limits. The period at the end of the perturbative denominator is source punctuation.

### Exact TeX transcription

The first displayed identity is:

```tex
\[
\langle q(\tau)q(0)\rangle \equiv G(\tau)
= \int \frac{dk}{2\pi}\,\widetilde G(k)e^{ik\tau}.
\]
```

The momentum-space expression is:

```tex
\[
\widetilde G(k)
= \frac{1}{k^2+1+\frac{g}{4}
-\frac{g^2}{8}\left(\frac{1}{4}+\frac{1}{k^2+9}\right)
+O(g^3)}.
\]
```

The spectral statement is written on two lines. The qualification sits to the right of the first line:

```tex
\[
\left\langle 0\left|\widehat q(\tau)\widehat q(0)\right|0\right\rangle
\qquad (\text{for }\tau>0)
\]
\[
= \sum_n
\left|\left\langle n\left|\widehat q(0)\right|0\right\rangle\right|^2
e^{-(\bar E_n-\bar E_0)\tau}.
\]
```

The overbars on `E_n` and `E_0` are part of the source notation. The adjacent energy discussion on physical pages 46-48 uses the same overbar notation.

### Diagram and visual semantics

The diagram occupies the lower half of the page beneath the two-line sentence about analytic continuation.

- A black, hand-drawn, roughly rectangular outline encloses the complex-plane sketch. Its sides and lower edge are irregular. It functions as the surrounding frame for the sketch. No black coordinate axes are drawn inside it.
- Near the upper-left interior is a black handwritten `k` with a horizontal black stroke beneath it. The stroke may be an underline or a short axis-marking stroke. There is no visible `\operatorname{Im}k` label.
- A blue horizontal line crosses the frame at the real axis. It extends slightly past the left black boundary, runs to the right boundary, and has a blue arrow pointing right near its center. The blue label at the right reads `Re k`, with the `e` written at the height of the `R`, followed by `k`.
- A green arc closes the real-axis path through the upper half-plane. It begins near the left endpoint of the blue line and ends near the right endpoint. The green arrow near the upper-left part of the arc points down and left along the arc, giving the upper arc the return orientation after the blue real-axis segment.
- Red marks on the central vertical line represent poles. Two red `x` marks lie in the upper half-plane. A red vertical dotted continuation appears above them. Two further red `x` marks lie below the real axis, followed by a red vertical dotted continuation toward the bottom of the frame.
- The upper marks have nearby colored labels. The red word is `poles`. The purple label beside the higher red cross reads `iω_2`; the purple label beside the lower red cross reads `iω_1`. The source does not visibly write `k=` before either purple label.
- The black frame, red pole marks, purple pole labels, blue real-axis path, blue arrow, blue `Re k` label, and green closing arc are separate visual layers. Preserve those colors when the diagram is later redrawn or represented as a figure.
- No crossed-out formula, overwritten prose, or marginal correction is visible on page 053. The colored marks are diagram components and labels.

### Page-053 ambiguities and constraints

1. The black upper-left `k` has a black stroke beneath it. Preserve the stroke in the figure record. Do not turn it into an unshown `\operatorname{Im}k` axis label.
2. The blue label is visually `Re k`. `\operatorname{Re}k` is the closest TeX reading. The source does not show an equality sign or a full pair of coordinate-axis labels.
3. The red crosses are explicit. The red vertical dots above and below are continuation marks. The lower crosses have no visible `-i\omega_n` labels. Do not add those labels from contour theory during source capture.
4. The purple labels visibly read `iω_2` and `iω_1`. A preceding `k=` is absent or too faint to recover. Record the labels as `i\omega_2` and `i\omega_1`.
5. The energy accents in the spectral exponential are overbars. The same reading is confirmed by the nearby source pages. Do not replace them with hats or tildes.
6. The handwritten source uses the letter `q` throughout these formulas. The glyph can resemble `g` at small size. The immediately preceding pages identify the field as `q`; transcribe `q`.

## Physical page 054

### Exact visible transcription, reading order

```text
Closing the contour at infinity on UHP.

∫ dk/(2π) G̃(k) e^{ikτ}
= i Σ_n e^{-ω_nτ} Res_{k→iω_n} G̃(k)

[blue box] ω_n = Ē_n - Ē_0

e.g. from G̃(k) = 1 / [ k² + 1 + g/4 - g²/8 (1/4 + 1/(k²+9)) + ...., ]

we expect the first pole to be near k ≃ i,

ω_1 = 1 + g/8 - g²/32 + O(g³).

[green bracket] agrees with Hamiltonian perturbation theory at second order [green bracket]

------------------------------------------------------------  [two short black marks at the right]

Example 3:

L = 1/2 q-dot² - 1/2 q² - g/8 q² q-dot²

p = ∂L/∂q-dot = q-dot (1 - g/4 q²)

H = q-dot p - L

= 1/2 (1 - g/4 q²)^(-1) p² + 1/2 q².
```

The source line after the repeated denominator has a handwritten continuation made of small dots followed by a comma. The raw line above records the visible continuation as `....,`; see the ambiguity entry below.

### Exact TeX transcription

The contour statement is:

```tex
\[
\int \frac{dk}{2\pi}\,\widetilde G(k)e^{ik\tau}
= i\sum_n e^{-\omega_n\tau}
\underset{k\to i\omega_n}{\operatorname{Res}}\,\widetilde G(k).
\]
```

The blue-boxed definition is:

```tex
\[
\omega_n=\bar E_n-\bar E_0.
\]
```

The repeated example is transcribed with its source continuation marks rather than with the `O(g^3)` form used above:

```tex
\[
\widetilde G(k)
= \frac{1}{k^2+1+\frac{g}{4}
-\frac{g^2}{8}\left(\frac{1}{4}+\frac{1}{k^2+9}\right)
+\ldotp\ldotp\ldotp\ldotp,}
\]
```

The first-pole estimate is:

```tex
\[
\omega_1=1+\frac{g}{8}-\frac{g^2}{32}+O(g^3).
\]
```

The lower-page example begins with:

```tex
\[
L=\frac{1}{2}\dot q^{\,2}-\frac{1}{2}q^2-\frac{g}{8}q^2\dot q^{\,2}.
\]
\[
p=\frac{\partial L}{\partial\dot q}
=\dot q\left(1-\frac{g}{4}q^2\right).
\]
\[
H=\dot q\,p-L
\]
\[
=\frac{1}{2}\left(1-\frac{g}{4}q^2\right)^{-1}p^2+\frac{1}{2}q^2.
\]
```

The source writes the last two lines as one vertical derivation: `H = q-dot p - L` followed by the line beginning with `=`. The display split above preserves the equation order and makes the continuation explicit.

### Color, annotation, and layout inventory

- The residue equation and the surrounding prose are black.
- A blue, uneven rectangular box surrounds `ω_n = Ē_n - Ē_0`.
- The first-pole equation has a blue check mark to its right. The check is an annotation, not part of the formula.
- A green square-bracket annotation below the first-pole equation reads `agrees with Hamiltonian perturbation theory at second order`. The left bracket begins before `agrees`; the right bracket closes after `order`. The annotation is green and spans two handwritten lines.
- A long black horizontal divider separates the perturbative pole discussion from `Example 3:`. At the right end are two short black vertical marks that resemble `!!` or a pair of continuation strokes. Their exact intended punctuation is uncertain.
- `Example 3:` is black and underlined with a separate black horizontal stroke.
- The Lagrangian, momentum, and Hamiltonian derivation below the heading are black. No color annotation accompanies this lower example on page 54.
- There is no visible printed or handwritten page number.
- No crossed-out formula or overwritten line is visible. The blue check and green bracket are validation annotations attached to the pole estimate.

### Page-054 ambiguities and constraints

1. The repeated `\widetilde G(k)` denominator visibly ends with a run of dots and a comma. Four dots are the best visual reading at the supplied resolution. The TeX line uses four `\ldotp` marks to preserve that reading. Do not silently convert this occurrence to `O(g^3)`.
2. The residue notation has a subscript that reads `k→iω_n`. The source places `Res` immediately before `\widetilde G(k)` and does not show an extra parenthesis.
3. The black divider's two marks at the far right are not a recoverable word or equation. Preserve them as a visual divider annotation with an uncertainty flag.
4. The first energy and ground-state energy in the blue box carry overbars. This agrees with the energy notation on the preceding pages.
5. The word `UHP` is written in capitals. Preserve the abbreviation in the transcription and use `\text{UHP}` only when a later written rendering needs the prose label.
6. The lower example uses `q`, `\dot q`, and `p` without hats. The hats belong to the operator spectral formula above and do not carry into this classical Lagrangian-Hamiltonian example.

## Topic boundaries and page transitions

### Physical page 052 to 053

Physical page 052 ends the perturbative self-energy calculation. Page 053 opens with `In conclusion :` and assembles the full two-point function from the momentum-space expression. The page then changes viewpoint from the perturbative expression to the exact spectral representation and its pole interpretation.

### Physical page 053 to 054

Page 053 ends with the complex `k`-plane sketch. Page 054 begins with `Closing the contour at infinity on UHP.` and evaluates the real-axis Fourier integral by the upper-half-plane residues. The residue equation, the definition `ω_n = Ē_n - Ē_0`, the first-pole estimate, and the Hamiltonian comparison form one continuous argument.

### Physical page 054 to 055

The divider and underlined `Example 3:` mark a new example. Page 054 supplies its Lagrangian, canonical momentum, and Hamiltonian. Physical page 055 continues the same example with a change of variable and the question `ordering in quantization?`. The example therefore remains open at the bottom edge of page 054.

## Recommended dispositions

| Unit | Source material | Recommended pass-1 disposition |
|---|---|---|
| `253a-ch02-p053-u01` | `In conclusion :`; definition of `G(τ)` and Fourier representation | Retain as a source-exact conclusion to the preceding perturbative two-point-function calculation. Merge with the preceding self-energy result when the argument map is built. |
| `253a-ch02-p053-u02` | Full `\widetilde G(k)` denominator through `O(g^3)` | Retain as `NOTES_EXACT`. Keep the denominator order and signs. Preserve the displayed perturbative remainder. |
| `253a-ch02-p053-u03` | Spectral matrix element and `τ>0` qualification | Retain as `NOTES_EXACT`. Keep the hats on the operator `q`, the overbarred energies, and the explicit positive-time condition. |
| `253a-ch02-p053-u04` | Complex `k`-plane diagram with UHP arc, real-axis arrow, pole marks, labels, and colors | Retain as a figure source. Reproduce the semantic layers and colors in a later redraw. Resolve the underline beneath `k` only from stronger source evidence. |
| `253a-ch02-p054-u01` | UHP closure and residue sum | Merge directly with `253a-ch02-p053-u04`. Retain the factor `i`, the upper-half-plane statement, and the residue subscript. |
| `253a-ch02-p054-u02` | Boxed energy-gap definition, first-pole estimate, blue check, and green comparison note | Retain the equations and annotations. The green note records a source claim that the pole result agrees with Hamiltonian perturbation theory at second order. |
| `253a-ch02-p054-u03` | Divider and `Example 3:` | Use as a new argument-map unit after the contour discussion. Preserve the underlining and the source boundary. |
| `253a-ch02-p054-u04` | Lagrangian, canonical momentum, and Hamiltonian | Retain as the opening equations of `Example 3`. Merge with physical page 055, which continues the example. |

## Source-capture blockers

- No substantive formula is withheld. The only unresolved visual readings are the exact dot count and punctuation after the repeated page-054 denominator, the two marks at the right of the horizontal divider, and the function of the underline beneath the diagram's `k` label.
- No page number is visible in the source image. Physical page numbers 53 and 54 are PDF provenance identifiers.
- No caption or video claim is assigned to these note units in this file. Alignment belongs to the separate lecture-source lane.
