# Pass 1 source capture: physical pages 49-50

## Source identity and inspection record

- Combined source PDF: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`
- PDF SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`, matching `SOURCE_MANIFEST.yaml`.
- Physical pages captured: 49 and 50 of 786.
- Attached renders inspected: `tmp/pdfs/253a-ch02-source/page-049.png` and `page-050.png`.
- Direct renders made from the combined PDF at 150 dpi match the two attached PNGs byte-for-byte.
- The handwritten page-label field is blank on both pages. Printed header, footer, and page-number fields are blank.
- The page images are handwritten notes with black primary writing, blue annotations, purple annotations, gray explanatory writing, and red diagram endpoints or labels.
- `pdftotext` returns an empty text layer for these physical pages. The render is the source for the capture.

## Physical page 49

### Page identity and boundary

The page opens after the Hamiltonian perturbation-theory comparison on physical page 48. Page 48 ends with the leading-order ground-state energy shift, written as $g/32$. Page 49 changes to the path-integral computation of the two-point function.

The page closes with the factorization statement for connected and disconnected graphs. Physical page 50 begins with the cancellation of $Z$ against $1/Z$, so the page break falls inside that derivation.

### Exact visible transcription

The upper black writing reads:

> Next we turn to

At the upper right, in black:

> (\(\tau>0\))

The first black equation is:

$$
\left\langle 0\middle|\hat q(\tau)\hat q(0)\middle|0\right\rangle
$$

The next line is:

$$
=\frac{1}{Z}\int [Dq]\,e^{-\int d\tau\,L^E}\cdot q(\tau)q(0).
$$

The centered multiplication dot between the exponential and $q(\tau)$ is visible. The final period is visible.

The purple annotation below the first path-integral line reads:

> expand in g.

A purple curved arrow rises from this annotation and points to $L^E$ in the exponent.

The next black equation expands the interaction:

$$
=\frac{1}{Z}\left\langle q(\tau)q(0)
\sum_{n=0}^{\infty}\frac{1}{n!}
\left(-\frac{g}{4!}\int d\tau'\,q^4(\tau')\right)^n
\right\rangle_0.
$$

The blue $0$ at the right of the closing angle bracket is written below the bracket baseline and functions as the subscript on the expectation value. The source uses $q^4(\tau')$, with the fourth power on $q$ before the parenthesized time argument.

The blue annotation below the expansion reads, with the source line breaks:

> Computed by summing up all possible
>
> Wick contractions, consisting of terms
>
> like

A blue arrow rises from this annotation toward the expansion sum.

The illustrative black formula fragment below the annotation is recoverable as:

$$
q(\tau)q(0)
\left(-\frac{g}{4!}\int d\tau'\,q^4(\tau')\right)
\left(\text{graphical Wick-contraction pattern}\right)\cdots
$$

The parenthesized contraction pattern is graphical. Purple overbrackets group the external $q(\tau)q(0)$, the interaction factor, and the successive contraction choices. The rightmost grouping ends in a purple ellipsis.

The graph immediately below the illustrative term has red endpoint dots. The left red label reads $x(\tau)$, and the right red label reads $x(0)$. A black horizontal propagator line joins the two endpoints. A black central vertex carries a purple loop, and purple coloring continues along portions of the horizontal line. This is the connected example selected from the contraction sum.

To the right, a blue brace marks a sequence of separate graphical contraction pieces. The blue text reads:

> other disconnected
> component of
> the graph

The black sentence near the bottom reads:

> The sum factorizes as

The factorization is then shown by a graph on the left and text on the right. The left graph has red endpoints labeled $q(\tau)$ and $q(0)$, magenta line segments, and a black oval around the word `connected`. A black multiplication sign follows. The parenthesized black text reads:

$$
\text{connected}\times(\text{disconnected graphs})
$$

A blue brace underlines `disconnected graphs`.

### Visual semantics and annotations

- Black writing carries the correlator, the path-integral expression, the perturbative expansion, the example term, and the factorization sentence.
- Purple marks identify the instruction to expand in $g$, point to $L^E$, group contraction factors, and color the connected example graph.
- Blue marks identify the Wick-contraction sum, point to the expansion sum, label the disconnected component, and supply the subscript $0$ on the free expectation value.
- Red marks identify graph endpoints and the endpoint labels in the connected example.
- The connected example contains one straight external line with a loop attached at its central vertex. The separate right-hand contraction pieces represent a disconnected graph component.
- The lower factorization picture gives a connected graph with the two external endpoints multiplied by a parenthesized collection of disconnected graphs.

### Ambiguities and source qualifications

- The field letter is $q$ throughout the equations. Its handwritten descender can resemble $g$ at reduced resolution. The $q$ reading agrees with physical page 48, where the oscillator Hamiltonian uses $q$. The coupling is $g$.
- The interaction coupling is the lowercase $g$ in $-g/4!$. The field in $q^4(\tau')$ is $q$.
- The Euclidean Lagrangian symbol is a plain handwritten $L$ with superscript $E$. The source stroke supports $L^E$; a calligraphic $\mathcal L^E$ would add an editorial styling choice.
- The red endpoint glyphs in the connected example read as $x(\tau)$ and $x(0)$. This creates a local symbol mismatch candidate with the $q$-based equations. The red $x$ reading is retained in the capture.
- The individual Wick-pairing arcs in the parenthesized schematic are graphical and partly overlapped by purple brackets. Their topology is captured in the visual description. The pairing index remains outside the recoverable written layer.
- The blue subscript $0$ is an annotation placed under the closing bracket. Its mathematical role is clear. Its blue color records its status as an after-the-fact correction.

### Recommended disposition

Retain the page as `NOTES_EXACT` source material. The page supplies the path-integral expansion of the positive-$\tau$ two-point function and the connected/disconnected factorization argument. Preserve the $x(\tau)$, $x(0)$ labels as a source ambiguity until a later frame or equation audit resolves them. Preserve the purple and blue marks as annotation metadata attached to the equation and graph records.

## Physical page 50

### Page identity and boundary

Page 50 continues the factorization argument from physical page 49. Its upper blue annotation records the normalization cancellation. The page then lists the resulting connected diagrams and defines the Euclidean Green function for both signs of $\tau$.

The final Fourier representation of $G_0(\tau)$ leads directly to physical page 51, where the one-loop diagram is evaluated and the $g^2$ diagrams are introduced. Page 51 is boundary evidence only for this lane.

### Exact visible transcription

At the upper right, in blue, the annotation reads:

> “Z”
>
> Cancels against \(\frac{1}{Z}\).

The black text below reads:

> leaving the result

The black correlator is repeated:

$$
\left\langle 0\middle|\hat q(\tau)\hat q(0)\middle|0\right\rangle
$$

The result is followed by a diagrammatic sum. Its exact visible order is:

$$
\text{free line}
+\text{one-tadpole line}
+\text{two-tadpole line}
+\text{double-tadpole line}
+\text{bubble line}+\cdots
$$

The words in this display are capture labels for the drawn graphs. The source page carries the diagrams themselves.

The first graph is a straight black line between red endpoints. The endpoint labels below it read $q(\tau)$ and $q(0)$. The next graph is a straight line with one loop attached at its center. The following graph has two separate tadpole loops attached along the line. On the second row, the first graph has a double or nested tadpole structure at one central region. The next graph has a large oval loop attached to the horizontal line at two points. A plus sign and a horizontal ellipsis continue the series.

The gray note below the diagrams reads, preserving its line break:

> Note: Same path integral expression computes
>
> \(\langle 0|\hat q(0)\hat q(\tau)|0\rangle\) for \(\tau<0\).

The gray explanatory sentence reads:

> We can write the Euclidean Green function

The gray piecewise equation is:

$$
\left\langle q(\tau)q(0)\right\rangle
=
\begin{cases}
\left\langle 0\middle|\hat q(\tau)\hat q(0)\middle|0\right\rangle, & \tau\ge 0,\\
\left\langle 0\middle|\hat q(0)\hat q(\tau)\middle|0\right\rangle, & \tau<0.
\end{cases}
$$

The source writes the first branch with $\tau\ge 0$ and the earlier page-49 heading with $\tau>0$. Both conditions are retained as written.

The free two-point graph appears below the piecewise equation. It is a black horizontal line with red endpoint dots. The left endpoint is labeled $\tau$, and the right endpoint is labeled $0$. The next black equation reads:

$$
G_0(\tau)=\frac{1}{2}e^{-|\tau|}.
$$

The blue Fourier representation reads:

$$
=\int\frac{dk}{2\pi}\,\widetilde G_0(k)e^{ik\tau},
\qquad
\widetilde G_0(k)=\frac{1}{k^2+1}.
$$

The comma between the Fourier integral and the definition of $\widetilde G_0(k)$ is visible. The tilde on $G_0$ is visible in both blue occurrences.

### Visual semantics and annotations

- The blue normalization note sits above the result and explains the disappearance of the $1/Z$ factor.
- The first row of graphs shows the free line, a single tadpole correction, and two tadpoles placed along the line.
- The second row continues with a nested or double tadpole, then a large oval bubble attached to the line, followed by $+\cdots$.
- The external graph endpoints are red. The graph lines and loop outlines are black.
- The note and piecewise Euclidean Green-function definition are written in gray.
- The explicit free Green-function value is black. Its Fourier representation and the momentum-space propagator are blue.
- The free graph labels use $\tau$ and $0$ directly under the red endpoints.

### Ambiguities and source qualifications

- Page 50 carries $\tau\ge 0$ in the first branch. Page 49 carries $\tau>0$ beside the operator ordering. The source distinction is preserved for later reconciliation.
- The fourth graph in the diagrammatic series has two touching or nested loop outlines at a central region. The render supports a double-tadpole reading. The exact vertex placement belongs to the diagram audit.
- The fifth graph is a large oval loop intersecting the horizontal line at two black points. It is captured as the bubble-line topology visible in the render.
- The gray color is retained as a source color label. The writing medium remains unspecified.
- The source uses $q$ for the field and $g$ for the coupling. The graph labels and equations retain that distinction.

### Recommended disposition

Retain the page as `NOTES_EXACT`. Use its result-diagram sequence as the source figure for the connected perturbative two-point function. Use the gray piecewise equation as the source definition of the Euclidean Green function, including the two sign conditions. Use the black and blue $G_0$ formulas as separate equation records because the source changes color at the Fourier representation.

## Page transition and topic boundary record

- Physical page 48 to 49: Hamiltonian perturbation theory and the leading ground-state energy shift give way to the path-integral two-point function.
- Physical page 49 to 50: the expansion by Wick contractions is organized into connected and disconnected factors. Page 50 records that $Z$ cancels against $1/Z$, then displays the remaining connected diagrams.
- Physical page 50 to 51: the free Euclidean Green function $G_0(\tau)$ and its Fourier transform prepare the one-loop evaluation on page 51.
- Page 49 source disposition: `NOTES_EXACT`, retained for the path-integral expansion, Wick-contraction example, and graph factorization.
- Page 50 source disposition: `NOTES_EXACT`, retained for the connected-diagram series, Euclidean time ordering, and free Green-function representations.
- The page-49 red $x$-labels, the blue expectation-value subscript, and the page-49/page-50 inequality change require explicit ambiguity records in later passes.
