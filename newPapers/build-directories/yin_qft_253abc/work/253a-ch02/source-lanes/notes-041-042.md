# Pass 1 source capture: physical pages 41-42

## Evidence and scope

This lane records the handwritten-note pages at physical PDF pages 41 and 42 of `qft_253abc_book.pdf`. The source PDF is `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`, with manifest SHA-256 `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`.

The supplied renders are `tmp/pdfs/253a-ch02-source/page-041.png` and `tmp/pdfs/253a-ch02-source/page-042.png`. Direct `pdftoppm` renders of physical pages 41 and 42 match those files byte for byte. The page dimensions are 1275 x 1668 pixels in RGB. Visual inspection governs the transcription. The PDF text layer returns page separators for these scans.

Original handwritten page labels: page 41, none visible; page 42, none visible. The pages contain no printed footer or source-page number. The only page-local numbering is the heading `Example 2` on page 41.

Audio, captions, and video alignment remain outside this note-only lane. The note evidence is ready for later reconciliation with those layers.

## Physical page 41

### Exact transcription in reading order

The heading reads `Example 2`, with a horizontal underline. To its right is the quoted phrase, written in lowercase:

```text
Example 2        "an harmonic oscillator"
```

The Hamiltonian is followed by a left-right arrow and the corresponding Lagrangian:

```tex
H = \frac{p^2}{2} + \frac{q^2}{2} + \frac{1}{4!} g q^4
\leftrightarrow
L = \frac{1}{2}\dot{q}^{,2} - \frac{q^2}{2} - \frac{1}{4!} g q^4.
```

The next prose fragment reads:

```text
Study its spectrum using
```

The black operator correlator has the condition `(tau > 0)` written to its right. Its exact TeX is:

```tex
\left\langle 0\middle|\hat{q}(\tau)\hat{q}(0)\middle|0\right\rangle
\qquad (\tau > 0)
```

The spectral decomposition is written in blue:

```tex
\left\langle 0\middle|\hat{q}(\tau)\hat{q}(0)\middle|0\right\rangle
= \sum_n \left|\left\langle n\middle|\hat{q}(0)\middle|0\right\rangle\right|^2 e^{-E_n\tau}
```

A large black curved arrow descends from the correlator and points to the Euclidean path-integral expression. The expression ends with a comma in the scan:

```tex
= \frac{1}{Z}\int [Dq]\,e^{-\int d\tau\,L^E}\,q(\tau)q(0),
```

The following prose fragment reads `where`, followed by:

```tex
Z = \int [Dq] e^{-\int d\tau\,L^E}
```

The last display on the page is:

```tex
L^E = \frac{1}{2}(\partial_\tau q)^2 + \frac{q^2}{2} + \frac{1}{4!} g q^4
```

### Visual semantics

Black ink carries the heading, quotation, Hamiltonian, Lagrangian, prose, operator correlator, arrow, path-integral formula, partition function, and Euclidean Lagrangian. The spectral sum is blue. The underline beneath `Example 2` is black. The downward arrow is a single thick black curve with an arrowhead at its lower end. The page has no diagram, graph, check mark, colored bracket, or crossed-out correction. Its lower region after the final display is blank.

The hats are visible on the two operators in the correlator and disappear in the path-integral formulas. The path-integral insertion is therefore plain `q(\tau)q(0)`, while the spectrum formula uses the hatted operator `\hat{q}`. The superscript `E` on `L^E` is raised and black in each occurrence.

### Ambiguities and resolved characters

- The heading says `an harmonic oscillator`. The article is written that way in the source and belongs in the exact transcription.
- The condition is a strict `\tau > 0`. The comparison mark has a single upper stroke and no lower bar.
- The coordinate is the handwritten looped `q`. Context from the neighboring free-theory page supports `q` in every occurrence here.
- The operator hats are small but clear. The path-integral measure is handwritten as `[Dq]`; the source does not supply a calligraphic `\mathcal D`.
- The quartic coupling is `g q^4`, with `1/4!` as the prefactor. The same coupling appears in the Hamiltonian, Minkowski Lagrangian, and Euclidean Lagrangian.
- The final path-integral correlator has a visible comma. The `Z` definition has no terminal punctuation.

### Topic and boundary

Physical page 40 ends with the free momentum-space two-point function and a blue check mark. Page 41 starts a new example built from the quartic oscillator Hamiltonian. The page then moves from the oscillator spectrum to the Euclidean correlator, the functional integral, the partition function, and the Euclidean Lagrangian. The final `L^E` display supplies the quadratic and quartic terms used in the coupling expansion on page 42.

### Recommended disposition

Retain the heading, quotation, prose fragments, qualifier, arrow, and every display in the exact note layer under `NOTES_EXACT`. Use the Hamiltonian-Lagrangian pair to open the quartic-oscillator example. Use the blue spectral formula as the source for the later spectrum argument. Keep the black path-integral arrow as source metadata; a later chapter figure or prose transition can absorb its mathematical role. Preserve the distinction between hatted operator insertions and plain path-integral fields during equation reconciliation.

## Physical page 42

### Exact transcription in reading order

The page begins with:

```text
Expand in powers of g,
```

The partition function is expanded as follows. The small centered dot before the sum is visible in the source:

```tex
Z = \int [Dq]\,e^{-\int d\tau\,\left(\frac{1}{2}(\partial_\tau q)^2 + \frac{q^2}{2}\right)}
\cdot \sum_{n=0}^{\infty} \frac{1}{n!}
\left(-\frac{g}{4!}\int d\tau'\,q^4(\tau')\right)^n
```

The next lines read:

```text
Assume that we can exchange the order of
\int[Dq] with the expansion in g
```

`Assume` is red. An orange square-bracketed annotation at the right reads, over two lines:

```text
[actually problematic,
 will revisit later]
```

The normalized partition-function expansion is:

```tex
\frac{Z_g}{Z_0}
= \sum_{n=0}^{\infty} \frac{1}{n!}
\left(-\frac{g}{4!}\right)^n
\int d\tau_1\cdots d\tau_n
\cdot \left\langle (q(\tau_1))^4\cdots(q(\tau_n))^4\right\rangle_0
```

The `0` on the expectation value is blue. A blue brace below the expectation points to the annotation:

```text
computed using
"free" Wick contractions
```

The page then gives:

```text
e.g.
order g:
```

followed by the order-`g` expression:

```tex
-\frac{g}{4!}\int d\tau\,\left\langle(q(\tau))^4\right\rangle_0
```

At the bottom is a row of three Wick-contraction diagrams separated by two plus signs. Each term is a black X-like four-valent interaction vertex with four black half-edges and two purple contraction arcs. The first drawing pairs the upper half-edges and the lower half-edges. The second drawing has a purple loop on the left and a purple loop on the right. The third drawing shows the remaining pairing, with the arcs arranged across the vertex. The plus signs are blue. The diagrams have no external labels, axes, arrows, or numerical coefficients.

### Visual semantics

The main prose and equations are black. The word `Assume` is red. The caveat in square brackets is orange. The expectation-value subscript `0`, the brace below the expectation, the words `computed using`, the quoted word `free`, the words `Wick contractions`, and the two plus signs in the diagram row are blue. The contraction arcs are purple or magenta. The interaction vertices and their short legs are black.

The orange bracket is a written correction to the displayed assumption. It qualifies the exchange of the functional integral with the power series and points forward to a later discussion. The blue annotation identifies the subscript-`0` expectation as a free-theory Wick-contraction calculation. The bottom graph row gives all pairings of four fields at one quartic vertex.

There is no handwritten page label, footer, page number, crossed-out equation, or separate graph on page 42. The graph row is the only diagram. No directional arrow appears on this page; the blue brace functions as an annotation pointer.

### Ambiguities and resolved characters

- The orange word is `problematic`, followed by a comma. The full annotation is `[actually problematic, will revisit later]`.
- The prime on `\tau'` in the interaction integral is visible. The field factor is `q^4(\tau')`.
- Both visible centered dots are multiplication dots: one precedes the sum in the expansion of `Z`, and one separates the multiple integral from the free expectation in `Z_g/Z_0`.
- The ratio uses `Z_g` over `Z_0`, with the lower `0` in the denominator. The expectation subscript is also `0`, colored blue in this display.
- The ellipses between the quartic factors are handwritten as continuation dots. They are transcribed as `\cdots`.
- The first two Wick diagrams are easy to separate. The purple arcs in the third drawing overlap the black legs more heavily; its role is the remaining fourth-field pairing.
- The measure glyph remains `[Dq]`. The note layer gives no normalization convention beyond the displayed ratio `Z_g/Z_0`.

### Topic and boundary

Page 42 continues the quartic-oscillator example without a new heading. It expands the Euclidean partition function in the coupling `g`, records the exchange assumption and its warning, normalizes by `Z_0`, and identifies the resulting expectation values with free Wick contractions. The lower diagram row instantiates the first-order term.

The neighboring page 43 begins by evaluating that row with:

```tex
= -\frac{g}{8}\int d\tau\,(G_0(\tau,\tau))^2
```

The page-43 blue annotation labels `G_0(\tau,\tau)` as `"const"` and states `diverges as tau -> +/- infinity, will interpret later.` Page 43 then moves to `At order g^2.` and the connected/disconnected Wick-contraction diagrams. This establishes the forward transition from the page-42 order-`g` diagrams.

### Recommended disposition

Retain the coupling expansion and normalized ratio as `NOTES_EXACT` equations. Preserve the red word `Assume` and the orange caveat in the source annotation ledger so the later prose carries the mathematical warning. Preserve the blue free-Wick annotation beside the expectation value. Reconstruct the bottom row as a figure or an equation-adjacent diagram with three distinct pairings, retaining the source colors during visual reconciliation. Link the page-42 order-`g` expression to the page-43 `G_0(\tau,\tau)` evaluation as one continuous source unit.

## Lane-level disposition

Pages 41 and 42 form one continuous source unit, `Example 2`, from the quartic oscillator through the first perturbative Wick contractions. Page 41 supplies the exact model, operator correlator, spectral sum, and Euclidean path-integral definitions. Page 42 supplies the expansion parameter, the exchange caveat, the normalized free-theory expectation values, and the order-`g` diagrams. The later chapter draft should preserve this source order and should attach lecture evidence after the note layer is reconciled.
