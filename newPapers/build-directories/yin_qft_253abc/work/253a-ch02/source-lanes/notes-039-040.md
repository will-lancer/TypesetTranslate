# Pass 1 source capture: physical pages 39–40

## Provenance

- Project: Xi Yin, Physics 253a, Chapter 2, “Lagrangian Quantum Mechanics, Path Integrals, and Perturbation Theory.”
- Source: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`, physical pages 39–40.
- Source SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`.
- Rendered source images inspected: `tmp/pdfs/253a-ch02-source/page-039.png` and `tmp/pdfs/253a-ch02-source/page-040.png`.
- Direct renders made from the source PDF matched the two attached full-page renders.
- Original handwritten page labels: none visible on either page. The usable page identifiers are physical pages 39 and 40.
- Ink colors: black carries the main derivation; blue carries the reality-condition line and the completion check; gray carries the residue calculation and the comparison with the earlier evaluation.

## Physical page 39

### Exact transcription in source order

Black prose, with the source line break:

```text
Alternatively, can work with the Fourier transformed
variable
```

The Fourier representation is written as:

```tex
g(\tau)=\int_{-\infty}^{\infty}\frac{dk}{2\pi}\,\widetilde{g}(k)e^{ik\tau}
```

Blue line to the right of the Fourier representation:

```tex
\widetilde{g}^{*}(k)=\widetilde{g}(-k).
```

The black operator equations are:

```tex
\widetilde{A}\cdot\widetilde{g}(k)=(k^2+\omega^2)\widetilde{g}(k).
```

```tex
\widetilde{A}^{-1}\cdot\widetilde{g}(k)=\frac{1}{k^2+\omega^2}\widetilde{g}(k).
```

The black implication and Fourier-space Green function are:

```tex
\Rightarrow\quad G(\tau,\tau')=\int_{-\infty}^{\infty}\frac{dk}{2\pi}\frac{1}{k^2+\omega^2}e^{ik(\tau-\tau')}
```

The gray residue annotation below the Green-function line reads:

```text
residue
```

The word has two gray horizontal underlines. The adjacent gray formula is:

```tex
\frac{1}{2\omega}e^{-\omega|\tau_2|}.
```

The gray comparison statement at the bottom reads, across its two source lines:

```text
In agreement with earlier direct
evaluation (having set ℏ = 1)
```

The handwritten symbol in the parenthesis is the reduced Planck constant. The exact TeX reading is `\hbar`.

### Visual semantics

The page has no explicit heading, diagram, graph, table, or boxed example. The first prose fragment introduces a second route through the same Gaussian calculation. The main equations are black. The blue reality condition sits above the operator equations and reads as a source-added correction or qualification to the Fourier transform of a real function. The black `\Rightarrow` points from the diagonal action and its inverse to the momentum-space representation of `G(\tau,\tau')`.

The gray word `residue` is double-underlined below the Green-function equation. The gray exponential result sits on the same horizontal band. The gray comparison sentence occupies the lower part of the page. There is no contour drawing or pole diagram. The page contains no visible original handwritten page number.

### Ambiguities and source conflicts

- The integration variable in the first equation and the field in the later equations is a handwritten lowercase `g`. The adjacent physical page 38 and the next page 41 use the same `g` glyph. The exact-layer reading is `g`, with confidence high.
- The tilde in `\widetilde{A}` and `\widetilde{A}^{-1}` is drawn above `A`; the superscript `-1` sits to the upper right of `A`. The source-layer TeX keeps the scope as `\widetilde{A}^{-1}`. A later notation audit may need to distinguish this from `\widetilde{A^{-1}}`.
- The gray residue result visibly ends in `|\tau_2|`. The preceding Green function is written with the general arguments `\tau,\tau'`, and the nearby page-38 derivation uses `\tau_1,\tau_2`. The literal `\tau_2` is retained. Its relation to `\tau-\tau'` remains a source-layer mathematical discrepancy.
- The first Fourier formula has no visible terminal period. The blue condition and both operator equations have visible periods. The Green-function integral has no visible terminal period.

### Recommended disposition

Include the heading-level prose fragment, all three black equation groups, the blue condition, the gray residue calculation, and the gray `\hbar=1` qualification in the frozen source packet. Preserve the colors, the double underline, and the implication arrow in the visual audit. Route the `\tau_2` result and the tilde scope for source reconciliation during a later pass. Keep the literal source forms available before any canonical normalization.

## Physical page 40

### Exact transcription in source order

The opening prose reads:

```text
Since A is diagonal in \widetilde{x}(k) basis,
```

The displayed Euclidean action is written first in the bilinear form and then in the time variable:

```tex
S^E=\frac{1}{2}(g,Ag)=\frac{1}{2}\int_{-\infty}^{\infty}d\tau\,g(\tau)A\cdot g(\tau)
```

The Fourier-basis form follows:

```tex
=\frac{1}{2}\int_{-\infty}^{\infty}\frac{dk}{2\pi}\cdot\widetilde{g}(-k)(k^2+\omega^2)\widetilde{g}(k).
```

The next prose fragment reads:

```text
Using the same ∫-by-part trick, we find
```

The final correlator is:

```tex
\left\langle\widetilde{g}(k_1)\widetilde{g}(k_2)\right\rangle=\frac{2\pi\delta(k_1+k_2)}{k_1^2+\omega^2}.
```

A blue check mark appears immediately to the right of the final period.

### Visual semantics

The page has no explicit heading, diagram, graph, table, or boxed example. The opening sentence continues the diagonalization argument from page 39. The action equations are black. A centered multiplication dot appears after `A` in the time-domain integrand and after `dk/(2\pi)` in the Fourier-space integrand. The phrase `∫-by-part` contains a hand-drawn integral sign followed by a hyphen and the words `by-part`.

The final expectation-value equation is black. The blue check mark is a source completion mark placed to the right of the equation. The page contains no visible original handwritten page number.

### Ambiguities and source conflicts

- The opening sentence visibly reads `\widetilde{x}(k)` with a lowercase `x`-shaped character. The equations on pages 39–40 use `\widetilde{g}(k)`, and page 38 uses `g` throughout. The exact transcription keeps the visible `x` and records a source inconsistency with the surrounding `g` notation.
- The first action line contains the handwritten centered dot in `A\cdot g(\tau)`. The Fourier line contains a second centered dot after `dk/(2\pi)`. Both dots are retained.
- The final denominator visibly uses `k_1^2+\omega^2`; the delta function uses `k_1+k_2`. The literal source form is retained.
- The blue check mark has no accompanying word or symbol. It is recorded as a visual annotation. The correlator ends at the preceding period.

### Recommended disposition

Include the opening qualification, both action formulas, the `∫`-by-part prose, and the final momentum-space correlator in the exact source packet. Preserve both centered dots and the blue check mark in the visual audit. Route `\widetilde{x}(k)` versus `\widetilde{g}(k)` to source reconciliation. Treat the check mark as a completion annotation in the source layer.

## Topic boundaries and page transitions

- Physical page 38 ends the coordinate-space functional-derivative and integration-by-parts route, including the identification of `G(\tau_1,\tau_2)`. Page 39 opens the Fourier-transformed alternative.
- Pages 39 and 40 form one continuous derivation. Page 39 diagonalizes `A`, writes its inverse in the Fourier basis, and gives the momentum integral for `G`. Page 40 rewrites `S^E` in that basis and obtains the Fourier-space two-point function.
- Physical page 40 closes on the Gaussian correlator. Physical page 41 begins a new section headed `Example 2` with the quoted phrase `an harmonic oscillator`, followed by a Hamiltonian, a Lagrangian, and a spectral calculation. The page-41 material marks the outgoing topic boundary.

## Page dispositions

| Physical page | Source-layer disposition | Open follow-up |
|---:|---|---|
| 39 | Include every visible prose line, equation, arrow, color, underline, gray annotation, and qualification in source order. | Reconcile the gray `\tau_2` result with the general `G(\tau,\tau')` notation and verify the scope of the tilde on `A`. |
| 40 | Include every visible prose line, equation, multiplication dot, color, and completion mark in source order. | Reconcile the literal `\widetilde{x}(k)` basis phrase with the surrounding `\widetilde{g}(k)` equations. |
