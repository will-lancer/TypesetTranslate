# Pass 1 source capture: physical pages 31-32

## Source identity

- Course section: Physics 253a, Chapter 2, *Lagrangian Quantum Mechanics, Path Integrals, and Perturbation Theory*.
- Combined source: `/Users/wlancer/Desktop/IAS/phy/qft/qft_253abc_book.pdf`, physical pages 31 and 32.
- Source PDF SHA-256: `9e5e4d241fffffa56c1c3df6dce4b83178f75787dd5d794a18c5d0c087769f21`.
- Lane renders: `tmp/pdfs/253a-ch02-source/page-031.png` and `tmp/pdfs/253a-ch02-source/page-032.png`, each 1275 x 1668 pixels.
- Direct source-PDF renders were inspected at 300 dpi. `pdftotext` returns page breaks for these pages, with no usable text layer.
- Original handwritten page label: no label is visible on either physical page.
- Ink: black handwritten text and mathematics, with blue explanatory annotations. The pages have white backgrounds and no printed header or footer.

## Physical page 31

### Exact transcription in visual order

The page continues the harmonic-oscillator `Example 1` from physical page 30. The first visible mark is a filled black bullet.

```text
• f_n(τ) is a basis of functions that
  obeys the prescribed boundary condition,
  orthonormal with respect to a suitable
  inner product, e.g.
```

The inner-product equation is:

```tex
(f_n,f_m) := \int_{-T}^{T} f_n^*(\tau) f_m(\tau)\,d\tau = \delta_{nm}.
```

The next prose fragment reads, with the lowercase initial word preserved:

```text
and, for convenience, diagonalize the
kinetic term in the action:
```

The Euclidean action and its diagonal mode form are:

```tex
S^E
= \int_{-T}^{T}
  \left[\frac{1}{2}(\partial_\tau q)^2+\frac{1}{2}\omega^2 q^2\right]\,d\tau
= \sum_{n=1}^{\infty}
  \left[\frac{1}{2}\left(\frac{n\pi}{2T}\right)^2+\frac{1}{2}\omega^2\right]q_n^2.
```

The path-integral line then reads:

```tex
\int [Dq] e^{-\frac{1}{\hbar}S^E}\cdots
\rightsquigarrow
\int \prod_{n=1}^{\infty} dq_n\,
e^{-\frac{1}{\hbar}\sum_{n=1}^{\infty}
  \frac{1}{2}\left[\left(\frac{n\pi}{2T}\right)^2+\omega^2\right]q_n^2}\cdots
```

The source uses a hand-drawn wavy rightward implication arrow between the two path-integral expressions. The TeX command `\rightsquigarrow` records its visual role. The trailing handwritten dots are retained as `\cdots`; the exact dot count is addressed below.

### Visual semantics

- The basis statement and both displayed equations use black ink.
- The first path-integral expression sits above the product measure and exponential. The product expression is preceded on its left by a black wavy rightward arrow.
- A blue underbrace-like curved line sits below the exponential Gaussian factor in the product expression. The blue label beneath it is exactly `Gaussian`.
- The blue brace is an annotation attached to the product-integral exponential. It does not introduce a new equation.
- There is no graph, coordinate sketch, boxed example, color key, strikeout, or visible correction on this page.
- The qualifications carried by the page are `prescribed boundary condition`, `suitable inner product`, `e.g.`, and `for convenience`.

### Page transition

- Physical page 30 ends with the blue mode definition \(f_n(\tau)=\frac{1}{\sqrt{T}}\sin\frac{n\pi(\tau+T)}{2T}\). Physical page 31 explains the basis property of that same \(f_n(\tau)\).
- Physical page 31 ends with the Gaussian product form and its blue `Gaussian` annotation. Physical page 32 begins with `write` and normalizes the expectation value using the same \([Dq]e^{-S^E/\hbar}\) measure.

## Physical page 32

### Exact transcription in visual order

The page opens with the lowercase board-transition word:

```text
write
```

The expectation-value definition is:

```tex
\langle\cdots\rangle
= \frac{\int [Dq] e^{-\frac{1}{\hbar}S^E}\cdots}
       {\int [Dq] e^{-\frac{1}{\hbar}S^E}},
```

The next prose fragment reads:

```text
Easy to evaluate using Gaussian integral:
```

The Gaussian mode two-point function is:

```tex
\langle q_n q_m\rangle
= \delta_{nm}\,\frac{\hbar}
  {\left(\frac{n\pi}{2T}\right)^2+\omega^2}.
```

The next handwritten transition is:

```text
So
```

The position-space two-point function is expanded as:

```tex
\langle q(\tau_1)q(\tau_2)\rangle
= \sum_{n,m=1}^{\infty}
  \langle q_n q_m\rangle f_n(\tau_1)f_m(\tau_2)
```

The substituted discrete sum is:

```tex
= \sum_{n=1}^{\infty}
  \frac{\hbar}{\left(\frac{n\pi}{2T}\right)^2+\omega^2}
  \cdot\frac{1}{T}
  \sin\frac{n\pi(\tau_1+T)}{2T}
  \sin\frac{n\pi(\tau_2+T)}{2T}
```

The blue limit annotation reads:

```text
Take limit T → ∞,
```

followed by:

```tex
\frac{n\pi}{2T}\equiv k,\qquad
\sum_n\frac{\pi}{2T}\cdots \rightsquigarrow \int dk\cdots
```

The final black expression on physical page 32 is:

```tex
= \frac{2\hbar}{\pi}\int_{0}^{\infty} dk\,
  \frac{\sin k(\tau_1+T)\cdot\sin k(\tau_2+T)}{k^2+\omega^2}
```

The handwritten sine operator begins with a tall `S` in the source. The TeX transcription uses the mathematical operator `\sin` while retaining the source argument form `k(\tau_i+T)`.

### Visual semantics

- The expectation-value formula occupies the upper portion of the page, with a long horizontal fraction bar. The numerator contains an ellipsis after the exponential. The denominator ends with a comma.
- `Easy to evaluate using Gaussian integral:` is black handwriting and functions as the transition into the mode covariance.
- The word `So` is black and sits immediately before the position-space correlator.
- The correlator equations are black. The centered dot between the mode propagator and \(1/T\) is visible in the discrete-sum line. The centered dot between the two sine factors is visible in the continuum expression.
- A large black curved arrow links the discrete mode sum to the blue \(T\to\infty\) annotation.
- The blue annotation contains `Take limit T → ∞,`, the substitution \(n\pi/(2T)\equiv k\), and the sum-to-integral replacement. Its rightward replacement arrow is hand-drawn and wavy.
- The page contains no graph, coordinate sketch, boxed diagram, color legend, strikeout, or visible correction.

### Page transition

- Physical page 32 begins as a direct continuation of the product Gaussian integral on page 31.
- Physical page 32 ends with the half-line \(k\)-integral for the two-point function. Physical page 33 begins with an equality rewriting this expression as a full real-\(k\) integral, then proceeds to the complex \(k\)-plane and the poles at \(k=\pm i\omega\).
- The page-32 bottom equation is therefore an active step in an equation chain. Preserve its leading equality sign when it is placed beside the page-31 and page-33 material.

## Ambiguities and character checks

| Location | Capture | Confidence and handling |
|---|---|---|
| p. 31, both path integrals; p. 32, expectation ratio | The measure glyph is a capital `D` followed by `q` inside square brackets: `[Dq]`. | High visual confidence for `[Dq]`. A calligraphic `\mathcal D q` rendering would be a later notation choice. Keep `[Dq]` in the source layer. |
| p. 31, diagonal action; p. 31, Gaussian exponent | The final mode variable is `q_n^2`. | Resolved from the 300 dpi source render and the preceding page-30 expansion \(q(\tau)=\sum_n q_n f_n(\tau)\). The glyph shape can resemble `f` at lower resolution. |
| p. 31, path-integral transition | The arrow is a wavy handwritten implication arrow. | Encode its semantic role with `\rightsquigarrow`; preserve the arrow shape in the visual-semantics record. |
| p. 31 and p. 32, trailing dots | The source uses hand-drawn dots after the observable and after the sum-to-integral replacement. | Transcribe as `\cdots`. The rendered dots have a variable handwritten count, with no mathematical content beyond an ellipsis. |
| p. 32, blue substitution | The symbol between \(n\pi/(2T)\) and \(k\) has three horizontal strokes. | Capture as `\equiv`, rather than a two-stroke equality sign. |
| p. 32, sine factors | The source writes a tall `S` in `Sin`; the arguments appear as `k(\tau_1+T)` and `k(\tau_2+T)`. | Use `\sin k(\tau_i+T)` in TeX. The operator case is a handwritten glyph choice; the argument structure is clear. |
| p. 32, covariance | The Kronecker symbol subscript reads `nm`. | Capture as `\delta_{nm}`, consistent with the page-31 inner product. |
| p. 31, Euclidean action | The superscript on \(S\) is a clear capital `E`. | Capture as `S^E` throughout. |
| p. 31 and p. 32, frequency | The handwritten frequency glyph is omega. | Capture as `\omega`, with square powers \(\omega^2\) in each displayed formula. |

## Topic boundaries and recommended dispositions

### Conceptual units

1. Basis and diagonalization. The page-31 bullet defines the boundary-adapted orthonormal basis, then diagonalizes the Euclidean oscillator action in the mode coefficients \(q_n\). This belongs with the page-30 Fourier-basis construction.
2. Gaussian factorization. The page-31 path-integral transition rewrites the functional integral as a product over \(q_n\), with the blue `Gaussian` brace identifying the factor to be integrated.
3. Mode covariance and position-space correlator. Page 32 defines normalized expectation values, evaluates \(\langle q_nq_m\rangle\), reconstructs \(\langle q(\tau_1)q(\tau_2)\rangle\), and converts the discrete sum to a continuum integral.
4. Contour-integral boundary. The last page-32 expression leads directly into page 33, where the full real-\(k\) representation and complex-plane pole calculation begin.

### Item dispositions

| Item | Recommended written disposition | Source class |
|---|---|---|
| p. 31 basis prose and inner product | Merge with the adjacent page-30 Fourier-basis argument. Retain `prescribed boundary condition`, `suitable inner product`, and `e.g.` as source qualifications. | `NOTES_EXACT` / `SOURCE_COMPOSITE` |
| p. 31 Euclidean action and diagonal mode sum | Include directly beside the basis definition. Preserve the \(T\) bounds, \(\partial_\tau q\), \(\omega^2q^2\), \(n\pi/(2T)\), and \(q_n^2\). | `NOTES_EXACT` |
| p. 31 product path integral | Include directly as the Gaussian factorization step. Preserve the observable ellipsis and the blue `Gaussian` annotation in the source packet. | `NOTES_EXACT` |
| p. 32 `write` and expectation ratio | Retain in the source transcript. In written prose, merge the board-transition word into the sentence that defines the normalized expectation value. Keep the numerator and denominator measures identical. | `NOTES_EXACT` / `SOURCE_COMPOSITE` |
| p. 32 Gaussian covariance | Include directly. Preserve the diagonal \(\delta_{nm}\) and the denominator \((n\pi/(2T))^2+\omega^2\). | `NOTES_EXACT` |
| p. 32 `So`, mode expansion, and discrete sum | Merge into the correlator derivation. Preserve the mode order, the \(1/T\) factor, both sine factors, and the centered multiplication dots. | `NOTES_EXACT` / `SOURCE_COMPOSITE` |
| p. 32 blue \(T\to\infty\) annotation | Include as the limit instruction immediately before the continuum expression. Preserve the equivalence sign and the sum-to-integral arrow. | `NOTES_EXACT` |
| p. 32 final half-line integral | Include directly and carry its leading equality into the page-33 contour-integral continuation. | `NOTES_EXACT` |

## Capture status

Pages 31 and 32 are fully captured in source order. The recoverable content is handwritten prose, equations, arrows, a blue Gaussian brace, and the blue continuum-limit annotation. The page boundary evidence is recorded for physical pages 30, 31, 32, and 33. The source lane contains no video alignment or transcript claims.
