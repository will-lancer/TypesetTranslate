% Original pages: 332--336
% Figures: Figure 13 placeholder on original p. 333; Figure 14 placeholder on original p. 335.
% Uncertainty log: Original p. 332 begins mid-sentence; one explicit VERIFY comment marks the gauge-current formula typography on p. 334.

zero angular momentum. Because $\phi$ and $\psi$ have non-zero mass, all
other states have (at least for weak coupling) a non-zero energy,
at least equal to the mass of the $\phi$ and $\psi$ particles. They do not
contribute to $\operatorname{tr}(-1)^F$. $\operatorname{tr}(-1)^F$ receives one contribution from the
vacuum at $\phi = +a/g$ and one contribution from the vacuum at $\phi = -a/g$.
Altogether $\operatorname{tr}(-1)^F = 2$, so supersymmetry is unbroken.

Suppose instead that we wish to calculate $\operatorname{tr}(-1)^F$ in the massless
Wess-Zumino model --- that is, with $a = 0$ in (34). The potential
is now simply $V(\phi) = \frac{g^2}{2} |\phi|^4$. Now $m_\phi = m_\psi = 0$, in perturbation theory.

In addition to the ``vacuum'' (which now is a trickier concept), one
can have in perturbation theory states of approximately zero energy
by adding $\phi$ or $\psi$ quanta to the ``vacuum'' in momentum eigenstates of
$\vec p = 0$. (Such states are normalizable in finite volume.) It is difficult
to count these states because any number of $\phi$ particles may
have $\vec p = 0$ and it is difficult --- because of the non-linearity ---
to know which of these states have $E = 0$ exactly, or to count them.
This makes it difficult to calculate $\operatorname{tr}(-1)^F$.

The easiest way to calculate $\operatorname{tr}(-1)^F$ in the $a = 0$ model is to
remember that $\operatorname{tr}(-1)^F$ is independent of $a$. So even if your interest
is $a = 0$, you can calculate $\operatorname{tr}(-1)^F$ by considering $a$ to be non-zero
and large. This makes the calculation easy. So $\operatorname{tr}(-1)^F = 2$. This
illustrates the utility of knowing $\operatorname{tr}(-1)^F$ to be independent of all
parameters.

Going back to the massive model, with $a \neq 0$, there actually is
a simpler argument to prove that supersymmetry is unbroken for small
enough $g$. For supersymmetry to be spontaneously broken,\textsuperscript{2} there
must be a massless Goldstone fermion. In this model, for $a \neq 0$ and
small enough $g$, the elementary fermion is certainly not massless.
Also, for small enough $g$ the elementary $\phi$ and $\psi$ will certainly not
form massless bound states.

% TODO FIGURE: Original Fig. 13 appears here on p. 333.
\begin{wrapfigure}{r}{0.40\textwidth}
\vspace{-0.4\baselineskip}
\centering
\fbox{\parbox[c][1.35in][c]{0.30\textwidth}{\centering TODO\\Figure 13 placeholder\\Original p.~333}}
\par\smallskip
Figure 13

What cannot happen in supersymmetric theories: the fermion
becomes a Goldstone fermion for large enough $g$.
\vspace{-0.5\baselineskip}
\end{wrapfigure}

So for small enough $g$, supersymmetry must be unbroken, there
being no candidate Goldstone fermion. However, one might have
believed that for large enough $g$
the fermion mass goes to zero and
supersymmetry is broken. Such
behavior is very common in the
case of global symmetries, but
the fact that $\operatorname{Tr}(-1)^F = 2$ shows
it cannot happen here --- supersymmetry is unbroken even for
strong coupling.

Also, the $a = 0$ model has a
massless fermion in perturbation
theory. Could not this particle become (at the non-perturbative
level, since it has been proved not to happen in perturbation theory)
a Goldstone fermion? The fact that $\operatorname{Tr}(-1)^F = 2$ shows that this does
not occur.

Now a few simple comments:

(1) If supersymmetry is broken spontaneously at the tree level,
then at the tree level $n_B^{E=0} = n_F^{E=0} = 0$, there being no zero energy
states of any kind. So $\operatorname{Tr}(-1)^F = 0$.

(2) If $\operatorname{Tr}(-1)^F \neq 0$ supersymmetry is definitely unbroken. But
if $\operatorname{Tr}(-1)^F = 0$ we do not know. We may have $n_B^{E=0} = n_F^{E=0} = 0$, and
supersymmetry broken, but we may equally well have $n_B^{E=0} = n_F^{E=0} \neq 0$,
and supersymmetry unbroken.

(3) As in the $a \neq 0$ Wess-Zumino model, it is easy to calculate
$\operatorname{Tr}(-1)^F$ in any model where supersymmetry is unbroken at the tree
level and all particles have mass. It is easy because, there being
no massless particles, only the ``vacuum states'' have $E = 0$ in perturbation
theory. The vacuum states are all (spin zero) bosons so
$\operatorname{Tr}(-1)^F$ is equal to the number of zeros of the classical potential
(there are some since supersymmetry is assumed unbroken at the tree
level). So in all theories with no massless particles at the tree
level, $\operatorname{Tr}(-1)^F$ is positive, and supersymmetry is not spontaneously
broken.

Finally, let us discuss a case in which these methods really
yield interesting results --- supersymmetric non-Abelian gauge theories.
In the simplest such theory, the only fields are the gauge
field $A_\mu^a$ and its partner, the fermi field $\psi^a$, also in the adjoint
representation of the gauge group. The Lagrangian is
\begin{equation}
\mathcal{L} = \int d^4x\, \left(- \frac{1}{4}\left(F_{\mu\nu}^a\right)^2 + \frac{1}{2}\,\bar\psi^a\, i \not\!D\, \psi^a\right)
\tag{35}
\end{equation}
The easiest way to show that this is a supersymmetric theory is to
show that the supersymmetry current
% VERIFY: The scan is faint in equation (36); the tensor-spinor current is transcribed as read from the source image but should be rechecked if stricter glyph-level fidelity is required.
\begin{equation}
S_\mu = \sigma_{\alpha\beta} F^{\alpha\beta a}\, \gamma_\mu\, \psi^a
\tag{36}
\end{equation}
is conserved. This is readily demonstrated, with the aid of some
Dirac algebra and the use of fermi statistics.

This theory is tricky to deal with because of the zero momentum
modes of the massless particles.

The main problem is the gauge field. Only a finite number of
$\vec p = 0$ fermions can fit in the box, and we could count those states.
But the $\vec p = 0$ mode of the gauge field is a problem.

In infinite volume the $\vec p = 0$ mode of the gauge field can be gauged
away. If $A_\mu = C_\mu$ (the $C_\mu$ being constants), then $A_\mu = \partial_\mu \epsilon$, with
$\epsilon = C_\mu x^\mu$. Here the constant can't be gauge away because the gauge
parameter $\epsilon$ isn't periodic.

More generally, the gauge invariant
\begin{equation}
\operatorname{Tr} P \exp i g \oint A_\mu\, dx^\mu
\tag{37}
\end{equation}
for a contour (figure (14)) that runs ``around the box'' is different
at $A =$ constant from its value at $A = 0$, proving that the zero
momentum mode can't be gauge away.

It is possible to come to grips with this problem,\textsuperscript{12} but in the
case of the gauge group $SU(N)$, there is a much simpler approach.
One can choose boundary conditions that the zero momentum mode does
not satisfy. Such boundary conditions are the ``twisted boundary
conditions'' of 't Hooft.\textsuperscript{13} In a special case that is general enough
for our purposes, the twisted boundary conditions, in a box of length
$L$, mean that
\begin{equation}
\begin{aligned}
\phi(x,y,z) &= P \phi(x+L,y,z) P^{-1} \\
&= Q \phi(x,y+L,z) Q^{-1} \\
&= \phi(x,y,z+L)
\end{aligned}
\tag{38}
\end{equation}
where $\phi$ may be $A_\mu$ or $\psi$. For
$P = Q = 1$ this would describe conventional periodic boundary
conditions. 'T Hooft instead requires
$P$ and $Q$ to be constant matrices
that obey
\begin{equation}
PQ = QP \exp 2\pi i/N
\tag{39}
\end{equation}

% TODO FIGURE: Original Fig. 14 appears here on p. 335.
\begin{wrapfigure}{r}{0.42\textwidth}
\vspace{-2.8\baselineskip}
\centering
\fbox{\parbox[c][1.8in][c]{0.30\textwidth}{\centering TODO\\Figure 14 placeholder\\Original p.~335}}
\par\smallskip
Figure 14
\vspace{-0.8\baselineskip}
\end{wrapfigure}

Explicit matrices $P$ and $Q$ that
obey (39) are easily found. For
instance, we may take
\begin{equation}
\begin{aligned}
P &= \alpha
\left(
\begin{array}{ccccc}
0 & 1 & & & \\
& 0 & 1 & & \\
& & \ddots & \ddots & \\
& & & 0 & 1 \\
1 & & & & 0
\end{array}
\right) \\
Q &= \beta
\left(
\begin{array}{ccccc}
1 & & & & \\
& e^{i\delta} & & & \\
& & e^{2i\delta} & & \\
& & & \ddots & \\
& & & & e^{(N-1)i\delta}
\end{array}
\right)
\end{aligned}
\tag{40}
\end{equation}
where $\delta = 2\pi/N$, and $\alpha$ and $\beta$ are constants chosen to ensure det $P =$
det $Q = 1$.

We are entitled to adopt the twisted boundary conditions because,
in the large volume limit, the physics is expected to be
independent of the boundary conditions. If we can show that, with
twisted boundary conditions, the ground state energy vanishes for
any value of the volume, then, in the large volume limit, the energy
vanishes, and supersymmetry is unbroken, for any choice of the
boundary conditions.

According to 't Hooft, the theory formulated as in (38) describes
a world with ``one unit of magnetic flux in the $z$ direction.''
The motivation for that terminology is not essential for our purposes.
For our purposes the key point is that the twisted boundary
conditions eliminate the $\vec p = 0$ mode. It just doesn't satisfy the
boundary condition.

The zero momentum mode $\phi_0$ of a field $\phi$ that satisfies (38)
would have to obey
\begin{equation}
\phi_0 = P \phi_0 P^{-1} = Q \phi_0 Q^{-1}
\tag{41}
\end{equation}
It is easy to see that for $\phi_0$ in the Lie algebra of $SU(N)$, (41)
requires $\phi_0 = 0$. (To commute with $Q$, $\phi_0$ must be diagonal. But a
traceless, diagonal matrix that commutes with $P$ must vanish.) Therefore,
the twisted boundary conditions remove the zero momentum mode.
